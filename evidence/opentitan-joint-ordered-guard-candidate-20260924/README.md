# Private ordered-guard OpenTitan replays

Published as [PR #358](https://github.com/dsellerbrock/iverilog-uvm/pull/358).

The source-tree VVP SHA-256 `bbe72e9db2dcffd0269173e21c52f6a315b9bd97f17dfda225a2c004f8ea2a8f` was used without installing over the runtime serving the live Caliptra job. The pinned, previously compiled CSRNG and OTP images matched their recorded SHA-256 values. Exact arguments, working directories, runtime and image hashes, and logs are in the paired `*-private-result.json` and `*-private-runtime.log` files.

The same private VVP passes the prior ordered/proven-guard focus 22/22 and six new paired 2017/2023 cases 6/6, with exact gold comparison. [focus-summary.json](focus-summary.json) records each command and verdict; raw new-case compile and runtime streams are retained locally. Paired ordered-randc controls also pass 6/6 in the worker's local check. The registered dedicated legacy focus passes 34/34 and the registered JSON focus passes 34/34 on a private wrapper using source-tree ivl SHA-256 302d709432e386f59a1c849d91c2fbe496b858dfde870641496888e7290db890 and the VVP above. The final expanded broad legacy gate passed 6581 total with 6576 ordinary passes, zero failures, two not implemented, three expected failures, bundled VPI 131/131, negatives 155/155, and runtime invariants 15/15. Real-DPI UVM passed 358/358 with zero failures/skips and the real umbrella loaded. Three further paired cases for independent same-stage weights, conditional randc rejection, and prefix-cap rollback are registered in [the focused list](../../ivtest/regress-joint-proven-guard-legacy.list), with matching JSON entries and golds. The final full JSON gate passed 3,616/3,616 with zero failures. Exact commands, result summaries, and tool/log hashes are in [legacy](private-final-legacy-gate-result.json), [JSON](private-final-json-gate-result.json), and [real-DPI UVM](private-real-dpi-uvm-result.json) records. The shared installed compiler/runtime remain unchanged.

Both released smokes advance past the earlier `cfg.randomize()` unresolved-guard diagnostic, but **both remain 0/1 DV**. CSRNG reports a null virtual-interface `%wait/vif/posedge` error and a TL-UL end-of-simulation assertion at 0 ps; its VVP exit 0 is not a pass. OTP raises a 0 ps `[DCLPSQ]` `p_sequencer` cast UVM_FATAL and a TL-UL end-of-simulation assertion; its VVP exit is 1. Neither run has a pass marker or meaningful stimulus/scoreboard completion. These distinct observations are parked as DD-055 and DD-056 for separate triage. The candidate runtime result is a solver advance, not an OpenTitan application pass or IEEE qualification.

Read-only follow-on triage has paired strict 2017/2023 REDs for the separate [CSRNG nested-VIF handoff](../opentitan-csrng-nested-vif-handoff-20260924/README.md) and [OTP class-specialization identity](../opentitan-otp-dd056-factory-registry-20260924/README.md) blockers. These do not change the pinned DV failures or qualify an application pass.
