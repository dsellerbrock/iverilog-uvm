# Active-package typedef qualification

The pinned OpenTitan checkout is `a78922f14a8cc20c7ee569f322a04626f2ac6127` and remains clean. This candidate starts from merged PR #372 at `6014f8ebf664a6ecca84536c0d7fc2a30fa1ead4`. Its private installed Icarus compiler engine (`local-install/lib/ivl/ivl`) has SHA-256 `f045786db709fa604990b8de166cfc78c84896acff8850ccbdac3d8eef0978d0`. Both released-DV replays explicitly used `-gcommercial-unsafe`; they are nonstandard compatibility evidence, separate from strict IEEE reducers.

The legal [reducer](../../ivtest/ivltests/sv_package_self_typedef.v) uses a typedef declared before `p::T` inside `package p`, in a class property and a function return/formal. It also checks escaped package spelling, a nearer same-name class, explicit import, and inherited class typedef. The pre-fix compiler rejected the legal case in both `-g2017` and `-g2023` ([2017 RED](red-sv_package_self_typedef-2017.log), [2023 RED](red-sv_package_self_typedef-2023.log)). The patched compiler compiles and runs it with `PASSED` in both editions. Unknown, forward, and shadowed-member controls reject in both editions. The focused [JSON](focused-modern.log) and [legacy](focused-legacy.log) harnesses pass 19/19 and 17/17 respectively, including neighboring self-qualified method, subroutine, enum-cast, missing-task, and constant-function regressions. Those invalid neighbors still reject; their expected diagnostics were updated for the new package lookup. [SVA dual-engine](sva-nfa.log) passes 62/62.

| Pinned OpenTitan row | PR #372 unsafe baseline | This compiler, unsafe | Released DV |
|---|---|---|---|
| EDN UVM compile | exit 11, 7 hard errors on self-qualified types | exit 0; original syntax errors gone; three dropped covergroup bins remain hard semantic failures | No runtime |
| EDN runtime lane | exit 11, 7 hard errors | exit 0; same three dropped bins prevent the matrix runner from starting runtime | 0/1 qualified |
| OTBN UVM compile | exit 4, 3 errors at `otbn_env_cov.sv:2294` | original `access_e` parse failure gone; later compile exits 208 with 233 hard diagnostics | No runtime |

The [EDN matrix](edn-matrix.json) retains the exact compile commands, source and tool fingerprints, setup logs, and diagnostics. Its three bins `none`, `some`, and `all` are dropped from a runtime range expression; the runner correctly reports FAIL even though both compile processes exit zero. The separate [OTBN matrix](otbn-matrix.json) reports later covergroup legality, nonconstant-index, packed-struct member, and hierarchy failures. This one-blocker change does not address them. Neither named replay adds a qualified DV pass to the PR #372 clean **0/49 baseline**; a full current-image matrix was not run. No pass banner or compile success was counted as released DV.

`make check` passed. The first full ivtest gate exposed three expected-error diagnostic gold mismatches while its VPI 131/131, negative 155/155, and malformed-runtime 15/15 subgates passed; those cases are now in the focused lists with updated exact gold output. The unchanged-binary [full legacy rerun](legacy-final.log) passed 6,686 total, with 6,681 ordinary passes, zero failures, two not implemented, and three expected failures. The independent [real-DPI UVM regression](real-dpi-uvm.log) passed 358/358 with zero failures or skips and the REAL DPI umbrella loaded.
