# OTP arbiter packed-tree RED

Published as [PR #362](https://github.com/dsellerbrock/iverilog-uvm/pull/362). After PRs #363–#370 merged, this branch retains only its distinct paired packed-driver and SDF tests; the overlapping compiler and resolver changes from this branch were superseded by PR #368. The original evidence below is historical. Its narrow 1/1 smoke assessment did not account for six intended covergroup bins dropped at compilation. The current qualified released OTP DV result is **0/1**, even though the later pinned PR #370 runtime completed checked traffic with a pass marker. See [the later qualification record](../opentitan-otp-dai-rand-index-20260925/result.json).

The [post-#370 reconciliation](reconciliation_after_pr370.json) passes the six retained 2017/2023 legacy reducers, all 14 JSON packed/SDF controls, and the full JSON suite at 3,693/3,693 using the hash-matched PR #370 private tool. No compiler or VVP source differs from merged `main`; the existing PR #370 real-DPI UVM and pinned OTP results therefore remain the current source validation and released-DV classification.

The pinned OpenTitan OTP smoke met the historical narrow 1/1 runtime gate on that private compiler, but is unqualified under the current full-check gate. The prior PR #360 image reached `tb.dut.u_otp_arb` and reported the enabled `GrantKnown_A` assertion at 1,559,061 ps; that run had no pass marker or meaningful checked traffic. Its compile command, binary hashes, and native DPI bundle are recorded in [the class-identity replay](../opentitan-otp-class-identity-20260924/README.md).

A copied VVP image with diagnostic prints only reports `req_i=00011111111111`, `ready_i=1`, `gnt_o=000xxxxxxxxxxx`, and the 31-bit `mask_tree` entirely Z at the failure. The pinned `hw/ip/prim/rtl/prim_arbiter_tree.sv` assigns `mask_tree[0]=0` and derives each child bit from earlier tree nodes; these selected continuous assignments form an acyclic dependency graph. The [emitted bytecode excerpt](otp_vvp_excerpt.txt) instead has a `.part` read of the full packed tree feeding that tree's `.concat8`. This locates the first wrong value before the later registered priority-mask and grant X values. The [diagnostic script](otp_arb_diagnose.py) and [output](otp_arb_diagnose.stdout) preserve the copied-image probe; the archived output has only trailing whitespace trimmed, with its current hash recorded in `paired_red_result.json`. They do not change the pinned application source or release checks.

The [paired strict 2017/2023 record](paired_red_result.json) uses the private PR #360 compiler/VVP. A legal three-bit disjoint packed-bit assignment tree compiles but produces `tree=zzz` and fails at runtime in both editions. Its scalar equivalent passes `100` in both editions. An overlapping continuous-driver control on one `logic` bit is rejected at elaboration in both editions. These are reducer results, not released DV passes.

The [pre-fix netlist stage probe](stage_probe_result.json) locates the first wrong representation in `collapse_partselect_pv_to_concat` (`netmisc.cc`), called from `PEIdent::elaborate_lnet_common_` (`elab_net.cc`) while selected-assignment RHS connections can still be incomplete. Its default netlist has `NetConcat8` driving the whole tree from a full-vector read and the reducer fails with `tree=zzz`. A diagnostic compile with the existing `-pDISABLE_CONCATZ_GENERATION=true` keeps the selected `NetPartSelect(PV)` drivers and runs to `tree=100`. Both probe outputs use the same pre-fix private compiler and VVP; this flag is only causal evidence, not a release option or the final fix.

Removing the eager collapse alone passed the DAG reducer but exposed a strength regression. A fully covered independent weak-driver vector connected through `tran` resolved to `a=10 b=0` before the patch and `a=1x b=x` with the first candidate: the later `cprop.cc` blend used a nontransparent concat and upgraded the weak driver. The [strength differential](strength_differential_result.json) records a dedicated test passing with the installed pre-fix compiler, failing on the intermediate image, and passing on the corrected private compiler. The final `cprop.cc` blend uses transparent concat only for eligible fully covered vectors, preserving the previous `NetConcat8` strength behavior; the existing combinational-dependency guard keeps dependent drivers separate. [Corrected netlists](dag-corrected.netlist.txt) retain three selected drivers for the DAG and use [transparent concat](strength-corrected.netlist.txt) for the independent strength control.

The corrected private ivl SHA-256 `5164a40b3cd44b4fd58a42accf85c1433ecd42a3836f9eb304d6518ff8d31931` and unchanged private VVP `bbe72e9d` pass [six paired 2017/2023 focused cases](corrected_focus_result.json): legal DAG and weak/strong `tran` runtime, plus illegal overlap compile rejection. The DAG also covers reversed assignment order, a 31-bit boundary, X/Z propagation, and a real cycle without claiming a standard fixed point for cycles. Four neighboring runs pass, including initial port delivery. The candidate was independently reviewed without further source or test findings.

The [first candidate OTP compile](first-candidate-otp.compile.result.json) succeeded, and its [runtime log](first-candidate-otp.runtime.log) reached 54.99 µs with zero reported UVM errors/fatals but hit a 180-second diagnostic timeout before a pass marker. This is **0/1 released DV** for that candidate. The [corrected pre-main compile](pre-main-otp.compile.result.json) used the same pinned filelist and native DPI exports; its [completed runtime](pre-main-otp.runtime.result.json) met the historical narrow 1/1 runtime gate. The merged-main private ivl SHA-256 is `0f770dc5dbe7e7be9853d27eebc365dc7b3dd65808ac87671494c86b6e638b81` with unchanged private VVP `bbe72e9d…`; its [focus record](integrated_focus_result.json) passes legacy and JSON 6/6 each. A strict 2017/2023 [cross-feature control](main_intersection/result.json) also passes legal packed-member dependencies and rejects same-element overlap.

The [integrated OTP compile](integrated-otp.compile.result.json) used the same pinned filelist. Its [completed runtime](integrated-otp.runtime.result.json) exited zero at 132,917,271 ps with one `TEST PASSED CHECKS`, zero fail markers, zero assertion/runtime errors, and UVM summary 2,492 INFO/0 WARNING/0 ERROR/0 FATAL. The [traffic assessment](integrated-otp.log_assessment.json) counts 2,470 OTP scoreboard callbacks strictly after the smoke sequence-start log, through enabled core TL A/D monitor-FIFO dispatch. This is meaningful checked traffic for the narrow released OTP smoke; it does not establish separate DAI or key-request coverage. The integrated and pre-main raw runtime logs are byte-for-byte identical. Archived runtime logs trim trailing whitespace only and retain both raw and archived hashes in their result records.

The pre-SDF private VVP passes the [full legacy gate](integrated-legacy_gate.result.json) with 6,593 total, zero failed, VPI 131/131, negatives 155/155, and runtime invariants 15/15; the [real-DPI UVM gate](integrated-real_dpi_uvm_gate.result.json) passes 358/358. Its full JSON gate failed 1/3,632 on `br_gh1248`, so that image was not PR-qualified. The [focused SDF failure](sdf_interconnect_regression/result.json) shows the removed early concat leaves `i1/Y -> .part/pv -> resolver -> .part/sa -> i2/A`, where the old lookup dropped 10 ps. The final private VVP `5053e2f4` records resolver input sources and inserts the delay only when the named PV is the sole structural driver of that bit; [focused SDF tests](sdf_interconnect_regression/final_focus.result.json) pass `br_gh1248` and all eight paired matching/wrong-bit/overlap/extension-port controls 9/9; the [six-input extension proof](sdf_interconnect_regression/extend_focus.result.json) places the named source at resolver input 4. The [final pinned OTP replay](final-otp.runtime.result.json) again met the historical narrow 1/1 runtime gate, with [checked traffic and zero-error assessment](final-otp.log_assessment.json); its raw runtime log is byte-for-byte identical to the prior archived log. The final private [legacy gate](final-legacy_gate.result.json) passes 6,593 total with zero unexpected failures, VPI 131/131, negatives 155/155, and invariants 15/15; the [full JSON gate](final-json_gate.result.json) passes 3,640/3,640 including `br_gh1248` and every new SDF control; the [real-DPI UVM gate](final-real_dpi_uvm_gate.result.json) passes 358/358. The final legacy and UVM logs are byte-for-byte identical to their pre-SDF archived logs and are reused. The historical local gate was complete; current released-DV qualification remains 0/1. Unknown resolver input provenance, including disjoint constant sources, remains fail-closed for SDF; general multi-driver interconnect timing is not claimed. The pinned OpenTitan source, installed Caliptra compiler/VVP, assertions, and released test sequence remain unchanged.
