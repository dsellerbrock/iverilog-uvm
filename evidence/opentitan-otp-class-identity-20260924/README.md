# Pinned OpenTitan OTP class-specialization replay

The pinned Earlgrey-PROD-M6 checkout remains clean at `a78922f14a8cc20c7ee569f322a04626f2ac6127`. This replay uses the existing hash-checked, disposable OTP RAM-path overlay from [PR #357](../opentitan-otp-backdoor-path-overlay-20260924/README.md), the private ordered-guard VVP from [PR #358](../opentitan-joint-ordered-guard-candidate-20260924/README.md), and the class-key compiler candidate in this branch. It does not edit the pinned OpenTitan source or the installed Caliptra tools. Exact commands, working directories, binary hashes, and logs are in [compile_result.json](compile_result.json), [smoke_result.json](smoke_result.json), and [result.json](result.json).

The pre-fix [factory reducer](../opentitan-otp-dd056-factory-registry-20260924/README.md) failed a cast between positional and named/default spellings of the same `sequencer#(32,33)` class in both 2017 and 2023 mode. The central `canonical_specialization_parm_key_` previously used source-sensitive keys when a class had more than one value formal. The scoped fix keys supported concrete value actuals in declaration order, reusing a same-type earlier formal for a bare dependent default. Paired registered tests exercise equal positional/named/default identity, unequal values, distinct type and declaration owners, and a currently unsupported dependent-width formal. The latter fails during type binding before key construction and is recorded as a separate support limit. The original factory reducer now accepts the equal object and rejects the unequal object in both modes; [factory_result.json](factory_result.json) retains those runs.

[Focused legacy and JSON runs](focus_result.json) pass 6/6 each. The [full legacy gate](legacy_gate_result.json) passes 6,587 total with 6,582 ordinary passes, zero failures, two not implemented, three expected failures, bundled VPI 131/131, negatives 155/155, and runtime invariants 15/15. [Full JSON](json_gate_result.json) passes 3,622/3,622 with zero failures. The [real-DPI UVM gate](real_dpi_uvm_gate_result.json) loads its native DPI umbrella and passes 358/358 with zero failed or skipped. These are regression results, not released OTP DV passes.

The pinned OTP compile exits zero on private `ivl` SHA-256 `57eec3fcd5e3b38c614f1e48ceb5f774e275e7b8d3b874e5e8eebb7feca423f3`. It still emits 46 nonfatal `sorry` diagnostics, including dropped covergroup bins and procedural-continuous-assignment warnings. [dpi_build_result.json](dpi_build_result.json) records the native C/C++ sources and generated DPI export shim compiled into the disposable `vvp -d` bundle. The runtime reports no missing DPI symbol and clears the prior time-zero `DCLPSQ` failure.

The released `otp_ctrl_smoke_vseq` is **0/1 released DV**. It terminates at 1,559,061 ps after one `GrantKnown_A` assertion failure in `prim_arbiter_tree.sv:206`; UVM reports one error, zero fatals, and `TEST FAILED CHECKS`. There is no pass marker or meaningful checked traffic. VVP exits zero because UVM calls `$finish`, so process exit is not a pass. The assertion and all other released checks remain enabled. This later failure belongs to a separate blocker, not this class-identity PR. Focused reducer and UVM regression results are separate from the released DV numerator.

Read-only hierarchy inspection found [four candidate arbiter instances](arbiter_scope_candidates.txt) in the OTP controller, but the UVM report omits the instance path. The failing output is `gnt_o`, and the responsible instance and X producer are still unproven. A subsequent diagnostic should report the instance path and `req_i`, `ready_i`, and `gnt_o` at the failing sample without disabling the assertion.
