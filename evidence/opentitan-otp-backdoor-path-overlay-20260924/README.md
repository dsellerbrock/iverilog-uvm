# Pinned OpenTitan OTP RAM-path compatibility replay

The pinned Earlgrey-PROD-M6 checkout is `a78922f14a8cc20c7ee569f322a04626f2ac6127` and remained clean. Its `hw/ip/otp_ctrl/dv/tb.sv` and the FuseSoC-generated copy both have SHA-256 `9ae010405678fbaad44c83143793345af6e5aa1e0152bd2e06a9c838f3d63f1a`. The [named helper](../../docs/conformance/release_overlays/opentitan/otp_mem_path_overlay.py) requires that exact input and writes only a new disposable file with SHA-256 `a7bd1964e2c7e626d5a25a04597129417a7c47f89c95069da6af86dd094689c6`. The only source change puts the two `MEM_MODULE_PATH` and `MEM_ARRAY_PATH` bodies on their `define` lines. The real RAM hierarchy, `$size`, `$bits`, ECC selection, and `uvm_hdl_check_path` fatal remain intact.

The original released `otp_ctrl_smoke_vseq` reached a time-zero `mem_bkdr_util.sv:76` fatal because the VVP path string began with 18 spaces. The [overlaid VVP image excerpt](otp-path-provenance.txt) shows the corrected hierarchy begins at the first byte. The [paired macro controls](reducers/results.json) compile and run in both 2017 and 2023 mode: the continued and literal-space cases yield 18 spaces, while the flattened case yields none. This identifies the source-compatibility workaround; it does not establish an IEEE defect in macro whitespace handling.

The helper's `--self-check` passed against the pinned testbench and its output matched the pilot source byte for byte. The pinned FuseSoC source-list copy changed only line 311, from the generated `tb.sv` to the disposable overlay; the outer command-file copy changed only its `-c` source-list path. Both copies are retained as [source-list.scr](source-list.scr) and [matrix-iverilog.scr](matrix-iverilog.scr). The exact compile command and working directory are in [compile_result.json](compile_result.json); the exact VVP command, `IVL_SVA_NFA=1`, and working directory are in [smoke_result.json](smoke_result.json). The private Icarus SHA-256 is `302d709432e386f59a1c849d91c2fbe496b858dfde870641496888e7290db890`; the installed VVP SHA-256 is `e9b7a64a8643973c9bc6597ca604285bbd718cf733e45181f6155390d9fdc8c6`. No installed tool or pinned source was edited, and Caliptra's live VVP was left running.

The overlay compile exited 0 with no hard errors, but its [log](compile.log) has 46 nonfatal `sorry` diagnostics: six dropped covergroup bins and 40 procedural-continuous-assignment warnings. It also warns that one `otp_ctrl_smoke_vseq` constraint item is ignored. The original OTP compile has the same 46 `sorry` diagnostics; the earlier PR #356 metadata mistakenly counted zero, corrected in its retained result metadata without altering the original compiler log. Compile exit is not a clean DV result.

The RAM-path fatal disappears and UVM starts, but the released [smoke log](smoke.log) still fails at 0 ps: the joint sampler reports an unresolved random guard, then `dv_base_test.sv:41` raises one `UVM_FATAL` because `cfg.randomize()` failed. There are 512 follow-on end-of-simulation assertion errors, zero pass markers, and no meaningful checked traffic. VVP's process exit is zero, so the OTP result is **0/1 DV pass** under this nonstandard source-compatibility profile. A [trace replay](smoke_trace.log) identifies the failing dynamic `otp_ctrl_env_cfg` solve. [Paired nested-object reducers](cfg_reducers/result.json) in 2017 and 2023 mode show standalone guarded distribution randomization succeeds, but the same pattern nested in one or two random children fails with the same diagnostic. They implicate the existing VVP conditional joint-distribution blocker; they do not uniquely identify the offending item in the full OTP config.

[result.json](result.json) records hashes and verdicts. The next solver work is `OT-CSRNG-CFG-JOINT-DIST`, shared with OpenTitan CSRNG. None of this OTP smoke or the separate Caliptra source probe counts toward Caliptra's selected 52 L0 tests.
