mission: Full standards-correct compiler/UVM campaign remains active; formal backend separate, preserve1364, defer1801.
branch: agent/scope-sva-next-20260921
head_checkpoint: e42f114cd
history: evidence/review-20260920/next-pwrmgr-hdl-path-assessment/before-CAMPAIGN.yaml
merged_baseline:
  pr: https://github.com/dsellerbrock/iverilog-uvm/pull/307
  main: 3d3f8086b91bb41c8833dd72b46e5e3655b89559
  qualification: docs/conformance/session_logs/2026-09-21_compiler_batch_qualification.json
  ci_run: 35588718454
  last_observed: Ubuntu22.04 and24.04 pass; macOS and3Windows jobs still running. No failure observed.
next_batch:
  integrated_fixes: 7
  revisions: [25653fdc0, acb786965, 346b48f68, c8e6cb97c, e42f114cd]
  evidence_index: docs/conformance/CURRENT_WORK.md
  pending: Required broad gates near10fixes, then coherent PR; no next-batch PR yet.
  processes: Release retests completed; Terra event runtime-scope assessment active; exploratory post-wake patch rejected, Sol private VPI packed-element implementation authorized.
applications:
  pins: OpenTitan Earlgrey-PROD-M6; Caliptra v2.1.2 and AdamsBridgev2.0.3. Sources unchanged.
  release_replay: docs/conformance/session_logs/2026-09-21_current_release_retest.json
  release_limits: Fresh bounded replay at51f3a67c0; crossbar and Caliptra power2round pass, GPIO and pwrmgr fail. Not whole-app qualification.
  latest_pwrmgr: docs/conformance/session_logs/2026-09-21_pwrmgr_startup.json
  pwrmgr_state: Compiles but semantic fallback warnings exclude qualification; startup UVM_FATAL on indexed packed HDL path, despite process exit0.
  live_path_probe: evidence/review-20260920/next-pwrmgr-hdl-path-assessment/application-probe.json
workspace:
  user_changes: AGENTS.md remains dirty and untouched.
  graph: Shared canonical graph refreshed from main3d3f8086b; feature worktree must not update graph.
  cleanup: docs/conformance/session_logs/2026-09-21_canonical_graph_cleanup.json
resume:
  next: Review VPI packed-element and compound virtual-interface event proposals; update ACTIVE_WORK before new implementation.
  exact_check: git status --short --branch
  ownership: Coordinator shared integration/builds; lower-model workers private disjoint patches.

selected_event_lane: COMPOUND-VIF-EDGE-EVENTS; Terra medium private elaborate.cc fix authorized after paired failing reducer. VPI packed-element implementation now authorized for Sol medium; scope and acceptance in ACTIVE_WORK.

parallel_boundary_evidence: docs/conformance/session_logs/2026-09-21_parallel_pwrmgr_boundaries.json

event_pulse_boundary: docs/conformance/session_logs/2026-09-21_vif_event_pulse_boundary.json

structural_event_lane: Terra authorized private structural VIF proxy implementation; coordinator owns shared target/parser glue, disjoint from Sol private VPI edits.

active_workers: constraint_guard_semantics (Sol medium packed VPI) and vif_proxy_implementation (Sol medium structural events); previous Terra worker stopped after repeated plan-only responses.

ready_after_active_lanes: VARIABLE-OUTPUT-PORT-DIRECTION; paired failing runtime and explicit-wire control in docs/conformance/session_logs/2026-09-21_variable_output_port_direction.json. Do not concurrently edit event-owned elaborate.cc.

packed_vpi_candidate_checkpoint:
  state: Shared uncommitted candidate built and installed; six integrated fixes unchanged.
  evidence: docs/conformance/session_logs/2026-09-21_packed_vpi_candidate_replay.json
  forward_metadata: Deferred net resolution repaired; paired forward-net runtime passes.
  pending: Richer callbacks and force/release sibling overlap semantics; permanent tests and review before commit.
  applications: Caliptra power2round2048 passes with working generator; OpenTitan pwrmgr still UVM_FATAL BUILDERR.
  next: Review worker force/release scope proposal and malformed metadata boundary; integrate structural event patch only when ready.

resolver_order_checkpoint:
  build: Serial build and install completed after resolver-order delta.
  evidence: evidence/review-20260920/next-vpi-packed-element-fix/resolver-order-results.json
  tests: Paired valid forward-net passes; malformed fixture currently stops at syntax error and requires repair before negative claim.
  worker: Sol packed-VPI lane authorized exact force/release overlap expansion; structural event lane remains independent.

packed_vpi_review_checkpoint:
  evidence: docs/conformance/session_logs/2026-09-21_packed_vpi_review_checkpoint.json
  crossbar: Fresh stable-release run passes180requests354scoreboard items; zero UVM warnings/errors/fatals.
  new_red: Existing packed_api now aborts for vpiSuppressVal in callback payload refresh; Sol repairing, no semantic commit permitted yet.
  event: Private structural build passes paired compound/table/pulse/mixed-or tests; automatic null rebind under standards review.

structural_review_pending:
  source: evidence/review-20260920/next-compound-vif-edge-fix/sol-structural/patch/event.cc
  findings: Relay ownership across repeated rebind; root_word selection contract; malformed descriptor compile_errors; automatic default root for future activations.
  action: Worker notified with bounded acceptance checks; private patch not integrated.

packed_boundary_checkpoint:
  positive: Paired2017/2023 INT_MIN/INT_MAX small-width dimensions and two-state packed memory reads pass; next-vpi-packed-element-assessment/root-oracle/boundary-results.json.
  negative: Root valid-bytecode mutation proves trailing comma wrongly accepted; missing target lacks focused diagnostic; first-runtime/metadata-negative-review.json.
  next: Worker repairing words.cc metadata boundaries and callback suppression/overlap; coordinator must rerun these exact cases before integrating.

vif_null_semantics_review: docs/conformance/session_logs/2026-09-21_vif_null_review.json; both editions25.9 require fatal on attempted null use, while dormant structural initialization is not procedural evaluation. Worker designing correct activation boundary.

user_priority_checkpoint:
  current: Two selected OpenTitan-driven IEEE fixes remain under active implementation and correction.
  next: Reproduced variable-output direction semantics, plus bounded Caliptra stable-release debt assessment by retained Luna.
  success_measure: Integrated runtime-correct fixes with evidence, not documentation volume or compilation alone.

vpi_force_candidate_validation:
  evidence: docs/conformance/session_logs/2026-09-21_vpi_force_candidate_validation.json
  result: Built and installed; rich access/force/forward cases pass both editions;13neighbor VPI tests pass; malformed metadata rejects correctly.
  pending: Permanent tests, nested callback dispatch lifetime, final review before semantic commit.
  next: Review worker regression patch and lifetime evidence, then integrate validated fix.

vpi_full_candidate_review:
  evidence: docs/conformance/session_logs/2026-09-21_vpi_full_candidate_review.json
  suite:110cases106pass4goldmismatch:
    correction: Four legacy partselect callback golds corrected to selected value per38.36.1;4focused reruns pass.
  pending: Reject illegal new packed variable leaf force/release callback registrations; integrate revised legal-prefix permanent regressions.
  caliptra: compress_tb probe needs missing package dependency restored in harness, not application edits.

packed_leaf_callback_checkpoint:
  source: New packed variable scalar force/release callback registrations now rejected under38.36.1; legal prefixes and net bits preserved.
  build: Serial build/install passed.
  tests: Corrected rich API passes both editions with two expected type49 diagnostics; legal-prefix nested overlap test passes both editions.
  evidence: evidence/review-20260920/next-vpi-packed-element-fix/rich-runtime/leaf-reject-corrected-results.json
  next: Integrate final permanent regression patch, run its exact manifest cases, then review semantic commit readiness.
caliptra_compress_probe: External filelist correction resolves missing package; unchanged TB then fails missing mem_wr_req/mem_wr_data ports. Upstream test validity issue; another compatible TB under assessment.

packed_vpi_local_integration:
  evidence: docs/conformance/session_logs/2026-09-21_packed_vpi_integration.json
  status: Focused tests and116/116VPI pass; local semantic checkpoint, broad batch gates pending.
  next: Continue structural VIF event implementation and choose next independent reproduced debt after worker handoff.
