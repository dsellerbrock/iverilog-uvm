campaign: IEEE1800-UVM-continuous
mission: IEEE 1800-2017 and 2023 independently, IEEE 1800.2/UVM separately, unmodified stable OpenTitan and Caliptra DV, and a sound formal foundation.
branch: agent/opentitan-nba-20260923
baseline: origin/main 3be18e4b6448671ed2d1323a133d147ff851bb19 (PR326 and PR327 merged)
active: .ai/ACTIVE_WORK.yaml
checkout_ledger: .ai/ISSUE_CHECKOUT.csv
active_blocker:
  id: TASK-CODEGEN-ERROR-DROPPED
  phase: LOCAL_GATES_PASSED_PR_PENDING
  requirement: Failed task-body target generation must propagate its error count to the compile result.
  evidence: evidence/opentitan-codegen-assessment-20260923/nba_task_drop_repro.sv
  next: Publish the coherent compiler PR; do not close until integrated and required checks pass.
parallel_lanes:
  - id: VIF-NBA-SELECTED-BIT
    phase: LOCAL_GATES_PASSED_PR_PENDING
    scope: Dynamic selected-bit NBA on a class or virtual-interface packed property; target lowering and VVP runtime only.
    standards: IEEE 1800-2017/2023 10.4.2.
    evidence: evidence/opentitan-codegen-assessment-20260923/vif_nba_selected_bit_pref_fix.sv
    next: Publish with task-error lane; follow remaining SPI semantic warnings separately.
candidate_qualification:
  source_test_commit: a1c94078b
  record: docs/conformance/session_logs/2026-09-23_opentitan_nba_codegen_focus.json
  result: All required local compiler, ivtest, JSON, SVA NFA, and real-DPI UVM gates passed; GitHub CI pending.
baseline_qualification:
  record: docs/conformance/session_logs/2026-09-23_pr326_postmerge_qualification.json
  result: Required local gates passed on merged compiler/test tree; real-DPI UVM 358 passed with -g2012. GitHub CI pending.
release_profiles:
  record: docs/conformance/release_overlays/README.md
  published: https://github.com/dsellerbrock/iverilog-uvm/pull/327
  meaning: Pinned stable sources plus target-specific selected patches and observed necessary options; not full DV qualification.
application_followup:
  caliptra: Adams Bridge rej_bounded patch focused-tested; upstream PR303 open; pristine and full DV unqualified.
application_status:
  opentitan: Candidate flagged SPI compile exits 0 with zero target errors, but four unresolved foreach indices and one dropped queue-pop expression preclude semantic compile or DV qualification. With upstream -DUVM_REGEX_NO_DPI enabled, fixed-seed SPI smoke advances past time-zero UVM build and aborts on a VIF event-relay runtime form at 3673793 ps; that mechanism is claimed in Claude's separate lane. Strict mode still rejects the packed container type mismatch.
  caliptra: rej_bounded patched-testbench 100-vector unit replay passes on this worktree's compiler and current Adams Bridge main; pristine release fails and full DV is unqualified.
release_profile: docs/conformance/release_overlays/README.md # "released OpenTitan/Caliptra" means pinned source plus target-specific patches and options; not full DV qualification.
preservation:
  user_dirty_and_unrelated: Existing unrelated working-tree modifications and untracked files were left untouched.
  documentation_scope: Post-merge qualification and the two selected compiler lanes are tracked; pinned application releases remain unmodified.
  history: Earlier dated logs remain unchanged.
  canonical_graph: Shared graph preserved and queried for navigation; it may lag merged main, so current source is checked before edits.
