campaign: IEEE1800-UVM-continuous
mission: IEEE 1800-2017 and 2023 separately, IEEE 1800.2/UVM separately, pinned released OpenTitan and Caliptra DV with selected overlays, and a sound formal foundation.
branch: agent/caliptra-late-default-clocking-20260923
baseline: origin/main 42f324c90294ed7356a467e8b6c95d94a9ba2c6e after PR331 merged; PR332 and PR333 remain open on frozen exact heads.
active: .ai/ACTIVE_WORK.yaml
checkout_ledger: .ai/ISSUE_CHECKOUT.csv
active_blocker:
  id: CALIPTRA-LATE-DEFAULT-CLOCKING
  phase: FOCUSED_TESTED
  evidence: evidence/caliptra-late-default-baseline-20260923/baseline.json on origin/main 42f324c90, paired red/early/no-default and pinned ECC compile controls; installed ivl SHA 1ddd808cd9ffda39.
  next: Publish the committed pform.cc correction after review and revision-scoped evidence; pinned ECC formal now stops only at separate released-source DSA_NOP.
parallel_active_blocker:
  id: OT-ADC-CASTED-UNBASED-INLINE-MACRO-ARG
  phase: REPRODUCED
  evidence: evidence/opentitan-next-independent-20260923/result.json on pinned clean Earlgrey-PROD-M6 adc_ctrl_sim.
  owner: Codex + reused Luna; ivlpp/lexor.lex and dedicated macro regressions only; coordinator owns shared builds and test output.
  next: Confirm IEEE macro lexical behavior and patch the first ADC compile blocker, preserving a separate Claude-owned direct-cast runtime issue.
pending_prs:
  - pr: https://github.com/dsellerbrock/iverilog-uvm/pull/332
    head: 9093a9e9f17a21847549a8433b28d59089a9d958
    status: Required local gates passed; all six current-head CI jobs queued at 2026-09-23 10:40 UTC. Merge after one completed exact-head success.
  - pr: https://github.com/dsellerbrock/iverilog-uvm/pull/333
    head: 80bd79f87f974dd2dccd9fd75faa4e5d62d6f54c
    base: PR332 branch; retarget to main after PR332 merges.
    status: Focused local gates and clean pinned ECC bind compile passed; current-head CI queued. Merge only after its own completed exact-head success.
release_profiles:
  record: docs/conformance/release_overlays/README.md
  meaning: Pinned stable sources with test-selected patches and options; neither full DV suite qualified.
application_status:
  opentitan_gpio: Pinned GPIO smoke prints TEST PASSED CHECKS and zero UVM errors, but matrix is DEBT due ten compiler warnings; full DV qualification remains open.
  opentitan_aon: Pinned AON compile fails event .triggered sensitivity. Claude owns the overlapping elaboration/runtime files.
  opentitan_spi_device: Pinned SPI Device compile fails nested inline constraint foreach. Claude owns overlapping elaborate.cc.
  caliptra: Patched-copy Adams Bridge rej_bounded 100-vector replay passes; clean ECC formal named-antecedent bind compiles on PR333, but the next ECC formal file exposes late default-clock rejection. No full DV or formal qualification.
preservation:
  user_dirty: AGENTS.md and unrelated untracked evidence remain untouched.
  application_sources: Pinned OpenTitan and Caliptra sources remain unmodified.
  graph: Shared canonical graph queried first; no feature-worktree graph created.
next_command: "Assign Luna ivlpp/lexor.lex and dedicated macro regression only; coordinator continues Caliptra PR integration and owns all builds."
