campaign: IEEE1800-UVM-continuous
mission: IEEE 1800-2017 and 2023 independently, IEEE 1800.2/UVM separately, unmodified stable OpenTitan and Caliptra DV, and a sound formal foundation.
branch: agent/state-xz-port-20260923
base: ea0be16d5cfcfb030c1a2346f8b5db93600d331f
active: .ai/ACTIVE_WORK.yaml
checkout_ledger: .ai/ISSUE_CHECKOUT.csv
completed_integration:
  pr: https://github.com/dsellerbrock/iverilog-uvm/pull/324
  state: MERGED
  merged_head: ea0be16d5cfcfb030c1a2346f8b5db93600d331f
  local_evidence: docs/conformance/session_logs/2026-09-23_pr322_reconciliation.json
  ci_run: 35810194896
  ci_status_at_checkpoint: in_progress
  action_if_later_failure: Diagnose the failed job and publish a separate correction PR without changing the merged history.
active_blocker:
  id: CONSTRAINT-STATE-XZ-SCALAR
  phase: REPRODUCED
  held_source: origin/claude/hold-state-xz-scalar a91acf870
  pre_fix_red: evidence/review-20260920/state-xz-port/pre-fix.sv prints three FAILED lines under both -g2017 and -g2023 using this worktree's native ARM64 install.
  source_commit: 527c0930a
  completed_gates: focused legacy/JSON and PR323 neighbors; full ivtest 6355 total with 0 failures; JSON 3287/0; NFA 58/0; make check.
  completed_gate: Real-DPI UVM session 76937 exited 0 with 358 passed, 0 failed, 0 skipped; skip frontend relocation gate because this isolated runtime fix does not touch frontend resource discovery.
  evidence: docs/conformance/session_logs/2026-09-23_constraint_state_xz_port.json
  next: Push and open the X/Z PR. Keep flag edits unstaged from that PR.
  exact_next_command: Stage only X/Z evidence and owning documents, commit, then push agent/state-xz-port-20260923.
parallel_next:
  id: COMMERCIAL-SIM-UNSAFE-FLAG
  phase: PATCHING; implementation agent edits disjoint compiler paths but must not build or install until the X/Z PR is isolated.
  reason: User explicitly ordered it after the X/Z source fix and asked for more agentic progress.
  pre_fix_red: evidence/review-20260920/commercial-unsafe-design/queue_copyback.sv and queue_input.sv fail strict compilation; -gcommercial-unsafe is unknown.
  requirement: Opt-in only, strict IEEE default, destination-typed X/Z conversion, root README update.
application_status:
  opentitan: Clean Earlgrey-PROD-M6 a78922f14a8cc20c7ee569f322a04626f2ac6127 still has two strict queue element type errors; no DV qualification.
  caliptra: Stable v2.1.2 and Adams Bridge v2.0.3; rej_bounded failure classified as same-event TB race, not a confirmed compiler defect.
preservation:
  user_dirty: AGENTS.md and unrelated untracked artifacts are untouched.
  held_branch: Keep claude/hold-state-xz-scalar until its behavior is ported, validated and merged.
  worktrees: No sibling met safe retirement conditions in the fresh origin/main audit; do not raw-delete.
  graph: Canonical main checkout and shared graph updated to ea0be16d5 after PR324; no feature-worktree graph.
history: docs/conformance/session_logs/2026-09-23_before_xz_CAMPAIGN.yaml preserves the previous operational checkpoint; dated session logs remain unchanged.
