id: "U13"
title: "Protect literal legacy DPI argv from runtime option parsing"
status: "active"
phase: "awaiting_linux_ci"
base_revision: "8e30e020c"
last_validated_revision: "8f2252dd3"
reproducer: "Published2785d1d5d GitHub run34575850689 Ubuntu22.04 job103188016019: legacy_10_dpi fails both editions before simulation with vvp invalid option -- f. Log archived under l27/ubuntu2204.plain.log. Ubuntu24.04 also failed; inspect matching log."
expected_behavior: "The standalone old-DPI fixture receives exactly its program name and four literal arguments, including -f, then verifies real iteration/restart and regex/metadata behavior."
standards: ["Runtime getopt option boundary;this is a harness invocation fix,not a new IEEE semantic qualification."]
root_cause: "vvp/main.cc intentionally permits GNU getopt argument permutation. Frontend S10 passes an option-looking test argument without --,so Linux consumes it before VPI/DPI sees argv."
allowed_scope: ["Insert end-of-options separator in the legacy_10_dpi fixture invocation only;retain all literal argv assertions and required diagnostics;local frontend restore/hash validation and fresh published Linux CI."]
non_goals: ["No runtime option-parser change,POSIXLY_CORRECT workaround,removed -f assertion,DPI behavior changes or forwarded-template implementation. L28 reducer preserved for later selection."]
required_gates: ["Existing Linux failure log;unchanged exact-argv fixture passes both editions locally;isolatedfrontend last alone with installationrestore/frozenhashes;fresh Linux CI confirms correction before final closure."]
next_action: "Await fresh Linux CI on published9006baa15;close U13 only after evidence,then select preservedL28."
next_command: 'gh pr view 275 --json state,headRefOid,statusCheckRollup'
stop_after_completion: false

local_validation: "Frontend35171exit0 allscenarios,including exact oldDPI argv both editions;installation restored/five8f2252dd3hashesmatch;independent review clear. Both Linux failurelogs confirm same getopt-f cause. Fresh Linux CI required."
