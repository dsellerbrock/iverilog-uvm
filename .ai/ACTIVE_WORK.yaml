id: CI-TARGET-API-EXPORTS
title: Export the public target API additions from PR280 on Windows
status: ROOT_CAUSED
base_revision: 4956b2c1c
reproducer: evidence/ci-strcmp-header/mingw64.log
current_behavior: "PR281 Windows target link fails on ivl_lval_part_carrier_width and ivl_lval_dynamic_part_carrier; Ubuntu builds passed the header fix."
expected_behavior: "Public target API functions used by targets are present in the Windows import library."
allowed_scope:
  - Add all PR280 public target API declarations missing from ivl.def: carrier, signal const, and constraint state-call families
  - Check declarations, definitions and consumer names for exact consistency
  - Update existing PR281 and observe platform CI
forbidden_scope:
  - Fold preserved L65/L66 features into this repair
  - Change carrier semantics or target ABI signatures
standards: "Build linkage repair for existing target API, no language semantic change"
prerequisites: "L65/L66 tested local branch agent/typed-array-return-20260914 at711e54893. Two focused features in next batch."
pending_gates: "Independent export review and Windows CI"
stop_after_completion: false
