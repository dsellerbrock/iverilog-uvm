blocker: SOLVE-BEFORE-FIXED-ARRAY-NONINTEGRAL-GUARD
status: LOCAL_QUALIFIED_PORT_PENDING_CI
branch: agent/pr322-remainder-20260923
authorization: PR322 was closed unmerged after its independent fixes were committed directly to main; port only the missing standards guard and regressions onto that baseline.
standards:
- IEEE 1800-2017 18.5.10: ordered variables shall be rand and integral values.
- IEEE 1800-2023 18.5.9: ordered variables may be integral or real values; class handles remain invalid. Real ordering is outside this bounded integration fix.
reproducer: evidence/review-20260920/pr322-reconcile-build1/nonintegral_array_order.sv
red: Installed merged-source build1 accepts an array of class handles in solve...before with compiler exit 0 under both -g2017 and -g2023.
expected: A whole fixed rand array in solve...before cannot convert class handles to fabricated scalar order tokens; 2017 requires integral ordered values, while 2023 also allows real values.
root_hypothesis: elaborate.cc expand_fixed_arrays checks rand and dimensionality but not element type before constructing selected element IR; the generic selected-array path can synthesize a 32-bit e: token for class handles.
allowed_scope:
- elaborate.cc fixed-array solve-before expansion and its directly related diagnostic.
- ivtest/ivltests/sv_constraint_solve_before_fixed_array_neg.v, paired edition wrapper, and actual-output golds.
- Owning blocker/matrix/session evidence and this campaign checkpoint after validation.
forbidden:
- Application source changes; solver sampling redesign; new IR architecture.
- Weakening existing positive, negative, or boundary cases.
acceptance:
- Reproducer compile fails in both editions with an integral-element diagnostic.
- Existing fixed-array positive, non-rand, randc, and multidimensional cases retain behavior.
- Combined PR322/PR323 focused suites pass; broad required gates precede PR322 merge.
lane:
  implementation_owner: Luna/medium
  files: elaborate.cc and ivtest/ivltests/sv_constraint_solve_before_fixed_array_neg.v only
  coordinator: Standards, actual-output golds, builds/install, integration, review, broad gates and PR merge.
suspended_prior_ticket:
  blocker: OT-SPI-INLINE-STATE-FUNCTION-CALL
  complete_snapshot: e200dfd46:.ai/ACTIVE_WORK.yaml
  state: PR323 state-only implementation merged; staged random-variable arguments remain unsupported and require a dedicated lifetime/descriptor design.
  resume_after: PR322 integration and this array-boundary gate.
stop_after_completion: Freeze validated patch and return to coordinator selection; full compiler/UVM mission continues.
qualification: docs/conformance/session_logs/2026-09-23_pr322_reconciliation.json
branch_reconciliation: docs/conformance/session_logs/2026-09-23_pr322_branch_reconciliation.json
next: Publish the small remainder PR with the compiler/test tree verified equal to the locally qualified e4ec3aa51 candidate; merge after a qualifying CI pass. Then select the opt-in commercial-unsafe flag without overlapping this elaborate.cc source freeze.
