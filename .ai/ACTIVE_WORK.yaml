id: "L11"
title: "Preserve copy-out-only scalar output argument semantics"
status: "in_progress"
phase: "focused_tested"
base_revision: "3c44fd13beb6bd0f19186e82fe5a229950c4fc0d"
last_validated_revision: "3c44fd13beb6bd0f19186e82fe5a229950c4fc0d"
prerequisite_for: "L10"
preserved_parent: "../evidence/campaign-20260908/l10/partial-implementation.patch and suspended-contract.yaml"
reproducer: "Plain fixed-array output actual with side-effecting selector; scalar output formal observes its own default, not caller value."
expected_behavior: "Native output actual evaluated only for return assignment; automatic formal starts with declared type default, static retains own state."
standards: "Both IEEE1800 editions13.5 output return copying,13.4.2 automatic/static formal lifetime; verify exact defaults."
allowed_scope: ["Minimized baseline reducers and exact standards/trace.", "tgt-vvp/draw_ufunc.c output classification,evaluation,send/default paths; no L10 typed property changes until prerequisite validated.", "Permanent regression and required gates; existing governance docs."]
non_goals: ["No unrelated parser,ref,DPI-open-array redesign.", "Preserve L10 patch and regressions for resume after validation."]
required_gates: ["Paired red/green both editions,automatic/static typed defaults,index side effects,existing container and DPI controls.", "Focused,independent review,full integrated/JSON/real-DPIUVM/NFA/makecheck,isolated frontend."]
stop_after_completion: false
next_action: "Restore installed validated L09 from reverted source, reproduce plain-array double evaluation, verify formal defaults and scope repair."
next_command: "../evidence/arm64-tooling/resource-runner make -j1 YACC=/opt/homebrew/opt/bison/bin/bison LEX=/usr/bin/flex"

root_cause: "function_port_is_container_output_ suppresses input evaluation only for containers. Scalar outputs therefore read caller value and evaluate side-effecting actuals twice. Existing automatic vvp signal alloc/reset initializes typed formals; static storage persists. Extend classification and skip scalar send using that existing lifecycle."
standards_evidence: "Read both editions13.5,13.4.2,13.3.2 and2017 Table6-7; automatic outputs default on entry, static retains state. Baseline plain fixed-array selector called twice in both editions."

l11_focused: "42/0, paired2families both modes baseline fail/candidate pass; independent review clear, selector strengthened to prove return-time evaluation."
