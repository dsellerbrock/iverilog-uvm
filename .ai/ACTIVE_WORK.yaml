status: focused_integrated_publication_pending
branch: agent/ot-spi-dist-csrng-forward-20260923
baseline: PR346 merged at 2899542be0a117d24e6d3ea4a3ca82ddf4c107d1 after exact-head Ubuntu 24.04 success; canonical main checkout and shared graph updated. Local parser, solver, overlay, and forward-driver fixes remain unpublished.
blocker: OT-SPI-LARGE-EXACT-DIST-SAMPLING and OT-CSRNG-POST-LOAD-NO-UVM-TRIAGE
goal: Preserve the passing unchanged released AON timer smoke, advance SPI Device through exact coupled distribution sampling, and resolve CSRNG time-zero initialization propagation after repairing its parser and loader.
standards: IEEE 1800-2017/2023 section 18.6.3 randomization; sections 3.13 and 28.3.5 interface naming and array instance references.
failing_reproducer: evidence/opentitan-spi-jedec-sampling-limit-20260923/reducer.sv fails satisfiable nested-field randomization in both editions; evidence/opentitan-csrng-interface-array-assign-20260923/minimal_red.sv fails continuous-assignment parsing in both editions when an interface type and instance array share a name.
expected: Sample a satisfiable joint constraint solution without reporting failure due to a cap; parse an indexed interface-array member even when its type and instance share a spelling.
root_cause: vvp/vvp_z3.cc converts complete-joint enumeration cap exhaustion into randomize failure; lexor/pform classifies the indexed same-name interface instance as a type identifier rather than a module-scope instance.
allowed_scope: Two delegated implementation lanes below with disjoint source ownership. Coordinator owns manifests, shared build/install and outputs, integration, PR review, and pinned release replay.
forbidden: Verilator implementation changes, edits to pinned OpenTitan/Caliptra sources, unrelated compiler mechanisms, fallback success, or application DV pass claims from compile alone.
validation: CSRNG name classification 6410b956c and nested full-power-two sampling df9f167f4 are locally integrated with paired 2017/2023 focus 10/10 legacy and JSON each, solver neighbor 10/10 each, interface neighbor 11/11 legacy and 13/13 JSON. Unchanged released AON smoke still passes; SPI flash smoke advances past JEDEC to a separate large-range dist failure at 319526568ps. Pristine CSRNG compile clears tb.sv:85 but fails on BlkLen widths; the exact pinned-release two-width overlay compiles with zero errors, then configured smoke aborts before UVM at vvp/array.cc:483. Evidence is docs/conformance/session_logs/2026-09-23_opentitan_spi_csrng_next.json and evidence/opentitan-csrng-blklen-overlay-20260923/smoke-replay.json. No full DV suite qualification claim.
parallel_spi_large_dist_triage:
  id: OT-SPI-PASS-LARGE-DIST-TRIAGE
  owner: Reused GPT-6 Luna agent; coordinator owns shared builds and integration.
  scope: Read-only released SPI pass-sequence diagnosis and independent cheap paired IEEE 1800-2017/2023 RED under evidence/opentitan-spi-pass-large-dist-triage-20260923 only; no compiler or application source edits yet.
  boundary: Unchanged released flash smoke fails at spi_device_pass_base_vseq.sv:146 at 319526568ps with exact large-range dist sampling diagnostic.
  acceptance: Confirm applicable distribution and randomize clauses, derive a reduced failing case plus legal controls, and identify narrow Icarus source ownership without implementing. Stop if the released call is not standards-valid.
  dependency: Disjoint from CSRNG BlkLen triage; no shared test-output directory or Verilator implementation.
parallel_spi_large_dist_implementation:
  id: OT-SPI-LARGE-EXACT-DIST-SAMPLING
  owner: Reused GPT-6 Luna agent; coordinator owns shared builds and integration.
  standards: IEEE 1800-2017 section 18.5.4 and 18.6.3; IEEE 1800-2023 section 18.5.3 and 18.6.3; preserve exact :/ range weighting over legal assignments and rollback on failure.
  reproducer: evidence/opentitan-spi-pass-large-dist-triage-20260923/reducer.sv and baseline.json; paired RED satisfiable coupled 3001-value range after smaller and impossible controls pass.
  files: vvp/vvp_z3.cc and dedicated paired constraint ivtest fixtures/golds only; coordinator owns manifests, shared builds/install, release replay, and publication.
  acceptance: Paired coupled large range succeeds with all hard constraints and exact range-item weighting; isolated large, span-257 and span-256 boundaries, endpoints, impossible rollback, and neighboring dist/solve-before tests remain green. No approximation, fallback success, application source edit, or broad selector.
  dependency: Disjoint from Sol pinned CSRNG release overlay files; no Verilator implementation.
  state: Revised weighted-item candidate passes paired legacy and JSON focus 14/14 each. Pinned SPI flash reaches UVM PRE_START but still times out at 120 seconds; bounded sample locates the current work in sparse wide-scalar feasible-domain enumeration before the dist resolver, a separate performance blocker. No DV pass. See evidence/opentitan-spi-lazy-item-20260923/result.json.
parallel_spi_sparse_wide_domain_triage:
  id: OT-SPI-SPARSE-WIDE-DOMAIN-ENUM
  owner: Reused GPT-6 Luna read-only agent; coordinator owns shared builds, release replay, and source selection.
  standards: IEEE 1800-2017/2023 sections 18.5.10 and 18.6.3 uniform sampling over feasible legal solutions; preserve width, solve order, and owner RNG.
  reproducer: Pinned released SPI flash smoke with exact runtime arguments in evidence/opentitan-spi-lazy-item-20260923/result.json; bounded sample shows z3_enumerate_sparse_wide_domain_ active after UVM PRE_START before dist resolution.
  files: evidence/opentitan-spi-sparse-wide-domain-20260923 only; read-only source inspection of vvp/vvp_z3.cc and paired cheap reducer. No source/build/install or application edits until scope reviewed.
  acceptance: Locate exact caller and condition, paired 2017/2023 RED plus passing boundary, and prove any proposed speedup preserves uniform sampling and four-state/width semantics. Do not infer a DV pass from a profile.
  dependency: Disjoint from Sol scheduler source and Caliptra release evidence; future solver implementation would reuse Luna ownership only after coordinator approval.
  state: Paired wide and six-bit probes pass, but a bounded release profile shows repeated 65-model sparse probes on soft-only 32-bit CIP TL item fields. The proposed skip has no paired failing RED yet, so no source change is authorized; solver ownership is released for CSRNG joint-dist triage. See evidence/opentitan-spi-sparse-wide-domain-20260923/.
parallel_csrng_cfg_joint_dist_triage:
  id: OT-CSRNG-CFG-JOINT-DIST
  owner: Reused GPT-6 Luna agent; coordinator owns shared builds, integration, and release replay.
  standards: IEEE 1800-2017/2023 distribution constraints and randomize semantics; preserve weighted legal sampling and rollback.
  reproducer: Pinned released CSRNG smoke fails at dv_base_test.sv:41 after reaching UVM; exact log in evidence/opentitan-csrng-timezero-scheduler-20260923/result.json.
  files: evidence/opentitan-csrng-cfg-joint-dist-20260923 only for read-only triage; vvp/vvp_z3.cc reserved for a later selected implementation if paired RED proves a narrow defect. No shared build/install or pinned application edits.
  acceptance: Identify which spec property triggers the diagnostic, confirm IEEE legality, produce paired 2017/2023 failing RED and positive/negative boundaries, and propose the smallest complete fix without implementing until coordinator review.
  dependency: SPI sparse-wide lane released vvp/vvp_z3.cc; disjoint from Caliptra release evidence and scheduler source.
  state: Paired 2017/2023 nested guarded-dist RED reproduces the joint-sampler diagnostic; standalone guarded and nested unconditional controls plus UNSAT rollback pass. Pinned dynamic cfg solve fails after a preliminary solve succeeds. Exact triggering DistSpec remains uninstrumented; no source fix yet. See evidence/opentitan-csrng-cfg-joint-dist-20260923/README.md.
parallel_legacy_gate_failure_triage:
  id: OT-LEGACY-GATE-EIGHT-FAILURES
  owner: Reused GPT-6 Sol agent; coordinator owns shared build/install, source edits, and integration.
  reproducer: Full .github/ivtest_gate.sh after local solver and scheduler candidates reports 8 unexplained legacy failures out of 6457; VPI 131/131 and negative 153/153 pass. Log at /tmp/ivl-focus-20260923/ivtest-gate-current.log.
  files: evidence/ivtest-broad-gate-20260923 and only the eight identified ivtest source/gold fixtures; no compiler source or shared manifests. Coordinator owns build/install and integration.
  acceptance: Preserve time-zero primitive checks after Active via #0; update only exact changed diagnostics; replace obsolete 256-coordinate negative limits with above-16384 negative controls and add a supported 301-coordinate positive check in both editions. Focused legacy and JSON checks must pass; no expected-failure entries or removed assertions.
  dependency: Disjoint from Luna CSRNG cfg joint-dist evidence and Caliptra release replay.
  state: Test-only repair integrated at fea3f1203 after isolated legacy and JSON focus; the full merged-branch legacy gate now passes. See evidence/ivtest-broad-gate-20260923/merged-gate-result.json. Full JSON runner remains active.
parallel_csrng_blklen_triage:
  id: OT-CSRNG-BLKLEN-PARTSELECT-TRIAGE
  owner: Reused GPT-6 Sol agent; coordinator owns shared builds and integration.
  scope: Read-only released CSRNG constant-width diagnosis and independent cheap paired IEEE 1800-2017/2023 RED under evidence/opentitan-csrng-blklen-triage-20260923 only; no compiler or application source edits yet.
  boundary: Unchanged released CSRNG compile advances past tb.sv:85 but fails at :152/:162 on hierarchical BlkLen indexed part-select widths.
  acceptance: Determine whether the source is strict IEEE-valid or commercial leniency, derive a reduced failing case plus legal controls, and identify narrow Icarus source ownership or minimal downstream overlay if strictly necessary. Do not implement before coordinator selection.
  dependency: Disjoint from SPI solver triage; no shared test-output directory or Verilator implementation.
parallel_csrng_blklen_overlay_lane:
  id: OT-CSRNG-BLKLEN-PINNED-OVERLAY
  owner: Reused GPT-6 Sol agent; coordinator owns shared builds and integration.
  standards: IEEE 1800-2017/2023 section 11.5.1 and Annex A constant-expression grammar; the released module-instance parameter width is outside the strict grammar.
  reproducer: evidence/opentitan-csrng-blklen-triage-20260923/FINDING.md and results.json; paired instance-path width fails while local constant width passes.
  files: docs/conformance/release_overlays/opentitan/csrng_blklen_width.patch; owning release_overlays/README.md entry; evidence/opentitan-csrng-blklen-overlay-20260923 only.
  acceptance: Exact pinned-release patch changes only two indexed part-select width operands plus a local constant if needed; dry-run and apply only to disposable copy; byte-compare untouched source; paired local RED remains strict-failing; fresh released CSRNG compile advances without suppressing assertions or changing widths.
  dependency: No Icarus compiler or pristine application source edits; disjoint from Luna SPI evidence lane; coordinator reviews and publishes.
  state: Focused pinned-release overlay compile passes with zero errors, but configured smoke aborts before UVM at vvp/array.cc:483; publication pending.
parallel_csrng_array_crash_triage:
  id: OT-CSRNG-ARRAY-WORD-SIZE-CRASH-TRIAGE
  owner: Reused GPT-6 Sol agent; coordinator owns shared builds and integration.
  reproducer: evidence/opentitan-csrng-blklen-overlay-20260923/smoke-replay.json; patched pinned CSRNG configured smoke aborts before UVM at vvp/array.cc:483 get_word_size assertion.
  files: evidence/opentitan-csrng-array-crash-triage-20260923 only; no compiler or application source edit until a cheap paired RED and root-cause scope are reviewed.
  acceptance: Preserve the pinned crash trace and derive cheap positive/negative/boundary controls; identify exact array representation and caller without masking assertions, then request coordinator implementation selection.
  dependency: Disjoint from Luna vvp/vvp_z3.cc solver lane; no shared test-output directory or Verilator implementation.
  state: Paired 2017/2023 RED and direct-driver controls reproduced the pre-UVM load-order assertion; LLDB traces eager packed-dimension metadata resolution before a forward concat net driver is attached. Evidence in evidence/opentitan-csrng-array-crash-triage-20260923/.
parallel_csrng_array_crash_implementation:
  id: OT-CSRNG-PACKED-NET-ARRAY-FORWARD-DRIVER
  owner: Reused GPT-6 Sol agent; coordinator owns shared builds and integration.
  standards: IEEE 1800-2017/2023 packed dimensions and net-array continuous assignments; valid elaboration must not depend on VVP driver definition order.
  reproducer: evidence/opentitan-csrng-array-crash-triage-20260923/concat_driver_candidate.sv; paired VVP abort at vvp/array.cc:483 before UVM starts.
  files: vvp/words.cc and dedicated paired ivtest fixtures/JSON/golds only; coordinator owns shared manifests, builds/install, release replay, and publication.
  acceptance: Forward concat-driven packed net array loads and runs in both editions; direct driver and one-dimensional controls remain green; unresolved or nonintegral word references fail with a hard diagnostic, never an assertion or false pass.
  dependency: Disjoint from Luna vvp/vvp_z3.cc SPI solver lane. No pinned application or Verilator source edits.
  state: Candidate vvp/words.cc built and installed locally; paired legacy and JSON 4/4 each. Configured pinned CSRNG replay no longer aborts but reaches no UVM report in 120 seconds, so full smoke remains unqualified.
parallel_csrng_post_load_triage:
  id: OT-CSRNG-POST-LOAD-NO-UVM-TRIAGE
  owner: Reused GPT-6 Sol agent; coordinator owns shared builds and integration.
  reproducer: The same selected pinned CSRNG overlay image and command in evidence/opentitan-csrng-blklen-overlay-20260923/smoke-replay.json run on the locally installed forward-driver candidate; no UVM report after 120 seconds.
  files: evidence/opentitan-csrng-post-load-triage-20260923 only; read-only runtime investigation and no compiler or application source edits.
  acceptance: Sample the actual Icarus runtime stack/progress, distinguish loading from simulation and output buffering, derive a cheap paired RED or name an exact remaining dependency; preserve the timeout and binary fingerprints without claiming DV pass.
  dependency: Disjoint from SPI solver vvp/vvp_z3.cc lane. No shared build/install, pinned application, or Verilator edits.
  state: Completed read-only triage. The paired packed_tri_feedback.sv RED times out before time one in 2017 and 2023; disabling the feedback edge passes. LLDB maps repeating X/Z values to packed interface cmd_req and related concat nodes before sim_started. See evidence/opentitan-csrng-post-load-triage-20260923/README.md.
parallel_csrng_packed_tri_feedback_implementation:
  id: OT-CSRNG-PACKED-TRI-FEEDBACK-CONVERGENCE
  owner: Reused GPT-6 Sol implementation agent; coordinator owns shared builds, manifests, pinned replay, and integration.
  standards: IEEE 1800-2017/2023 net resolution, continuous-assignment propagation, and packed-struct field semantics; preserve four-state values and driver behavior.
  reproducer: evidence/opentitan-csrng-post-load-triage-20260923/packed_tri_feedback.sv and packed_tri_feedback.json; paired RED stalls in initialization while the control reaches time one.
  files: vvp/vvp_net_sig.cc vec8 signal filter and dedicated paired ivtest fixtures/golds only; no vvp/vvp_z3.cc, vvp/words.cc, application source, shared build/install, or manifests. Request coordinator scope review before touching any other compiler mechanism.
  acceptance: Paired RED reaches time one with the expected resolved value; FEEDBACK=0, scalar, packed-field X/Z, two-driver and field-width boundaries remain correct; reject a dropped-event/watchdog workaround; pinned configured CSRNG replay must advance beyond the former time-zero loop without false DV pass.
  dependency: Disjoint from Luna SPI solver vvp/vvp_z3.cc lane and Caliptra release evidence lane.
  state: Candidate vvp/vvp_net_sig.cc is unaccepted. Its initial paired 4/4 focus was a false green because the new condition simplified the frozen RED; the exact RED and pinned CSRNG overlay smoke still loop in pre-start initialization. Agent is correcting the test and tracing the real X/Z feedback root cause. Preserve invalidated evidence in evidence/opentitan-dist-vec8-followon-20260923/result.json and postfix profile in evidence/opentitan-csrng-post-load-triage-20260923/.
parallel_csrng_timezero_scheduler_implementation:
  id: OT-CSRNG-TIMEZERO-INIT-SCHEDULER
  owner: Reused GPT-6 Sol implementation agent; coordinator owns shared builds, manifests, pinned replay, and integration.
  standards: IEEE 1800-2017/2023 sections 4.5 and 4.9.1 initialize values and schedule startup events into the time-zero slot; continuous assignments evaluate there alongside procedural startup.
  reproducer: evidence/opentitan-csrng-post-load-triage-20260923/packed_tri_feedback.sv and postfix_mode_matrix.json; faithful paired RED stalls before sim_started while constant-mode controls pass.
  files: vvp/schedule.cc and vvp/schedule.h only if essential plus dedicated paired ivtest fixtures/golds; restore unaccepted vvp/vvp_net_sig.cc candidate. No vvp/vvp_z3.cc, vvp/words.cc, application source, shared build/install, or manifests.
  acceptance: Faithful paired RED and released CSRNG image advance past pre-start without forcing application pass; startup callback and VPI ordering, initial net delivery, region order, X/Z resolution, and truly oscillatory nets remain correct. No arbitrary CSRNG-specific scheduling preference or watchdog workaround.
  dependency: Disjoint from Luna SPI solver vvp/vvp_z3.cc lane and Caliptra release evidence lane.
  state: Local candidate vvp/schedule.cc passes faithful paired RED/control, legacy and JSON focus 6/6 each, startup VPI 2/2, initial delivery controls, and make check. Pinned CSRNG reaches UVM then fails at cfg.randomize() on a separate joint-dist restriction; no DV pass. Broad gates and publication pending. See evidence/opentitan-csrng-timezero-scheduler-20260923/result.json.
parallel_caliptra_l0_gap_assessment:
  id: CALIPTRA-PINNED-L0-52-AND-FULL-DV-GAPS
  owner: Delegated GPT-6 Luna assessment agent; coordinator retains implementation selection and shared Icarus builds.
  pinned_release: Caliptra v2.1.2 49370266d12cb0c4a8f71b3a0ff7e54ba7d4866e and Adams Bridge v2.0.3 b77e3d899e828d626cfc2a0d26a6b5704cc121e0.
  files: evidence/caliptra-l0-52-and-full-dv-gap-20260923 only; read existing exact L0 runners, overlays, and full-DV inventory without editing any tool or pinned application source.
  acceptance: Reconcile strict 49/52 L0 baseline with two already focused-tested firmware overlays and the DOE SVA error; run reproducible selected replays and a full 52-case patched-copy L0 pass if feasible. Count an assertion error as failure regardless of exit/banner. Inventory full DV targets, dependencies, and first runnable Icarus blocker separately from L0. Never claim 52/52 by hiding assertions or editing Verilator.
  dependency: Independent from OpenTitan SPI vvp/vvp_z3.cc and CSRNG time-zero evidence lanes. Use isolated output roots and coordinate CPU-heavy runs; no shared build/install.
parallel_csrng_interface_name_lane:
  id: OT-CSRNG-INTERFACE-ARRAY-NAME-COLLISION
  owner: Delegated GPT-6 Sol implementation agent; coordinator owns shared builds and integration.
  standards: IEEE 1800-2017/2023 sections 3.13 and 28.3.5; an interface type and a local instance may share a spelling, and a constant genvar may select an interface-array member.
  reproducer: evidence/opentitan-csrng-interface-array-assign-20260923/minimal_red.sv and baseline.json; unchanged released CSRNG tb.sv line 85 has the same parse error with -g2017 -gcommercial-unsafe.
  files: lexor.lex; pform.cc; parse.y only as necessary for root-cause name classification; dedicated ivtest fixtures/golds. No vvp/vvp_z3.cc, other runtime files, shared manifests/builds, application source, or Verilator edits. Stop if another source path is required.
  acceptance: Paired 2017/2023 RED minimal and typed release-shaped cases become runtime/compile green; renamed-instance, typed noncollision, scalar same-name, ordinary type-name and invalid member controls retain correct behavior; fresh unchanged released CSRNG compile advances past tb.sv line 85 without accepting invalid syntax. Coordinator owns focused runner and release replay.
  state: Integrated locally at 6410b956c; paired focused and neighboring checks pass; released CSRNG clears tb.sv:85 and stops on separate BlkLen elaboration errors. Publication pending after PR346.
parallel_spi_jedec_solver_lane:
  id: OT-SPI-JEDEC-SAMPLING-LIMIT
  owner: Delegated GPT-6 Luna implementation agent; coordinator owns shared builds and integration.
  standards: IEEE 1800-2017/2023 section 18.6.3; randomize success requires a satisfiable joint solution, and failed randomization preserves prior values.
  reproducer: evidence/opentitan-spi-jedec-sampling-limit-20260923/reducer.sv and baseline.json; unchanged released SPI Device flash-mode smoke fails at 8.676568us on the same enumeration-cap diagnostic.
  files: vvp/vvp_z3.cc and dedicated ivtest constraint fixtures/golds only. No elaborate.cc, event target/runtime code, shared manifests/builds, application source, or Verilator edits. Stop if another source path is required.
  acceptance: Paired 2017/2023 satisfiable nested 16/8-bit fields, size-10 and size-11 boundary, truly UNSAT prior-value rollback, hard dist/soft/preference behavior preserved, and neighboring constraint tests; no false success from sampling a forbidden assignment. Coordinator reruns pinned SPI flash-mode smoke after integration.
  state: Integrated locally at df9f167f4; paired focused and neighboring checks pass; released SPI flash smoke clears JEDEC and stops on separate large-range exact dist blocker at 319526568ps. Publication pending after PR346.
parallel_spi_constraint_inside_lane:
  id: OT-SPI-CONSTRAINT-INSIDE-CONST-ARRAY
  owner: Delegated GPT-6 Luna implementation agent; coordinator owns shared builds and integration.
  standards: IEEE 1800-2017/2023 sections 11.4.13 and 18.12; preserve wildcard comparison and solver width/signedness semantics.
  reproducer: evidence/opentitan-spi-constraint-inside-param-20260923/reducer.sv; one pinned released SPI Device site at spi_device_read_buffer_direct_vseq.sv:94.
  files: elaborate.cc constraint IR inside branch; vvp/vvp_z3.cc and tgt-vvp/eval_vec4.c only if necessary; dedicated ivtest fixtures/golds. No elab_expr.cc, tgt-vvp/eval_object.c, tgt-vvp/vvp_priv.h, shared manifests/builds, application source, or Verilator edits. Stop if another source path is required.
  acceptance: Paired 2017/2023 positive first/last/miss, wildcard X/Z, width/signedness, singleton/multidimensional boundaries, UNSAT rollback, negative unsupported type; ordinary inside controls remain green. No scalar fallback for an array aggregate.
  state: Integrated at b7551af50 after legacy and JSON focus 4/4 each, neighboring inside/constraint checks 12/12 each, and unchanged released SPI Device compilation exit 0 with zero hard errors. Runtime flash-mode smoke reaches a separate solver cap failure.
parallel_spi_queue_concat_lane:
  id: OT-SPI-QUEUE-ARRAY-CONCAT
  owner: Delegated GPT-6 Sol implementation agent; coordinator owns shared builds and integration.
  standards: IEEE 1800-2017/2023 sections 10.10 and 7.10.4; queue assignment is an assignment-like context.
  reproducer: evidence/opentitan-spi-queue-array-concat-20260923/reducer.sv; two pinned released SPI Device sites at flash_mode_vseq.sv:16 and intercept_vseq.sv:15.
  files: elab_expr.cc PEConcat queue context; tgt-vvp/eval_object.c and tgt-vvp/vvp_priv.h collection splice classifier; tgt-vvp/vvp_process.c only if necessary; dedicated ivtest fixtures/golds. No elaborate.cc constraint IR, vvp/vvp_z3.cc, tgt-vvp/eval_vec4.c, shared manifests/builds, application source, or Verilator edits. Stop if another source path is required.
  acceptance: Paired 2017/2023 constant and live fixed-array operands, mixed scalars, descending order, empty/bounded queue, self-reference snapshot, invalid type/dimension negative; RHS evaluated before destination update. No vector zero fallback.
  state: Integrated at 2e648c7ec after legacy and JSON focus 8/8 each, neighboring queue 2/2 each, and unchanged released SPI Device replay clearing both queue-concat errors. The remaining constraint-inside error is assigned to the disjoint Luna lane. No SPI DV verdict.
parallel_aon_mixed_vif_lane:
  id: OT-AON-MIXED-VIF-EVENT-WAIT
  owner: Delegated GPT-6 Sol implementation agent; coordinator owns shared builds and integration.
  standards: IEEE 1800-2017/2023 sections 9.4.2 and 25.9; mixed edge-mode event control resumes on the first matching event and cancels sibling subscriptions.
  reproducer: evidence/opentitan-aon-mixed-vif-event-20260923/reducer.sv; paired RED baseline in baseline.json. Released AON source wait_for_interrupt mixes negedge reset and anyedge pins.
  files: tgt-vvp/vvp_process.c; vvp/vthread.cc; vvp/compile.cc; vvp/codes.h and event runtime helpers only if needed; dedicated ivtest fixtures/golds. No elaborate.cc, vvp/vvp_z3.cc, elab_expr.cc, shared manifests/builds, application source, or Verilator edits. Stop if overlap is required.
  acceptance: Paired 2017/2023 mixed VIF negedge+anyedge positive; reset-edge alternate; single-VIF and concrete controls; one-shot cancellation; same-slot and rebinding boundaries; negative unsupported receiver path. Recompile unchanged released AON and obtain a real smoke verdict without weakening its timeout or checker.
  state: Integrated at 07d8ba6df after legacy and JSON focus 4/4 each and neighboring VIF event controls 8/8 each. Unchanged released AON timer smoke passes at 348420188ps with zero UVM warnings/errors/fatals; this is one test, not full DV qualification.
parallel_spi_inside_lane:
  id: OT-SPI-INSIDE-CONST-ARRAY
  owner: Delegated GPT-6 Luna implementation agent; coordinator owns shared build and integration.
  standards: IEEE 1800-2017/2023 section 11.4.13 unpacked array membership; confirm exact wildcard and sizing rules from the primary text.
  reproducer: evidence/opentitan-spi-device-array-pattern-vector-20260923/reducer.sv line 8 and six unchanged released SPI Device source sites.
  files: elab_expr.cc inside lowering and dedicated ivtest fixtures/golds only. No elaborate.cc, event target/runtime files, vvp/vthread.cc, shared manifests/builds, pinned application source, or Verilator edits. Stop for scope expansion.
  acceptance: First/last/nonmember constant-array membership, empty/singleton boundary, four-state wildcard control, invalid operand/type negative, paired 2017/2023 tests; queue array-concatenation line 9 remains a separate RED mechanism.
  state: Integrated at 88f0e9044 after focused validation. Ordinary expression membership is fixed; constraint inside membership and queue concatenation remain independent RED mechanisms.
parallel_aon_triggered_lane:
  id: OT-AON-EVENT-TRIGGERED-SENSITIVITY
  owner: Delegated GPT-6 Sol implementation agent; coordinator owns shared build and integration.
  standards: IEEE 1800-2017/2023 section 15.5.3 named-event triggered state and expression event control; verify first-change and slot-reset behavior.
  reproducer: evidence/opentitan-aon-event-control-triage-20260923/triggered_sensitivity_boundaries.sv; five elaboration errors per edition before fix.
  files: elaborate.cc event-control path; necessary tgt-vvp wait codegen; vvp/vthread.cc, vvp/event.cc/h, vvp/schedule.cc, vvp/compile.cc, and vvp/codes.h for a complete time-step transition notification; dedicated ivtest fixtures/golds. No elab_expr.cc, inside lowering, shared manifests/builds, pinned application source, or Verilator edits. Stop for further overlap.
  acceptance: Event expression wakes on 0-to-1 and 1-to-0 changes; second trigger in the same slot does not retrigger a rearmed expression wait; next-slot and ->> boundaries plus ordinary @(e)/wait(e.triggered) controls pass in 2017/2023. No compile-only bypass.
  state: Integrated at 0a902ee9f after focused validation. Static and automatic event triggered sensitivity use real time-advance notifications; pinned AON compile is clean but its bounded smoke runtime did not complete. Never schedule a fake +1 time step.
parallel_scope_queue_lane:
  id: OT-SPI-STD-RANDOMIZE-QUEUE
  owner: Delegated lower-cost implementation agent; coordinator owns shared builds, manifests, integration, review, and pinned release replay.
  standards: IEEE 1800-2017/2023 section 18.12 scope randomization of variables; section 7.10 queue size and element semantics.
  reproducer: evidence/ot-spi-std-randomize-queue-20260923/reducer.sv; released SPI Device byte_q[$] call.
  files: elab_expr.cc; elaborate.cc; tgt-vvp/eval_vec4.c; tgt-vvp/vvp_process.c; vvp/compile.cc; vvp/codes.h; vvp/vthread.cc/h; vvp/vvp_z3.cc/h; dedicated ivtest fixtures/golds. No manifests, application, or Verilator edits.
  acceptance: Direct local integral queue whose length is proven exactly from captured state; size 3 and size 0; element/foreach constraints at that length; prefilled resize and UNSAT preserve size/values; reject unsupported or nonexact size, out-of-bounds, oversize, and unsupported element forms loudly; paired 2017/2023 checks. Never silently clamp or report success without complete writeback.
  dependency: PR341 source and associative-pop d0560368e are committed; elaborate.cc ownership is released. Coordinator owns build/install and cross-file integration.
  state: Integrated at c44dc1dd4 after an initial Luna lane stopped at the architectural boundary and a Sol implementation reused size/element IR. Build/install and paired focused tests pass; signed four-state and bounded-maximum additions are under final focused validation. Pinned released SPI Device no longer rejects the scope queue call but fails eight later vector-context codegen errors; no DV pass.
parallel_assoc_pop_statement_lane:
  id: ASSOC-QUEUE-POP-STATEMENT
  owner: Delegated lower-cost implementation agent; coordinator owns shared builds, manifests, integration, and review.
  standards: "IEEE 1800-2017/2023 sections 7.9 and 7.10: associative-array methods differ from queue-only pop methods."
  reproducer: evidence/assoc-queue-pop-statement-20260923/reducer.sv compiles and vvp exits 139 after aa.pop_front(); push_probe.sv in the same directory accepts push_front/push_back/insert incorrectly.
  files: elaborate.cc; dedicated ivtest fixtures/golds only. No elab_expr.cc, target/VVP source, shared manifests, application, or Verilator edits.
  acceptance: Associative pop_front/pop_back/push_front/push_back/insert statements fail compilation with a clear diagnostic; valid queue methods still run; empty/nonempty queue boundaries and paired 2017/2023 checks.
  dependency: Disjoint from scope-queue lane. Coordinator owns build/install and integration.
  state: Integrated at d0560368e. Former runtime-crashing reducer and push probe now fail compilation; positive queue behavior passes. Four focused tests pass in each legacy and JSON runner across 2017/2023; follow-on PR/CI and broad qualification remain pending.
parallel_assoc_find_index_lane:
  id: OT-SPI-ASSOC-FIND-INDEX
  owner: Delegated lower-cost implementation agent; coordinator owns integration, shared build/install, manifests, and pinned release replay.
  standards: IEEE 1800-2017/2023 section 7.12.1 array locator find_index returns matching associative keys with the declared key type.
  reproducer: evidence/ot-spi-assoc-find-index-20260923/reducer.sv is a compile-time sorry in both editions on PR340 head.
  files: elab_expr.cc; tgt-vvp/eval_object.c; dedicated ivtest fixtures and gold only. No elaborate.cc or shared build outputs.
  acceptance: Sparse bit[7:0] keys return matching keys in index order, not ordinal positions; empty/no-match, string-key, negative unsupported locator, and receiver-preservation controls; paired edition focused checks and pinned SPI compile replay after integration.
  state: Merged in PR341 at 7943dffd1 after exact-head Ubuntu 24.04 CI passed; pinned SPI find_index error removed. Other associative locator methods remain loud.
parallel_class_event_list_lane:
  id: OT-CLASS-EVENT-MULTI-OBJECT-LIST
  owner: Delegated lower-cost implementation agent; coordinator owns integration, shared build/install, manifests, and pinned release replay.
  standards: IEEE 1800-2017/2023 section 15.5 named-event controls preserve each class object identity.
  reproducer: evidence/ot-class-event-multi-object-list-triage-20260923/reducer.sv compiles but fatals at time 2 in both editions on PR340 head.
  files: elaborate.cc; class-event wait codegen/runtime only if necessary; dedicated ivtest fixtures and gold only. No elab_expr.cc or tgt-vvp/eval_object.c.
  acceptance: Either object event wakes an explicit list once; same-object and static-event controls preserve behavior; repeated firing rearms cleanly; paired edition focused checks after coordinator build. Null/rebound handle semantics need a separate standards review.
  state: Merged in PR341 at 7943dffd1 after exact-head Ubuntu 24.04 CI passed; no source overlap with the find_index lane.
release_lane:
  id: CALIPTRA-EXACT-L0-52
  owner: Coordinator
  source: Caliptra v2.1.2 49370266d12cb0c4a8f71b3a0ff7e54ba7d4866e; Adams Bridge v2.0.3 b77e3d899e828d626cfc2a0d26a6b5704cc121e0.
  toolchain: Official xPack GCC 15.2; RV32IMC/ILP32 multilib.
  options: Pinned release Verilator L0 configuration with xPack GCC 15.2 rv32imc/ilp32, BUILD_CFLAGS=-std=gnu11 -O2, CALIPTRA_INTERNAL_TRNG=1, PLAYBOOK_RANDOM_SEED=1, Verilator -Wno-MISINDENT, and +CLP_REGRESSION.
  evidence: evidence/caliptra-exact-l0-20260923/baseline_summary.json and focused patched-copy replay results in the same directory.
  checkpoint: Full 5.050 baseline complete at 49/52; two test-specific firmware overlays pass focused replays; DOE remains a genuine SVA failure on both 5.050 and a fresh v5.052 replay. Standalone reset-window differential supports a Verilator issue. No combined 52-case patched rerun yet.
  selected_downstream_overlays:
    - "smoke_test_hw_config only: docs/conformance/release_overlays/caliptra/hw_config_inline_c11.patch, focused build and simulation passed."
    - "smoke_test_hmac_errortrigger only: docs/conformance/release_overlays/caliptra/hmac_errortrigger_stdlib.patch, focused build and simulation passed."
parallel_vpi_implementation_lane:
  id: VPI-FORCE-RELEASE-CALLBACK-STMT-OBJECT
  owner: Delegated lower-cost implementation agent; coordinator owns shared builds, integration, and review. Claude is not active on this lane.
  standards: IEEE 1800-2017/2023 VPI cbForce/cbRelease registration and callback object identity; verify exact contract before editing.
  reproducer: evidence/review-20260920/next-vpi-force-callback-assessment/force_cb.sv and force_cb.c.
  files: vvp/vpi_callback.cc, vvp/vvp_net_sig.cc, vvp/vthread.cc, vvp/vpi_signal.cc, vvp/vpi_real.cc, vvp/array.cc, vvp/parse.y, vvp/vpi_priv.h, vvp/vvp_vpi_callback.h, vpi_user.h, and necessary force/release codegen callsites; dedicated VPI regression fixtures. No ADC files, shared builds, installation, or application source edits. Shared manifests remain coordinator-owned; generated parser files are not edited.
  acceptance: Baseline red; whole-net/variable force and release callbacks report the originating statement handle; separate statements on the same target remain distinguishable; variable bit-select registration still rejects; focused positive, negative, and boundary regressions pass after coordinator build.
  state: Locally integrated at d2d996419 and published in PR339. The candidate passed paired-edition VPI cases, neighboring controls, and a file-line-enabled probe. PR339 merged after Ubuntu 24.04 exact-head CI success; broad qualification remains open.
parallel_adc_implementation_lane:
  id: OT-ADC-NESTED-DYNAMIC-ARRAY-RESIZE
  owner: Delegated lower-cost implementation agent; coordinator owns shared builds, integration, and review. Claude is not active on this lane.
  standards: IEEE 1800-2017/2023 sections 7.5 and 18.4 for typed dynamic-array resizing and retained values; nested indexed size constraints are a follow-on requirement.
  reproducer: evidence/opentitan-adc-class-resize-triage-20260923/reducer.sv and pinned Earlgrey-PROD-M6 adc_ctrl_sim time-zero failure.
  files: elaborate.cc, vvp/vvp_z3.cc, vvp/vvp_darray.cc/h, and vvp/class_type.cc as required; dedicated constraint regressions. No VPI force/release files, shared builds, installation, or application source edits. Shared headers/manifests require coordinator review before edits.
  acceptance: First complete slice resizes a rand nested dynamic array's outer dimension as typed array values, preserves existing inner arrays, and leaves newly grown inner arrays empty; impossible size and zero-size boundaries behave correctly in both IEEE editions; actual class-handle arrays remain distinct and are not silently mishandled. Then address indexed inner sizes before claiming ADC smoke progress.
  state: Typed outer resize and traversal type guard committed locally as 0bf19490c and published in PR339. Legacy and JSON paired-edition cases pass. Indexed inner-size constraints remain dropped; no ADC smoke progress is claimed.
parallel_spi_implementation_lane:
  id: OT-SPI-DEVICE-CONSTRAINT-FOREACH-PATH
  owner: Delegated lower-cost implementation agent; coordinator owns shared builds, integration, and review. Claude is not active on this lane.
  standards: IEEE 1800-2017/2023 hierarchical array source in constraint foreach; verify relevant clause and preserve locator semantics.
  reproducer: evidence/opentitan-spi-device-constraint-reducer-20260923/foreach_inline_exact.sv; pinned released Earlgrey SPI Device `spi_device_pass_base_vseq.sv` line 277.
  files: Root parse.y, PExpr.h, PExpr.cc, elaborate.cc, vvp/vvp_z3.cc, and dedicated regression fixtures. No VPI vvp/parse.y or vvp/vthread.cc edits, shared builds, installation, manifests, or application source edits.
  acceptance: Hierarchical foreach on the pinned-shaped sparse bit[7:0]-keyed associative array of class handles enforces constraints over actual keys in both editions; empty collection is vacuous; invalid or nonarray paths fail loudly; existing one-member foreach and find_index controls remain passing.
  state: Locally integrated at c64b1791f and published in PR339. The released SPI shape is a sparse bit[7:0]-keyed associative array of class handles; paired positive, empty, contradictory, and nonarray checks pass. Full SPI Device DV and broad qualification remain open.
parallel_adc_inner_implementation_lane:
  id: OT-ADC-NESTED-DYNAMIC-ARRAY-RESIZE
  owner: Reused lower-cost implementation agent; coordinator owns shared build, install, manifests, integration, and review.
  standards: IEEE 1800-2017/2023 sections 7.5, 18.4, and constraint foreach/size ordering in 2017 18.5.8.1 and 2023 18.5.7.1.
  reproducer: evidence/opentitan-adc-filter-debt-20260923/reducer_nested_struct_dynamic_size.sv remains RED on the PR339 candidate in both editions; warning says the indexed inner-size item is ignored and inner size remains 1 instead of 3.
  files: elaborate.cc and vvp/vvp_z3.cc, with vvp/vvp_darray.cc/h only if needed; dedicated ADC constraint fixtures. No VPI, Verilator, application sources, shared manifests, or build outputs.
  acceptance: Enforce each indexed inner size after outer allocation, preserve retained values, solve dependent element constraints in order, and roll back on contradiction; test preallocated and empty outer arrays, zero and impossible sizes in both editions. No ADC DV claim from a reducer.
  state: Scoped diagnostic complete; no inner-size source edit. Correct support requires row-indexed size and element identities plus a third solve stage; the dependent-element reducer is RED in both editions. Hold for a dedicated larger solver design lane, with no ADC DV progress claim.
parallel_spi_parameter_foreach_lane:
  id: OT-SPI-DEVICE-PACKAGE-PARAM-FOREACH
  owner: Reused lower-cost implementation agent; coordinator owns shared build, install, manifests, integration, and review.
  standards: IEEE 1800-2017/2023 foreach over unpacked package parameter arrays and lexical package lookup from a class method.
  reproducer: evidence/opentitan-spi-package-param-foreach-20260923/reducer.sv is RED in both editions; pinned Earlgrey-PROD-M6 spi_device_env_cov.sv line 296 has the same shape.
  files: elaborate.cc and the smallest necessary lexical parameter/type lookup implementation, plus dedicated ivtest fixtures only. No VPI runtime files, Verilator, pinned application source, shared manifests, or build outputs.
  acceptance: Iterate all constant package string-array entries inside a class constructor with correct index and values; empty array is vacuous; missing/nonarray target fails loudly; paired 2017/2023 regressions and neighboring foreach controls pass. Coordinator replays pinned SPI Device compile after integration.
  state: Committed at a6b609d5a with paired focused tests and main-manifest integration. The full local ivtest gate passes on the repaired candidate; pinned SPI Device still fails compilation. PR339 merged after Ubuntu 24.04 exact-head CI success; other CI jobs remained observable at merge.
parallel_spi_struct_queue_size_lane:
  id: OT-SPI-DEVICE-STRUCT-QUEUE-PARENLESS-SIZE
  owner: Reused lower-cost implementation agent; coordinator owns shared build, install, manifests, integration, and review.
  standards: IEEE 1800-2017/2023 queue size query and no-argument method-call spelling; verify the applicable clause and preserve strict struct-member errors.
  reproducer: evidence/opentitan-spi-queue-struct-size-20260923/reducer.sv fails in both editions; released spi_device_scoreboard.sv lines 1294 and 2235 report the same error.
  files: elab_expr.cc and dedicated ivtest fixtures only. No elaborate.cc package foreach edits, VPI files, Verilator, pinned application source, shared manifests, or build outputs.
  acceptance: Packed-struct element queue `.size` and `.size()` return equal live counts for empty and populated queues; genuine packed-struct `.size` without such a member stays an error; paired-edition tests and neighboring queue/string/assoc controls pass. Coordinator replays pinned SPI Device compile.
  state: Committed at 5afbe4c7f; paired runtime and missing-member negative tests pass in both runners, and the pinned SPI Device queue-size errors are removed. Full local gate passes; PR339 merged after Ubuntu 24.04 exact-head CI success; other CI jobs remained observable at merge.
parallel_adc_typed_fill_lane:
  id: OT-ADC-DIRECT-CAST-WIDTH
  owner: Reused lower-cost implementation agent; coordinator owns shared build, install, manifests, integration, and review.
  standards: "IEEE 1800-2017/2023 sections 5.7.1, 6.24.1, and 18.5: an unbased fill literal in a sized type cast fills the cast destination before constraint equality."
  reproducer: evidence/opentitan-adc-cast-width-triage-20260923/cast_width_repro.sv, cast_width_controls.sv, and cast_width_unsat_control.sv reproduce wrong 16/17-bit fills in both editions on the current installed compiler.
  files: elaborate.cc constraint IR and vvp/vvp_z3.cc cast/fill solver path as required; dedicated constraint ivtest fixtures only. No elab_expr.cc queue-size edits, VPI, pinned application sources, shared manifests, or build outputs.
  acceptance: Typed 16- and 17-bit unbased '1 constraints solve to all-ones before equality; 1-bit and sized 1'b1 controls remain 1; 'x/'z four-state direct cast controls stay exact; compatible and contradictory combined constraints behave correctly; paired editions and neighboring constraint regressions pass. No ADC DV claim from reducer.
  state: Committed at 5afbe4c7f; direct unbased-fill cast checks pass in both editions and runners with explicit unsupported boundaries. Full local gate passes; indexed ADC inner sizing and PR339 merged after Ubuntu 24.04 exact-head CI success; other CI jobs remained observable at merge.
pr339_caller_queue_ci_repair_lane:
  id: PR339-CALLER-QUEUE-STATE-WIDTH
  owner: Reused lower-cost implementation agent; coordinator owns shared builds, manifests, integration, and CI.
  reproducer: PR339 exact-head Ubuntu 22.04 CI and local six-test legacy focus; all paired sv_constraint_caller_queue, boundaries, and rollback cases fail at runtime with state value width mismatch.
  files: elaborate.cc constraint foreach IR and vvp/vvp_z3.cc state foreach solver path as needed; dedicated regression fixtures only. No VPI callback files, elab_expr.cc queue-size lane, shared manifests, or build outputs.
  acceptance: Six existing 2017/2023 caller-queue tests pass with enforced values, rollback and out-of-bounds behavior; new sparse-key SPI foreach checks and ADC typed-fill controls remain passing. No warning suppression or dropped constraints.
  state: Repaired at 5afbe4c7f; all six paired caller-queue cases pass in legacy and JSON runners and the full local gate passes. PR339 merged after Ubuntu 24.04 exact-head CI success; other CI jobs remained observable at merge.
pr339_vpi_ci_repair_lane:
  id: PR339-VPI-PACKED-FORCE-OVERLAP
  owner: Reused lower-cost implementation agent; coordinator owns shared builds, manifests, integration, and CI.
  reproducer: PR339 exact-head Ubuntu 22.04 CI and local bundled VPI suite fail packed_force_overlap in 2017 and 2023; new statement-object VPI tests passed focused locally.
  files: VPI force/release callback source in vvp and necessary tgt-vvp codegen only; dedicated VPI fixture if required. No elaborate.cc, vvp/vvp_z3.cc, elab_expr.cc, shared manifests, or build outputs.
  acceptance: Existing packed_force_overlap paired tests and new cbForce/cbRelease statement-object tests pass together; preserve selected-part overlap filtering and VPI registration semantics. Do not alter gold just to silence failure.
  state: Repaired at e4e2f00ea; paired packed-overlap and statement-object controls plus the full local VPI suite pass. PR339 merged after Ubuntu 24.04 exact-head CI success; other CI jobs remained observable at merge.
caliptra_verilator_differential_lane:
  id: CALIPTRA-L0-DOE-STATUS-SVA
  owner: Coordinator, read-only differential evidence.
  standards: IEEE 1800-2017/2023 disable iff abort of an active assertion attempt during an asynchronous reset pulse between sampling clocks.
  reproducer: evidence/caliptra-doe-verilator-midwindow-20260923/disable_midwindow.sv passes control=1 gated=0 on Icarus but fails control=1 gated=1 on Verilator 5.050 and isolated official v5.052.
  files: Existing diagnostic evidence only; no Verilator source changes.
  acceptance: Use Verilator only as a differential cross-check for Icarus work. Keep the pinned DOE SVA result failed; no tool patch or downstream qualification claim.
  state: Diagnostic complete and implementation deselected at user direction. Isolated Verilator source was restored to official v5.052; no patch remains. The direct defutured |=> and NFA paths are separate; see session_logs/2026-09-23_caliptra_doe_verilator_defuture_blocker.json.
completed_implementation_lane:
  id: OT-PWRMGR-CASE-EXIT-STACK-BALANCE
  owner: Claude Code Opus 5.5 high; coordinator owns shared builds, integration, and review.
  standards: IEEE 1800-2017/2023 sections 9.4 and 12.5; exiting a case body must not leave the selector on the VM evaluation stack.
  reproducer: /tmp/case-break-leak-2JBl42/case_break.sv emits one CLEANUP-leak stack_vec4 diagnostic while case_no_case.sv does not; pwrmgr csr_spinwait has the same case/break shape.
  files: tgt-vvp/vvp_process.c and dedicated case-exit regression fixtures/golds only; no application sources, pform.cc, ivlpp, elaborate.cc, vvp/vthread.cc, or shared build outputs.
  acceptance: Baseline red; case break/default/no-match, unique/priority, real selector, and nested control flow preserve values and leave no cleanup diagnostic; focused positive, negative, and boundary tests pass. Coordinator runs builds and tests.
  state: Merged as PR336 at 6ee647ca7 after Ubuntu 24.04 exact-head CI passed; focused and scoped released pwrmgr checks passed. Full release DV remains open.
completed_compiler_lane:
  id: OT-ADC-GUARDED-LARGE-DIST-EXACT
  owner: Codex + reused GPT-6 Luna; coordinator owned shared build, integration, and review.
  standards: IEEE 1800-2017/2023 distribution constraints and conditional constraint semantics; preserve exact legal sampling.
  files: vvp/vvp_z3.cc and dedicated constraint regressions only.
  state: Merged in PR337 at 9ac5a0fd1 with Ubuntu 22.04 and 24.04 exact-head CI success. Focused paired-edition and neighboring distribution checks passed; corrected ADC smoke advances to nested dynamic-array resize. Full ADC DV and broad IEEE qualification remain separate.
completed_fileset_lane:
  id: OT-PINMUX-FILESET-SOURCE-CLOSURE
  owner: Codex + GPT-6 Luna
  files: scripts/opentitan_matrix.py only.
  state: Merged in PR337 at 9ac5a0fd1 with Ubuntu 22.04 and 24.04 exact-head CI success; full OpenTitan matrix rerun remains open.
release_full_dv_campaign:
  objective: Fresh complete pinned-release OpenTitan and Caliptra DV inventory and execution; record every pass, compiler/runtime failure, setup gate, and qualification limit, then repair compiler blockers by priority.
  opentitan_inventory_lane:
    owner: Codex + GPT-6 Luna medium
    files: evidence/opentitan-full-dv-inventory-20260923 only
    source: Earlgrey-PROD-M6 a78922f14a8cc20c7ee569f322a04626f2ac6127, with named test-selected overlays/options.
    acceptance: Enumerate official DV targets/tests from pinned release metadata and runnable commands; distinguish formal/lint/unit entries and environment gates.
    state: Inventory frozen at SHA256 4441e17f1c077fea541b5ec850531bb2a00277d996b0119b3315cf273f02ba21 after a count-prose correction; 66 signoff configs and 1914 UVM named tests. The 358-job Icarus matrix and pinned UVM 1.2 rerun are complete; no application source edits.
  caliptra_inventory_lane:
    owner: Codex + GPT-6 Luna medium
    files: evidence/caliptra-full-dv-inventory-20260923 and evidence/caliptra-full-dv-unit-census-20260923 only
    source: Caliptra v2.1.2 49370266d12cb0c4a8f71b3a0ff7e54ba7d4866e and Adams Bridge v2.0.3 b77e3d899e828d626cfc2a0d26a6b5704cc121e0, with named test-selected overlays/options.
    acceptance: Enumerate official DV simulation/test groups and external dependencies; distinguish formal/unit/software entries and environment gates.
    state: Inventory frozen at SHA256 413bb36923db2b50103a24b40ed65b03f90046a8ac404231ac8fe352099b7c1b; all 44 unit RTL filelists attempted with 20 compile-only passes, 6 fails, 17 setup gates, and 1 unsupported. Two documented runtime probes complete; no source edits.
  prior_caliptra_l0_diagnostic: Earlier selected-overlay iccm_lock replay with only -Wno-MISINDENT emitted zero SVA errors and one pass marker at evidence/caliptra-kv-sva-overlay-20260923/patched-iccm-lock.result.json; its prior inspection found non-exact RISC-V multilib provenance. It preceded the official xPack GCC 15.2 rv32imc/ilp32 52-case lane and did not establish full L0 or DV qualification.
  coordinator: Owns OpenTitan matrix execution, shared builds, integrated failure catalog, implementation selection, and review after inventories are frozen.
