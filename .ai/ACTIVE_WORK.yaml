id: OPENTITAN-DV-PRIORITY-20260921
status: INTEGRATED_PASS_JSON_RUNNING
objective: Full IEEE1800-2017 and2023 independently, IEEE1800.2/UVM separately, ordinary RTL and standards-correct SVA/formal frontend, unmodified stable OpenTitan/Caliptra.
authority: User-authorized continuous campaign; coordinator selects standards-grounded reproduced blockers and preserves all semantics.
branch: agent/constraint-dependency-kind-repair-20260921
head_checkpoint: 0b1aeaee6c42e2d8fdc783aa86c112b9afa91027
history: docs/conformance/session_logs/2026-09-21_pre_spi_host_publication_ACTIVE_WORK.yaml
coordination:
  coordinator: Selection, hard semantic reasoning, integration, shared build/install/test directories, final review and publication.
  implementation_agents: At most two cheaper capable agents, disjoint ownership; reuse Sol and Terra. No new worktrees.
  preservation: External AGENTS.md edits and unrelated artifacts remain untouched. Stage explicit paths only.
  forbidden: Dropped semantics, fabricated outputs, application source workarounds, silent fallback, weakened validation.
selected_work:
  spi_host:
    id: OT-SPI-HOST-STRUCT-MEMBER-ARRAY-CONSTRAINT
    status: FOCUSED_TESTED_SOURCE_FROZEN
    commit: 81de58051
    owner: Sol medium plus coordinator integration; Terra medium getter completed.
    standard: 2017 18.3/18.4/18.4.2/18.5.8.1/18.8; 2023 iterative constraints18.5.7.1, other references shared.
    reproducer: ivtest/ivltests/sv_struct_array_constraint.v
    root: Direct member foreach grammar absent, no selected member-array IR leaf identity, selected rand_mode rejected; deeper tests exposed X/Z placeholder use and singleton randc classification.
    files: parse.y, PExpr.h, PExpr.cc, elaborate.cc, elab_expr.cc, vvp/vvp_z3.cc and registered tests.
    scope: One-level one-dimensional fixed integral member-array constraint leaves up to64bits; bounds/sign, state/activation, guarded unknowns, soft/dist/order, UNSAT rollback and cyclic history.
    acceptance: Paired runtime and negative tests, existing related tests, required broader gates and next unmodified application boundary; compiler success alone is insufficient.
    evidence: docs/conformance/session_logs/2026-09-21_spi_host_member_array_focus.json
    pending: Broad qualification and CI; next SPIHost output-type/event diagnostics and post-error crash are separate debt.
  spi_device_setup:
    id: OT-SPI-DEVICE-DEFAULT-SRAM-CONFIG
    status: LOCALLY_INTEGRATED
    commit: cd60ff54f
    evidence: docs/conformance/session_logs/2026-09-21_spi_device_default_sram_setup.json
    limit: Correct upstream1r1w default selection only;2p and full variant inventory remain unqualified.
  regex_configuration:
    id: OT-UVM-REGEX-CONFIG-PROPAGATION
    status: FOCUSED_TESTED_SOURCE_FROZEN
    commit: 1c18cd83c
    owner: Terra medium, coordinator independent self-test and fresh application replay.
    root: Discovery retained upstream UVM_REGEX_NO_DPI but compile_command omitted it, selecting strict DPI regex instead of upstream UVM1.2 SV glob matcher.
    files: scripts/opentitan_matrix.py and docs/conformance/opentitan_matrix.md
    fix: Forward only the known exact discovered define on UVM-importing UVM/runtime jobs; keep ordinary -uvm and other DPI enabled.
    evidence: docs/conformance/session_logs/2026-09-21_opentitan_regex_configuration.json
    limits: No compiler/UVM/application source changes. GPIO still has weighted-soft fallback; pwrmgr still fails null-VIF event at0. No full DV pass.
qualification:
  risk: HIGH because shared constraint IR and element sampling changed.
  focused: New12cases pass legacy in2017 and2023 and24pairedJSON; declaration15, array-mode8 andtransaction29perharness pass.
  expectation_reconciliation: b04404191 reconciles four stale focus-only extension golds; unsupported iterator remains a compile-negative, all randc-soft rejections preserved. Audit is source-history based, not exact-baseline binary replay.
  active_gate: Repaired integrated gate42301 passed; full JSON running as session99700 in spi-host-repaired-qualification.
  pending_gates: Full JSON, mapped UVM/smoke/full UVM, NFA, frontend and makecheck according to risk; CI before promotion.
  source_freeze: No shared compiler/runtime edits or installation during gates; assess next blockers read-only.
next_action: Publish dependency-kind repair follow-up because PR311 merged before repair; finish existing required gates before next implementation selection.
assessments_during_qualification:
  gpio_dist:
    owner: Retained Terra medium, read-only compiler/app navigation and private reducers.
    observation: Correctly configured GPIO smoke reaches finish but emits dist range exceeds256-member exact-sampling cap and weighted-soft fallback.
    scope: Trace exact application dist directive and runtime warning path, produce smallest positive/negative/boundary reducer and exact-semantics proposal. No shared source/build/install or application edits.
    evidence: evidence/review-20260920/opentitan-regex-config-replay/result.json
  pwrmgr_vif:
    owner: Retained Sol medium, read-only compiler/app navigation and private reducers.
    observation: Correctly configured pwrmgr reaches time0 null virtual-interface event errors and TL outstanding assertion.
    scope: Root assessed; Sol medium prepares private paired activation/isolation/rebind/lifetime/negative tests and considers existing signal accessor reuse. No shared source/build/install or application edits; no null suppression or constant substitution.
    evidence: evidence/review-20260920/opentitan-regex-config-replay/result.json

qualification_enum_regression:
  owner: Coordinator, vvp/vvp_z3.cc VarRef/OrderRef kind values only
  status: FOCUSED_TESTED_SOURCE_FROZEN
  evidence: evidence/review-20260920/spi-host-qualification/integrated.run.json and enum-regression/names.json
  failure: Legacy6138cases has42new constraint-function failures; VPI126,negatives148,runtime15 pass. No expectation waiver.
  root: Inserting MEMBER_ELEM before ELEM/SIZE shifted numeric values, but class_type constraint dependency metadata still encodes PROP0/MEMBER1/ELEM2/SIZE3 and is cast into VarRef kinds.
  fix: Explicitly alias existing VarRef kinds to metadata values, append MEMBER_ELEM, and alias OrderRef kinds to VarRef. Existing42tests directly cover the compatibility invariant.
  acceptance: All42failedcases, newmember-arraypairedpackage and adjacentfunctions; then rerun integrated gate because installedruntime changed.

repair_validation:
  evidence: docs/conformance/session_logs/2026-09-21_spi_host_dependency_kind_repair.json
  results: All42priorfailures pass; expandedmemberfocus16peredition and32pairedJSON pass.
  next: Integrated repaired gate passed; JSON session99700 running. Remaining UVM/NFA/frontend/makecheck and CI pending.
