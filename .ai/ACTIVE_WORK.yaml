id: "L31"
title: "Associative compound assignment expression signedness and sizing"
status: "active"
phase: "awaiting_validation"
base_revision: "39475cf98"
last_validated_revision: "34787868e"
reproducer: "../evidence/campaign-20260908/l31/assoc-compound.sv and red.json; signed byte associative element -64/=2 produces96 in both editions,compile0/runtime1."
expected_behavior: "Associative element compound arithmetic must evaluate the equivalent binary expression with declared element signedness, mixed-operand signedness, expression width and four-state information preserved until final assignment conversion. Shift counts remain self-determined and unsigned selections remain unsigned."
standards: ["Both IEEE1800-2017/2023 11.4.1,11.4.3,11.4.10,11.6,11.8.1/11.8.2;6.11.2 final state conversion."]
root_cause: "PAssign::elaborate_compressed_ uses default lvalue signed flag for plain associative elements; associative read/modify/write expansion rereads it and builds the binary node at destination width. Emitted signed-byte case uses unsigned div. Existing resolved net_type provides element type."
suspected_symbols: ["PAssign::elaborate_compressed_", "build_compound_binary_", "lv_is_assoc_element_", "NetAssign_::lval_type"]
allowed_scope: ["Correct existing associative compound expression typing at elaboration; reuse resolved element type and existing expression sizing/padding rules; local/property and signed/unsigned/mixed/shift/state controls; permanent regressions and fullvalidation."]
non_goals: ["No new runtime instruction or container architecture, no general class completion or blanket signedness on concatenations/part-selects, no UVM recording provider. Unrelated preexisting key/receiver evaluation defects are record-only unless proven prerequisites or patch-caused."]
dependencies: ["Fully validated34787868e;published PR2756506fc7a2 freshCI pending,not a semantic dependency."]
required_gates: ["Both-edition red/green and independent design/code review; signed division/remainder/arithmeticshift,mixedunsigned,wide operands,fourstate and local/property controls; focus/neighbors/makecheck/integrated/JSON/UVM/NFA/releases;frontend lastalone andrestorefrozenhashes."]
evidence: "../evidence/campaign-20260908/l31/"
discovery_policy: "Unrelated discoveries record-only; regressions remain in scope."
next_action: "UVM32316exit0 REAL DPI355/0/0; all other prior gates terminal. Poll frontend49875 lastalone,then verify installation restored/fivefrozenhashes. L31notclosed until frontend passes."
next_command: "tail -n 15 ../evidence/campaign-20260908/l31/frontend.log"
stop_after_completion: false

focused_validation: "Original/expanded reds nowPASS both editions/no diagnostics;permanentfocus2legacy/2JSON;neighbors125legacy/92JSON;makecheck and independent design/code/ownership reviews clear. Nativebuild24224exit0. Integral typingguard preservesreal path;unsigned selections/concats unchanged. Fullgates pending."
