id: L45
title: Bare unresolved SystemVerilog task calls compile as warning-only no-ops
status: IMPLEMENTED_FOCUSED_PASS
base_revision: ad33d31e2
origin: DD-020
standards:
  - IEEE 1800-2017 13.3 and 23.8.1
  - IEEE 1800-2023 13.3 and 23.8.1
reproducer: evidence/unresolved-task-assessment/missing_task.sv
current_behavior: A bare missing_task() call warns, compiles successfully, and execution continues after silently dropping the call.
expected_behavior: After task, function, upward, compilation-unit, and implicit-class lookup fail, an unresolved bare task enable is an elaboration error.
root_cause: PCallTask::elaborate_usr converts the final unresolved SystemVerilog task path to an empty NetBlock under a broad compile-progress fallback.
allowed_scope:
  - elaborate.cc final bare unresolved SystemVerilog task diagnostic
  - Focused missing-task and legal lookup regressions, manifests, gold and evidence
  - ACTIVE_WORK and directly related campaign records
forbidden_scope:
  - Qualified or indexed unknown-task compile-progress fallbacks
  - Class/interface method resolution changes
  - UVM stub-candidate policy changes
  - Broad lookup refactoring
completion_requirements:
  - Missing bare task is red before and a compile error after in 2017 and 2023
  - Forward same-scope, complete compilation-unit, upward enclosing-scope, and implicit class task lookup stay green
  - Focused legacy and JSON regressions plus one cheap applicable UVM smoke
  - Durable logs with exit status; broad suites deferred to the campaign batch boundary
validation_batch: 20260914
focused_features_before_this: 2
last_validated_revision: Working tree after rebuild/install. Missing bare task is a compile error in 2017 and 2023; forward same-scope, complete compilation-unit, upward enclosing-scope and implicit class task lookup pass in both. Focused legacy 2/2 and JSON 4/4 pass. Real UVM config-db smoke compiles and runs PASS. Durable logs are evidence/unresolved-task-assessment/final-focus-{legacy,json}.log and final-uvm-smoke.log, all with zero status.
pending_gates: Root review/checkpoint and campaign batch gates; broad suites deferred until batch boundary.
next_action: Root reviews and checkpoints L45; qualified/indexed/class compile-progress fallbacks remain out of scope.
evidence: evidence/unresolved-task-assessment/
discovery_policy: Qualified and class fallback gaps remain separately tracked and out of scope.
stop_after_completion: false
