id: CONSTRAINT-MULTIPREFIX-REDUCTION
title: Reduce correctly selected multidimensional fixed-array rows in constraints
status: DONE
base_revision: 9ad2905c8
standards:
  ieee1800_2017: "7.12.3 and18.5.8.2"
  ieee1800_2023: "7.12.3 and18.5.7.2"
reproducer: evidence/constraint-multidim-reductions-l65/selected-row.sv
current_behavior: "Both editions reject a[1][2].sum() as unsupported; baseline.json preserves compiler1, stable installed artifacts."
expected_behavior: "Select the exact one-dimensional fixed-array row, apply all five integral reductions with normal with-expression sizing, preserve active/state aliases and rollback."
root_cause: "Reduction matcher excludes more than one prefix; canonicalization processes only the first selected dimension."
allowed_scope:
  - Match multiple constant prefix selections leaving exactly one fixed unpacked dimension
  - Canonicalize every selected dimension with declared bounds and exact row stride
  - Preserve reduction width/sign, with iterator scope/index, active element identities and transactional rollback
  - Add focused positive, boundary and negative paired edition tests
forbidden_scope:
  - Runtime-selected prefix solver redesign
  - DD030 packed-select increment and DD031 fixed-array return changes
  - Broad unrelated random solver or locator redesign
validation_batch: 20260914_after_l64
focused_features_before_this: 0
focused_features_completed_in_batch: 1
pending_gates: "Required feature checks passed: root2/2, independent paired positives/negatives, permanent8/8 and neighbors6/6 each. Full suite deferred to batch checkpoint by user cadence."
next_action: "Local focused L65 checkpoint ready; return to coordinator selection for feature2 of the batch. PR280 publishes only L43-L64; CI pending. Broad qualified baseline remains L64."
stop_after_completion: false
