id: B01
title: Restore Windows coverage API exports for PR273
status: in_progress
phase: awaiting_validation
user_authorization: Required correction to the existing user-requested PR273; preserve L06 at a deliberate prerequisite validation boundary.
reproducer: PR273 UCRT64 job102955775883 fails linking vvp.tgt with seven undefined ivl_type_covgrp functions.
root_cause: Seven implemented/declaration-visible coverage API functions are missing from ivl.def used by the Windows import library.
allowed_scope:
  - Add exactly the missing coverage exports to ivl.def.
  - Permanent coverage API/export consistency check and integrated gate registration.
  - Focused evidence and update existing PR273 with only this correction; no merge.
non_goals:
  - No L06 semantic code in this patch, no other export/API redesign, no installed tool mutation while L06 gates run.
required_gates:
  - Failing old export map and passing corrected map; all declared coverage API names exported.
  - Independent review, script syntax/diff checks, required Windows CI before completion/merge.
completion: Local export invariant can validate the patch; Windows remote gate remains required and cannot be reported passed from native checks.
suspended_parent: L06 candidate source/tests and gatehandles preserved in evidence/b01/suspended-l06.yaml; integrated48575 completed, UVM55602 still running, fullJSON25806 passed1719/0.
stop_after_completion: false
next_action: Inspect required PR273 CI at08b86b949; await L06 UVM55602. Close B01 only after Windows gate passes, then resume L06 validation.

local_validation: Old map fails exact seven; fixed map passes66/66; syntax, diff and independent review pass. Windows CI pending.
publication: https://github.com/dsellerbrock/iverilog-uvm/pull/273 at08b86b94931f62958d1f4441a7fb90a7e5c48318
next_command: gh pr view 273 --json headRefOid,statusCheckRollup
