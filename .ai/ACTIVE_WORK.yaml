id: "DD046-TRIAGE-2"
title: "Re-triage the DD046 curated candidate table against current source"
status: "active"
phase: "candidate_investigation"
base_revision: "b8c5bfe6b"
last_validated_revision: null
parent: "DD046 user-authorized sweep of all fallback/compile-progress paths"
reproducer: "See evidence/campaign-20260908/dd046/c14_incdec.sv, c15_locator.sv, c16_locator_prop.sv for the three dispositioned-resolved candidates this pass; no open reproducer yet for an undiagnosed candidate."
expected_behavior: "Every DD046 curated-table row classified per its own completion rule: valid behavior, unreachable/superseded with evidence, explicit unsupported/qualification gap, or a reproduced semantic defect -- not left as a stale 'Observed implementation' description from an earlier revision."
root_cause: "N/A this pass -- no new defect found and fixed. Four of twelve curated-table rows re-examined: Membership and Increment/decrement and Array locators are already resolved (loud diagnostics + real implementations for the legal constructs tried, per the DISCOVERED_DEBT.md table update); String/object lowering is partially resolved, with eval_string.c:249's silent empty-string-on-unsupported-select-base branch still open but no legal reproducer found in five attempts (each hit an earlier parser/elaboration rejection or resolved correctly through a different path)."
allowed_scope: ["Re-verify each remaining curated-table row against current source with a concrete reducer before selecting it as the next implementation blocker. Do not assume the table's 'Observed implementation' column is still accurate -- three rows checked this pass were stale."]
non_goals: ["No fix attempted this pass without a reproducer (per AGENTS.md targeted-fix protocol: REPRODUCED before PATCHED).", "The real-typed (IVL_VT_REAL) nested class-property VPI-lvalue gap named as out-of-scope in L33/L34 is a legitimate future candidate but was sized by advisor review as needing a NEW runtime class + grammar token + codegen (no existing CPR class or __vpiClassPropertyRealVar, unlike L33/L34 which reused an existing class) -- larger than a single L33/L34-style increment; not started this pass, would need its own sizing pass first."]
validation_contract: "Once a genuinely open, reachable candidate is selected and reproduced: minimal fix, permanent regression, independent review, full required gate sweep (integrated/JSON/UVM/NFA/releases/frontend-last), matching the L33/L34 pattern exactly."
discovery_policy: "Unrelated discoveries record-only in DISCOVERED_DEBT.md."
evidence: "../evidence/campaign-20260908/dd046/"
next_action: "Remaining unexamined curated-table rows, in rough order of apparent reachability: Casts (elab_expr.cc:18184, already has a loud warning for packed-struct cast targets -- check if the 'return sub unchanged' branch for NON-packed unhandled targets is reachable and wrong); Constraints (parse.y:18467, with-clause dropping); Events and waits (elaborate.cc:19847 etc.); Iteration (elaborate.cc:22119, nested associative foreach); Unresolved methods/functions (elab_expr.cc:8297, elaborate.cc:14061/14072); Lost expression typing (netmisc.cc's elab_and_eval, deep in parameterized/generic method argument typing -- construction of a minimal legal reducer not yet attempted, likely the hardest of the remaining rows). Simulator/VPI and Qualification fallback rows are harness/infra-flavored, lowest priority for a compiler-semantics blocker."
next_command: "sed -n '735,745p' docs/conformance/DISCOVERED_DEBT.md to re-read the current table, then pick one row and write a reducer before touching any source."
stop_after_completion: false
