id: "U12"
title: "Reject simulator diagnostics in release smoke classification"
status: "closed"
phase: "completed"
base_revision: "c37f5b9c8"
last_validated_revision: "e18128369"
reproducer: "DD043 results-zyhh1jv9/1.0p1/runtime.log: rawSMOKE_PASS despite3placeholder-net and2cast errors;permanent offline classifier negative."
expected_behavior: "Zero exit and clean UVM summaries cannot override simulator runtime errors, fatal/warning diagnostics or unresolved placeholder nets. Preserve required marker,summary,timeout checks and legitimate informational output."
standards: ["Evidence-integrity harness correction;no new IEEE language implementation claim."]
root_cause: "smoke_passed validates exit/timeout/exactmarker/UVM summary only;does not reject simulator diagnostics outside UVM reporting."
allowed_scope: ["Shared smoke_passed classifier,existing offline test_matrix controls,full original15release replay and directly affected documentation."]
non_goals: ["No compiler/runtime/DPI changes,originalsource edits,1.0p1cast/functor repair,1.1b/cmacro/parser repair or new qualification claims."]
required_gates: ["Pre-fix false-positive proof;all offline matrix tests;historical runtime log polarity controls;fresh15release matrix and unchanged tool/source fingerprints;independent review. Existing e18128369 full compiler gates remain baseline;harness-only change does not require rebuild or duplicate compiler sweeps."]
next_action: "Inspect remaining original1.0p1 runtime diagnostics and select exact causal blocker."
next_command: "sed -n '460,492p' third_party/uvm-releases/sources/1.0p1/uvm-1.0p1/src/base/uvm_callback.svh"
stop_after_completion: false
