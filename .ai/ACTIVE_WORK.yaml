id: C01
title: Reject untranslated inline constraints instead of solving without them
status: completed_local
phase: checkpoint
base_revision: 8f298eefe
standards:
  - IEEE 1800-2017 18.7 inline constraints
  - IEEE 1800-2023 18.7 inline constraints
expected_behavior: A randomize with call applies every requested constraint; unsupported lowering must fail explicitly, never emit a successful partial solve.
observed_behavior: make_randomize_with_expr still warns and continues when an item yields empty IR without an existing error.
hypothesis: The shared inline class builder fails to increment Design errors on its fallback path.
allowed_scope:
  - elab_expr.cc (shared inline constraint failure path)
  - ivtest (focused failure and supported controls; affected diagnostic gold)
  - .ai/ACTIVE_WORK.yaml
  - .ai/CAMPAIGN.yaml
  - docs/conformance/BLOCKERS.md
  - docs/conformance/matrices/ieee1800_2017_clause_matrix.md
  - docs/conformance/session_logs/2026-09-08_continuous_campaign.md
non_goals:
  - Implement unsupported expression shapes or broader constraint semantics.
  - Change unrelated class-declaration or runtime solver behavior.
invariants:
  - Preserve existing precise diagnostics; no duplicate generic error after a diagnosed failure.
  - Preserve receiver/capture ownership and all shared expression/task callers.
  - Explicit failure does not qualify the unsupported legal feature as implemented.
required_gates:
  - Both-edition red acceptance/constraint-loss reducer and permanent rejection regression.
  - Supported inline expression/task controls and focused legacy/JSON diagnostics.
  - Integrated ivtest/VPI/negative/runtime, full JSON, real-DPI UVM, make check and independent review.
  - Remote CI before merge.
completion: Silent-discard defect resolved; all required local gates passed. Unsupported shapes remain unimplemented; remote CI/merge pending.
discovery_policy: Unrelated discoveries are record-only.
stop_after_completion: false
