id: "U10"
title: "OpenTitan UVM1.2 selection and truthful replay provenance"
status: "closed"
phase: "done"
base_revision: "578ca58e9"
last_validated_revision: "19e7f5592"
selection_reason: "User requests OpenTitan pinned to1.2; pinned VCS/Questa/Riviera/Xcelium configs explicitly choose1.2. U09 all required gates passed."
reproducer: "Current matrix compile_command unconditionally injects bundled -uvm and compiler_fingerprint hashes installed bundled sources. No argument selects original1.2."
expected_behavior: "Explicit --uvm-home selects an acquired original library only for jobs that require UVM; source fingerprints describe that selected source tree. Existing default behavior and no-UVM lanes remain unchanged. Replay pinned OpenTitan debug crossbar using checksum-pinned original1.2 and authoritative traffic/checker classification."
standards: ["Pinned OpenTitan7a3ad34 hw/dv/tools/dvsim/{vcs,questa,riviera,xcelium}.hjson select1.2; harness/provenance change, not IEEE semantic gain."]
root_cause: "Harness does not forward the existing compiler source selector and assumes bundled sources for provenance."
allowed_scope: ["scripts/opentitan_matrix.py argument validation, UVM-only compiler selection, source fingerprint and permanent self-test; directly affected README/evidence/blocker records."]
non_goals: ["No compiler/library/application source changes or warning/traffic/checker threshold relaxation. Application FAIL remains FAIL and may select a later blocker. No full UVM/IEEE/formal completion claim."]
required_gates: ["Permanent self-test for selected source fingerprints and UVM-only command routing; invalid path rejection; actual pinned OpenTitan1.2 replay with commands, source hashes, traffic/checker/normal completion evidence. Compiler remains validated19e7f5592.", "Independent scope review and diff/YAML checks."]
next_action: "Select DD031 legacy nested macro expansion after publication checkpoint."
next_command: "graphify query \"macro argument expansion\" --graph /Users/danielellerbrock/projects/iverilog_uvm/graphify-out/graph.json"
stop_after_completion: false
