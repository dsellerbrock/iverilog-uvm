id: "DD046-TRIAGE-2"
title: "Comprehensive re-triage of the DD046 curated candidate table"
status: "closed"
phase: "done"
base_revision: "b8c5bfe6b"
last_validated_revision: "HEAD (post-retriage commits, post-probe-sweep)"
parent: "DD046 user-authorized sweep of all fallback/compile-progress paths"
reproducer: "See evidence/campaign-20260908/dd046/c14_incdec.sv through c21_nested_foreach3.sv for the concrete reducers used on 9 of 12 rows; the remaining 3 (Unresolved methods/functions, Simulator/VPI, and finally Lost expression typing) were settled by tracing callers/overrides and, for Lost expression typing specifically, by an instrumented empirical sweep (getenv-gated probes at all 7 silent stub sites in netmisc.cc, run against the 357-test UVM smoke suite and the full ~5000-test ivtest suite -- 0 hits in either, instrumentation fully reverted afterward, verified via git diff)."
expected_behavior: "Every DD046 curated-table row classified per its own completion rule: valid behavior, unreachable/superseded, explicit unsupported/qualification gap, or a reproduced semantic defect."
root_cause: "N/A -- no new defect found this pass. All twelve curated-table rows re-examined against current source; all twelve are RESOLVED or ALREADY-LOUD (fixed by earlier sessions since the table was written, never actually silent per DD046's own definition, or -- for the one row that WAS genuinely silent everywhere on its path, Lost expression typing -- empirically unreached across both the UVM and ivtest corpora). Full findings per-row in docs/conformance/DISCOVERED_DEBT.md's DD046 section."
allowed_scope: ["Documentation/triage only this pass -- no source changes shipped. A temporary getenv-gated probe was added to netmisc.cc to test reachability of the Lost-expression-typing branch, then fully reverted (git diff clean) before this file was written; the reverted state was rebuilt and reinstalled."]
non_goals: ["No fix attempted without a reproducer, per AGENTS.md's targeted-fix protocol.", "Did not force-fit a fix onto any of the 12 resolved rows to manufacture 'progress' -- the honest finding IS the progress (closes 12 open questions DD046's completion rule required answered)."]
validation_contract: "No committed code changed, so no gate sweep required for the final state. The probe build itself was validated against the full ivtest suite as a side effect (4981/4976/0F/2NI/3EF, exact clean baseline -- confirms the instrumented build was behaviorally identical to the tracked baseline for every non-probe path) before being reverted."
discovery_policy: "Unrelated discoveries record-only in DISCOVERED_DEBT.md (done: elab_expr.cc's sorry: on find() over fixed non-integral arrays, noted but not opened as its own blocker since it's loud)."
evidence: "../evidence/campaign-20260908/dd046/"
next_action: "DD046's curated 12-row table is now fully exhausted -- every row is RESOLVED, ALREADY-LOUD, or (Lost expression typing) empirically unreached across ~5300 tests. A genuinely fresh DD046 increment requires mining the raw 12849-line evidence/campaign-20260908/fallback-tracked-candidates.tsv for NEW candidates the curated table never captured (9633 constant_return / 2178 degradation / 501 stub-tagged lines exist outside the 12 curated rows), OR shifting to the user's third priority phase (broader implementation work) since DD046's bounded, quickly-reachable candidates are exhausted for now. The real-typed (IVL_VT_REAL) nested class-property VPI-lvalue gap (L33/L34's named follow-on) remains available too, but was already sized as needing a new runtime class + grammar token (bigger than L33/L34's reuse pattern) -- may warrant its own dedicated sizing session."
next_command: "Either: (a) filter fallback-tracked-candidates.tsv for stub/degradation-tagged lines in files already proven bug-dense this session (tgt-vvp/*.c, vvp/*.cc, elab_expr.cc, netmisc.cc) that are NOT within a few lines of an existing cerr/fprintf(stderr -- a mechanical way to find genuinely silent paths per advisor's guidance rather than guessing; or (b) size the real-typed CPR/__vpiClassPropertyRealVar runtime-class + grammar-token work as its own increment; or (c) ask the user which of the above, or the third priority phase, to pursue next."
stop_after_completion: false
