status: pr_open_ci_pending
branch: agent/sva-overlap-failure-count-20260923
baseline: PR331 head 3566335a5bc02148d17b940670cceb05e7888563 now merged as 42f324c90294ed7356a467e8b6c95d94a9ba2c6e; feature branch remains based on that source head.
blocker: SVA-LITERAL-OVERLAP-FAILURE-COUNT
goal: Report one assertion failure action for each distinct live implication attempt whose literal-repetition consequent fails on the same sampled edge.
standards: IEEE 1800-2017 and IEEE 1800-2023 implication attempt/verdict semantics in 16.12.7; verify exact edition wording before patching.
failing_reproducer: The paired baseline at 7590c2627 observed one same-edge failure action instead of two; the checked-in reducer and paired current result are recorded in docs/conformance/session_logs/2026-09-23_sva_literal_overlap_attempt_identity.json.
expected: Distinct active attempts each receive their own fail action; alternate NFA paths belonging to one attempt must not duplicate a verdict. Preserve vacuity, successful attempts, four-state sampling, disable iff cancellation, and pass/fail action ordering.
root_cause: This literal shape routes through the legacy linear checker, not the NFA. A one-bit failure flag collapsed distinct fixed-offset dying tokens, and sampled X/Z was not classified as a definite nonmatch. The count repair exposed a separate raw-time VPI attempt identity gap, also fixed for this subset.
allowed_scope:
  - pform.cc legacy linear checker fixed-offset verdict representation and dispatch, plus definite-match testing for four-state sampled guards in those same fixed-offset checks.
  - Dedicated paired-edition positive, negative, and boundary SVA regressions and gold files in tests/sva_nfa and ivtest.
forbidden:
  - OpenTitan or Caliptra source edits; counting NFA sibling paths as separate attempts; silent assertion drops or capped approximations.
  - Edits to PR329/PR330/PR331 heads or Claude-owned elaborate.cc, vvp/vthread.cc, vvp/vvp_z3.cc, and vvp/vvp_vinterface.cc without coordinator scope expansion.
  - Shared builds, installations, and shared regression output directories by implementation agents; coordinator owns them.
acceptance: Met locally at e54b76124 for the stated fixed-linear subset; paired reducers, focused neighbors, integrated gate, full JSON, real-DPI UVM, and make check pass. The revision-scoped session record owns exact evidence.
implementation_lane:
  owner: Codex + Sol
  files: pform.cc legacy linear checker; dedicated SVA tests and gold files only
  handoff: Implementation integrated locally and published in PR332; exact-head CI pending.
parallel_assessment_lane:
  id: OT-SPI-DEVICE-CONSTRAINT-FOREACH-PATH
  owner: Codex + Luna
  files: evidence/opentitan-spi-device-constraint-reducer-20260923 only
  scope: Completed evidence-only reducer. The exact hierarchical foreach fails in both editions; dotted and plain controls plus the exact locator pass. No compiler implementation or shared build/install.
  dependency: Claude owns elaborate.cc; implementation requires explicit ownership handoff.
completed_assessment: OT-AON-EVENT-TRIGGERED-SENSITIVITY reducer confirms that @(e.triggered) fails elaboration in both editions while @e and wait(e.triggered) controls pass; evidence/opentitan-aon-triggered-reducer-20260923 remains local pending durable evidence review.
integration_blocker:
  id: SVA-VPI-ATTEMPT-START-IDENTITY
  owner: Codex + Sol, continuing the selected SVA implementation lane
  standard: IEEE 1800-2017 and 2023 39.4.2 requires cbAssertionFailure attemptStartTime to identify the actual attempt.
  evidence: Resolved for fixed non-negated linear checkers at e54b76124. Distinct t25 callbacks now report start25/start5; VPI 127/127, full real-DPI UVM 358/358, and required local gates pass. See docs/conformance/session_logs/2026-09-23_sva_literal_overlap_attempt_identity.json.
  allowed_scope: pform.cc fixed linear checker; vpi/sys_sva.c and vpi/libvpi.c; vvp/vpi_scope.cc and vvp/vpi_priv.cc; internal callback API declarations in vpi_user.h; focused VPI tests/gold. Preserve unrelated checker behavior and Claude-owned files.
  acceptance: Met locally for this subset; PR332 exact-head CI pending. Other checker families retain the two-argument path and remain separate.
pending_prs: PR329, PR330, and PR331 merged after exact-head CI success; PR332 is open against main.
failed_attempt: The first candidate changed the NFA engine, but the generated literal image contains legacy t-stage registers and no NFA slot registers. Both paired-edition reducers remained red; evidence/sva-literal-overlap-candidate-20260923/focus.json. Revert that ineffective patch before the legacy correction.
next: Track PR332 exact-head CI and merge after one current-head success if still ready and mergeable. Do not infer full assertion, formal, or application qualification.
