id: PARALLEL-BATCH-L90-L91
title: Enum container packed selects and finite Boolean multiclock repetition
status: IMPLEMENTING
base_revision: fd500e2b5
authority: "User authorized two existing cheaper implementation lanes, disjoint ownership, focused validation per fix and broad qualification near ten integrated fixes."
coordinator_owns: [All shared builds and installation, Legacy and JSON harnesses, Main manifests and documentation, Integration and review]
lanes:
  - id: L90-ENUM-CONTAINER-PACKED-SELECTS
    parent: IEEE1800 enum and packed-select elaboration
    agent: /root/constraint_abi
    status: REPRODUCED
    standards: "IEEE1800-2017/2023 6.19,6.19.3,11.5.1; preserve enum nominal type for whole-object assignment."
    reproducer: evidence/batch-20260914-after-l84/next-packed-assessment/enum.sv
    actual: "Both editions reject b.values[0][1] and b.values[0][3:2] for a dynamic array of enum values; equivalent bit/signed/logic controls run PASSED. See typed-baseline.json."
    expected: "Recognize the enum packed carrier for selected class-container elements, preserving canonical indices, selected vector type and partial-write behavior."
    owns: [elab_expr.cc, elab_lval.cc]
    test_prefix: sv_class_enum_container_packed_select
    allowed_scope: "Packed bit, fixed-part and indexed-part selects on enum elements of class dynamic arrays and queues, for read and generic lvalue paths; use enum base_type_obj and existing select/word-part lowering. Fix sibling read/write gates once, preserve signedness/four-state/direction, evaluate selectors/RHS once, preserve neighboring words/bits."
    forbidden_scope: "Whole-enum conversion relaxation, object/real containers, array-return expansion, unrelated parser/runtime changes, pform/SVA/solver sources, fabricated values or ignored indices."
    dependencies: "L89 integrated1ffe476b6; independent of L86. Root owns shared builds/install, harnesses/manifests, documentation/integration."
    acceptance: "Both editions: dynamic/queue element bit/fixed/indexed +/- reads and blocking writes, signed/ascending and2/4-state enum bases, full-width selected vector vs nominal whole-enum typing, side-effectful selectors/RHS exactly once, neighbor preservation, X/OOB defaults and suppressed writes, const/readonly negatives; compound/incdec generic lvalue controls and non-enum neighbors."
  - id: L91-MULTICLOCK-BOOLEAN-CONSECUTIVE-REPETITION
    parent: S02
    agent: /root/constraint_wide
    status: REPRODUCED
    standards: "IEEE1800-2017/2023 16.9.2,16.9.2.1,16.12.7,16.13 and16.14. Verify empty-match handoff separately for each implication form."
    reproducer: evidence/batch-20260914-after-l84/next-repeat-assessment/multiclock_bounded_repeat.sv
    actual: "Both editions reject a[*1:2] as a multiclock antecedent; expected one parent failure after the second endpoint child fails."
    expected: "Finite consecutive repetition of Boolean expressions retains every permitted match and one parent verdict through the L86 engine, including standards-correct zero-count/empty composition where legal."
    owns: [pform.cc, pform_sva_nfa.cc, pform_sva_nfa.h, parse_misc.h]
    test_prefix: sv_assert_multiclock_boolean_repeat
    allowed_scope: "Finite literal-bound Boolean expr[*m:n] in otherwise supported single-clock antecedent chains, including m=0, internal/trailing repetitions, and existing fixed first-clock consequent prefixes. Reuse finite NFA expansion and L86 parent transport. Add minimal repetition provenance if needed to distinguish Boolean operands from unsupported grouped sequences; preserve every empty/nonempty endpoint according to each implication clock rule."
    forbidden_scope: "Unbounded or goto/nonconsecutive repetition, grouped multi-step repetition silently treated as last-expression repetition, symbolic-bound redesign, general locals/match-action operators, L90 expression/lvalue files, new runtime scheduler architecture. Diagnose remaining forms."
    dependencies: "L86 integrated13fee7ec9; independent of L90. Root owns all shared builds/install/harnesses/main manifests/docs."
    acceptance: "Both editions: multiple endpoints and parents, m=0/1/>1 and m=n, internal/zero-delay composition and legal empty-match handoff, |->/|=> and fixed prefixes, sampled vector truth, early failure/vacuity, disable/kill/restart, paused-clock backlog,64tick boundary and unsupported grouped/unbounded controls; L86 and NFA neighbors. Do not reduce this ticket to only positive-lower-bound repetition."
file_conflict_policy: "No edits outside lane ownership; root owns shared builds, harnesses, manifests and docs. Preserve published PR283/284 branches."
validation_batch: 20260914_after_l84
focused_features_completed_in_batch: 5
last_broad_qualified_semantic_revision: 240dd5f6f
last_broad_qualification: docs/conformance/session_logs/2026-09-14_compiler_batch_l75_l84_qualification.json
stop_after_completion: false

coordinator_scope_refinement:
  l85: "A retained complete-range guard evaluated after a prefix draw can cause seed-dependent unsupported rejection. Prove every reachable prefix before sampling or implement exact conditional range behavior; no success-conditioned sampling."
  l86: "IEEE 16.12.7 and 16.14.1 require a single property-attempt verdict across sibling antecedent endpoints. Scalar request counts lose parent identity. A parent with two matches and one failed child must dispatch pass=0/fail=1. Dynamic parent-tagged transport and retirement are necessary because a paused destination clock leaves an unbounded backlog even with a finite antecedent. Architecture expansion is coordinator-authorized; fixed-path cloning is incorrect."

integrated_focused_fixes:
  - id: L85
    revision: 9f416e09a
    evidence: docs/conformance/session_logs/2026-09-14_joint_ordered_distribution_validation.json
    broad_qualification: pending near ten integrated fixes
  - id: L87
    revision: 831cdeca2
    evidence: docs/conformance/session_logs/2026-09-14_joint_element_ordering_validation.json
    broad_qualification: pending near ten integrated fixes

l86_increment_status: "Integrated13fee7ec9; see committed multiclock bounded antecedent validation evidence linked in CAMPAIGN."

l88_index_identity_refinement: "Coordinator confirmed both editions silently accept ordering at64'h100000000 by narrowing it to element0; evidence l88-wide-index.json. Authorize a narrow shared delem bounds guard before unsigned VarRef/ElemVar conversion in vvp/vvp_z3.cc, preserving exact index identity and rejecting unsupported indices before sampling. Add wide-index and negative-index rollback controls. This is necessary L88 boundary correctness, not a separate counted fix."

l88_focused_evidence: docs/conformance/session_logs/2026-09-15_joint_dynamic_element_ordering_validation.json

l88_integration_revision: d6a15ca2b

l86_latest_failures: "Historical failures resolved in13fee7ec9; original failed reducers preserved in CAMPAIGN-linked evidence."

l89_integration_revision: 1ffe476b6

l86_integration_revision: 13fee7ec9
