blocker: CI-WIN-UVM-REGEX-NOOUTPUT
status: FOCUSED_TESTED
base: 1af223c8f45b922dc1b966aa5e399c4f59f8546f
predecessor: OT-SPI-INLINE-CALLER-QUEUE-FOREACH merged as PR320 (1af223c8); PR319 merged 288132f2. Both frozen.
objective: Remove the Windows (MSYS2 libsystre/TRE) crash of the strict-regex UVM DPI test without changing regex semantics or the pinned uvm-core sources.
authorization: Continuous campaign coordinator selection; main CI red on all three MSYS2 jobs since PR317 with this single test failing.
standards:
- Pinned uvm-core src/dpi/uvm_regex.cc API contract (UVM_REGEX_MAX_LENGTH 2048; null on compile failure); IEEE 1800.2 clause mapping unassessed.
- POSIX regcomp/regfree: regfree is defined only for a successfully compiled regex_t; REG_NOSUB affects only submatch reporting.
reproducer: tests/uvm_regex_strict_exec_test.sv (Windows CI job logs, main 288132f2 run 35691405507); local TRE 0.8.0 proxy under scratch evidence.
red: MSYS2 MINGW64/UCRT64/CLANG64 report "vvp produced no output at all"; Linux/macOS pass (glibc/libc tolerate regfree after failure).
expected: 2048-character patterns compile; >2048 and invalid patterns return null with an error; no crash; glob/strict behavior unchanged.
root: Vendored uvm_re_comp calls uvm_re_free -> regfree on a regex_t whose regcomp failed (POSIX-undefined). TRE leaves a dangling tnfa pointer and segfaults. TRE's submatch-tracking parser also rejects 2048-literal patterns (REG_ESPACE; limit 1463), which triggers that path.
allowed_files:
- uvm_dpi/uvm_dpi_iverilog.cc regcomp/regfree wrapper around the vendored uvm_regex.cc include
- .github/uvm_test.sh vvp exit-status reporting (recorded next discriminator for this blocker)
- owning blocker/debt/evidence/campaign records
forbidden:
- uvm-core or third_party UVM source edits
- Changing strict-vs-glob semantics, length limits, or test expectations
- Marking the test known-fail or skipping it
acceptance:
- Local TRE-linked umbrella reproduces the crash before and passes after; glibc build unchanged.
- uvm_regex_strict_exec_test and uvm_regex_legacy_glob_test pass locally (real DPI).
- Mapped UVM regex/config groups plus smoke pass locally; Windows CI UVM step passes on the PR.
risk: LOW-MEDIUM (fork-owned DPI shim; affects every regex compile through uvm_re_comp).
stop_after_completion: Freeze validated patch and return to coordinator selection; full mission remains active.
latest_validation:
  evidence: docs/conformance/session_logs/2026-09-22_win_regex_tre_fix.json
  completed: TRE red/green and ablation; regex pair 2/2; full real-DPI UVM 358/358; UVM front-end all scenarios. Linux container host.
  pending: MSYS2 CI UVM regression step on the draft PR.
