id: "DD046-TRIAGE-2"
title: "Comprehensive re-triage of the DD046 curated candidate table"
status: "closed"
phase: "done"
base_revision: "b8c5bfe6b"
last_validated_revision: "HEAD (post-retriage commits)"
parent: "DD046 user-authorized sweep of all fallback/compile-progress paths"
reproducer: "See evidence/campaign-20260908/dd046/c14_incdec.sv through c21_nested_foreach3.sv for the concrete reducers used; 9 of 12 curated-table rows were reducer-verified, 3 settled by tracing callers/overrides (Unresolved methods -- deliberate/justified, Simulator/VPI test_value_callback_ready -- legitimate virtual default with real overrides, Missing executable targets -- defensive bookkeeping)."
expected_behavior: "Every DD046 curated-table row classified per its own completion rule: valid behavior, unreachable/superseded, explicit unsupported/qualification gap, or a reproduced semantic defect."
root_cause: "N/A -- no new defect found this pass. All twelve curated-table rows re-examined against current source; eleven are RESOLVED or ALREADY-LOUD (fixed by earlier sessions since the table was written, or never actually silent per DD046's own definition). Full findings per-row in docs/conformance/DISCOVERED_DEBT.md's DD046 section."
allowed_scope: ["Documentation/triage only this pass -- no source changes, since REPRODUCED never succeeded for any row (every reducer either showed correct behavior or hit an earlier loud diagnostic)."]
non_goals: ["No fix attempted without a reproducer, per AGENTS.md's targeted-fix protocol.", "Did not force-fit a fix onto any of the 11 resolved rows to manufacture 'progress' -- the honest finding IS the progress (closes 11 open questions DD046's completion rule required answered)."]
validation_contract: "N/A this pass -- no code changed, so no gate sweep required. Documentation-only commits."
discovery_policy: "Unrelated discoveries record-only in DISCOVERED_DEBT.md (done: elab_expr.cc's sorry: on find() over fixed non-integral arrays, noted but not opened as its own blocker since it's loud)."
evidence: "../evidence/campaign-20260908/dd046/"
next_action: "The ONE remaining open DD046 row is 'Lost expression typing' (netmisc.cc's elab_and_eval, silent empty-string/0.0-real substitution for unresolved parameterized/generic method argument typing). This is qualitatively different from everything else checked this pass: it requires tracing which caller of elab_and_eval can reach the fallback with cast_type in {STRING,REAL} and tmp->expr_type() unresolved/NO_TYPE -- likely something in parameterized-class method-argument binding -- before a reducer can even be attempted, not a quick guess-and-check. Needs its own focused investigation pass. ALTERNATIVELY: the 12-candidate curated table is now essentially exhausted (11/12 resolved); a genuinely fresh DD046 increment likely requires mining the raw 12849-line evidence/campaign-20260908/fallback-tracked-candidates.tsv for NEW candidates the curated table never captured, OR shifting to the user's third priority phase (broader implementation work) since DD046's bounded, quickly-reachable candidates are exhausted for now. The real-typed (IVL_VT_REAL) nested class-property VPI-lvalue gap (L33/L34's named follow-on) remains available too, but was already sized as needing a new runtime class + grammar token (bigger than L33/L34's reuse pattern)."
next_command: "Either: (a) grep -c '' evidence/campaign-20260908/fallback-tracked-candidates.tsv and sample fresh, uncurated lines; or (b) grep -n 'elab_and_eval' elab_expr.cc | head -30 to find callers that could pass an unresolved generic type; or (c) ask the user which of the three next-step options above to pursue."
stop_after_completion: false
