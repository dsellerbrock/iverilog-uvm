id: OT-SPI-COMPOUND-EVENT-LIST
status: REGRESSION_TESTED_LOCAL_CHECKPOINT
baseline: a52e3f677f09f72632a88c758d13784d187a70cc
baseline_dependency: PR317 locally qualified and published; remote CI pending. Parent branch preserved, no new worktree.
authority: Coordinator selection under continuous campaign after prior source was frozen, all local gates passed and PR317 published.
objective: Correct atomic value-change delivery for SPI host integral class-property event lists, including nested receivers.
clauses:
- IEEE1800-2017 9.4.2
- IEEE1800-2023 9.4.2
reproducer: evidence/review-20260920/spi-event-list-next/event_list.sv
red_evidence: evidence/review-20260920/spi-event-list-next/implementation/baseline.json
expected: Either listed expression changing wakes once; equal values stay waiting; rebind refreshes dependencies; arming is synchronous in original process.
root_cause: prepare_event_list_ classifies nested NetEProperty as direct, but actual precise-key check rejects owner_expr and multi-leaf observer. Forked alternative cannot establish atomic source arming.
design: Reuse one integral NetEConcat observer and existing cached-expression adapter for eligible pure ANYEDGE leaves, including ordinary/static/constant leaves when at least one class dependency is present. No runtime opcode or fork observer.
allowed_files:
- elaborate.cc
- private evidence/review-20260920/spi-event-list-next/
- owning regression files and documentation after coordinator review
forbidden:
- No application source changes, assertion suppression, queue-type relaxation, new interpreter or runtime ABI.
- No opportunistic changes to null-call diagnostic debt or other blockers.
- No acceptance of method/function or system-call list leaves without evaluation-order and per-leaf dependency proof.
- No concat routing for edges,iff,named events,VIF,real,string,class-handle or zero-width leaves; preserve existing paths/diagnostics.
lanes:
  implementation:
    owner: canonical_graph_refresh Terra/medium
    files: elaborate.cc and private implementation notes only
    task: Smallest complete reuse of existing observer and cached-expression lowering; report source scope before widening.
  semantic_tests:
    owner: vif_proxy_implementation Sol/medium
    files: private spi-event-list-next/tests only
    task: Paired positive,negative,boundary,atomic arming and lifetime regressions, using retained event-recipe context.
coordinator: Selection,standards,shared builds/install,harnesses,permanent registration,integration,review and PR/CI.
acceptance:
- Original comma and equivalent or list wake on each leaf.
- Equal-value rebind refreshes owner; old/unselected member changes do not wake.
- Four-state changes including X/Z and mixed packed widths preserve equality semantics.
- Concurrent automatic contexts,cancellation and queued Active/Reactive writer arming remain correct.
- Unsupported/boundary event families preserve prior behavior and focused diagnostics.
- Fresh unmodified pinned SPI host compile records next exact blocker; compile alone is not DV pass.
validation: Focused tests per fix; broader gates at coherent batch or when new runtime/architecture risk requires them. Prior baseline full local gates pass.
preservation: implementation/pre-selection.patch preserves local publication notes and external AGENTS87-line patch. PR317 compiler patch committed; no running local build/test processes at selection.
next: Terra implements in elaborate.cc; Sol builds isolated tests. Coordinator reviews before serial build/install.

scope_refinement:
  reason: Parent PR317 qualifies combined ordinary and class dependencies in one observer. For pure integral list leaves, concatenation changes iff any leaf changes regardless of storage class.
  decision: Allow mixed ordinary/static/constant leaves with at least one class dependency; remove unnecessary class-only Nexus filtering. Ordinary-only lists retain established path.
  acceptance: Paired mixed static and automatic-local lists,constant leaf,each leaf wake and equal-value suppression. No function/system-call or edge/iff/VIF expansion.

build_checkpoint:
  session: 22103
  command: python3 evidence/review-20260920/spi-event-list-next/build_candidate.py build1
  source: elaborate.cc only,+105/-1; coordinator reviewed closed pure-expression predicate,ownership transfer,width guard and direct single-observer reuse.
  lanes: Terra source frozen; Sol prepares private tests without tool execution during installation.
  next: Poll22103, then original paired reducer,focused tests and fresh SPI application compile; no concurrent builds.

focused_checkpoint:
  build: build1 passed with stable source and installed hashes
  original: Both editions pass original comma-list reducer.
  adjacent: Existing legacy117/117 and JSON117/117 passed.
  failure: core_prequeued_regions reports wakes1 instead of2 in both editions; Sol traces first incorrect observation in private probe.
  boundaries: Edge-list abort and real-list runtime failure match baseline and are NOT functional passes.
  application: Original event-list diagnostic removed; two queue element-type errors and exit139 remain.
  next: Resolve focused writer failure before permanent registration or qualification claim.

focused_resolution:
  root: Initial prequeued fixture excluded its standalone program under selected module top. Instantiating the program restores intended stimulus; no compiler change.
  result: Corrected16 core paired semantic cases pass;12 boundary comparisons retain baseline,including2 edge aborts and2 real failures not counted as implementation passes.
  source: build1 remains frozen and adjacent117/117 legacy+JSON passed.
  lane: Sol now owns permanent sv_class_event_list source/gold/JSON plus focused/main registrations; coordinator alone runs shared harness.
  next: Register corrected core cases and genuine-null diagnostic negative,run both permanent harnesses,record scoped evidence and retained failures.

checkpoint_20260921_permanent:
  permanent: 16 normal tests passed in both legacy and JSON harnesses.
  negative_lane: vif_proxy_implementation Sol/medium adds paired invalid-member CE tests; permanent test files and manifests only.
  assessment_lane: canonical_graph_refresh Terra/medium reduces SPI NetESelect synthesis crash read-only; private crash-assessment files only.
  coordinator_gate: Mapped scheduler/classes/uvm_seq_tlm plus deduplicated smoke UVM running; shared build unchanged.

uvm_checkpoint:
  result: 76/76 real-DPI tests pass; no failures/skips.
  risk: MEDIUM local elaboration routing; full gates batched per user cadence.
  review: Strengthen rebind negative check with live waiter; paired invalid-member negatives pending.

final_local_checkpoint:
  permanent: 18/18 legacy and 18/18 JSON, including paired invalid-member negatives and corrected live-waiter regression.
  evidence: docs/conformance/session_logs/2026-09-21_spi_class_event_list_focus.json
  disposition: Freeze this patch; broad suites remain at batch checkpoint; next implementation requires coordinator selection after bounded crash assessment.
