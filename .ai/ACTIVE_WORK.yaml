id: "DD046-TRIAGE-1"
title: "Select and qualify one requirement cluster from the DD046 fallback inventory"
status: "active"
phase: "candidate_investigation"
base_revision: "431b756e6"
last_validated_revision: "90e95c339"
parent: "DD046 user-authorized sweep of all fallback/compile-progress paths"
reproducer: "None written yet. Candidate: $value$plusargs (or similar VPI-lvalue write) targeting a class property of string type silently drops the value (vvp/vpi_cobject.cc:341-364, case vpiStringVal); a one-shot warning prints but the plusarg's own matched/not-matched status is unaffected."
expected_behavior: "Not yet finalized: either the write reaches the class's inner string field losslessly, or the destination is rejected loudly rather than silently accepted-but-dropped."
root_cause: "Per the existing in-code comment: the compiled program hands VPI a generic per-object vpiClassVar handle for this destination, which has no way to address a single inner string field. The actual construction site (elaboration and/or tgt-vvp codegen for a class-member-string VPI-lvalue argument) is not yet located -- vvp/vpi_tasks.cc and vvp/compile.cc were checked and are not it; tgt-vvp/draw_vpi.c and elab_expr.cc are the leading unexamined candidates."
allowed_scope: ["Trace tgt-vvp/draw_vpi.c and elab_expr.cc for class-member-string VPI-lvalue argument construction; write a minimal $value$plusargs + class-string-property reducer; confirm reachability and exact behavior before proposing any fix."]
non_goals: ["No fix without a reducer. No folding in DD049 (vpiLineNo) -- confirmed architecture-wide in this same triage pass, recorded separately, too large for one increment."]
validation_contract: "Once root-caused: minimal fix, permanent regression, independent review, full required gate sweep (integrated/JSON/UVM/NFA/releases/frontend-last)."
discovery_policy: "Unrelated discoveries record-only in DISCOVERED_DEBT.md."
evidence: "../evidence/campaign-20260908/dd046/"
next_action: "Read tgt-vvp/draw_vpi.c for how a class-member string used as a VPI-lvalue systf argument gets its handle emitted; cross-reference elab_expr.cc for how that argument is elaborated. Write the reducer once the construction site is found."
next_command: "grep -n \"class\|vpiClassVar\|member\|property\" tgt-vvp/draw_vpi.c | head -60"
stop_after_completion: false
