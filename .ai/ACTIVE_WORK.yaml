id: U04
title: Qualify DPI regex loading for original UVM2020.1 releases
status: in_progress
phase: reproducing
base_revision: 47e6c87b3f420776e915cc969fe29ac03d8ac827
last_validated_revision: 47e6c87b3f420776e915cc969fe29ac03d8ac827
selection_reason: Original2020.1.0/1.1 compile after L08 but missing DPI regex symbols prevent meaningful runtime smoke; this directly advances user-requested historical UVM support.
reproducer: third_party/uvm-releases/results-gizcr5j0/results.json and both original release runtime.log files; complete and baseline_valid true.
expected_behavior: Original pinned release regex imports resolve with correct signatures, matching and nonmatching cases execute correctly, and command-line parsing is not corrupted by unavailable DPI calls.
observed_behavior: Missing uvm_re_match and uvm_glob_to_re symbols precede five UVM command-line errors and BUILDERR; no runtime pass despite exit0.
standards:
  - Establish applicable IEEE DPI import and exact official release declarations before any semantic or ABI patch; do not infer equivalence from symbol spelling.
allowed_scope:
  - Read-only pinned release DPI declarations and runtime source.
  - Trace installed UVM DPI loading, driver selection and existing native wrappers.
  - Evidence reducers and active contract, campaign, blocker and session records.
  - Refresh exact implementation scope only after causal mechanism and ABI expectations are established.
non_goals:
  - No original library modifications, regex substitution shortcuts, ignored missing imports, warning waivers or release-wide qualification by compilation alone.
  - No unrelated compiler/runtime repairs during this blocker.
invariants:
  - One active blocker; L08 all required local gates passed before selection.
  - Native ARM64, serial builds,300second CPU guard, no RSS cap; preserve unrelated worktrees and merge permissions.
required_gates:
  - Reproduce exact unresolved imports and compare original declaration signatures to loaded exports.
  - Verify matching and nonmatching regex semantics plus glob conversion with permanent coverage for any code change.
  - Run required focused and integrated gates and independent review before closure.
completion: Close only the exact validated DPI loading or ABI subcase; historical releases and application qualification remain separate.
stop_after_completion: false
next_action: Finish graph query, trace original declarations and installed exports, establish smallest causal reducer before implementation.
next_command: 'rg -n "uvm_re_match|uvm_glob_to_re|uvm_dpi" driver/main.c vvp/dpi.cc uvm-core/src/dpi'
