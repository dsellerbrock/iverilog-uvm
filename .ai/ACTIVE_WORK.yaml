id: CLASS-ARRAY-PACKED-SELECT-INCREMENT
title: Preserve fixed-array class-property word identity through packed updates
status: FOCUSED_TESTED
base_revision: 813fafadf
standards:
  ieee1800_2017: "7.4.6, 11.4.1, 11.4.2, 11.5.1"
  ieee1800_2023: "7.4.5, 11.4.1, 11.4.2, 11.5.1"
reproducer: evidence/class-array-packed-incdec-l70/abi/receiver-word-base.sv
current_behavior: "Packed ++/-- over fixed-array class-property elements is rejected at target generation in both editions."
expected_behavior: "Capture receiver, word and base once; preserve current neighbors, selected width, result context and independent invalid word/base suppression."
root_cause: "Selected-property lowering rejects all words; scalar partial-property stores address slot zero and stack merge is signed-only."
allowed_scope:
  - Extend packed-property expression lowering to canonical fixed-array words
  - Reuse fixed-slot index capture, typed full-element invalid reads and selected arithmetic
  - Add indexed signed/unsigned partial-property stores sharing existing runtime RMW implementation
  - Paired focused positive, boundary, negative, permanent and affected-neighbor tests
forbidden_scope:
  - Dynamic-array, queue, associative or nested inner-carrier redesign
  - Stale whole-word writeback, repeated selectors or unsigned-offset truncation
  - Null-default conformance claims
validation_batch: 20260914_after_l64
focused_features_completed_in_batch: 6
pending_gates: "L70 focused and neighboring validation passed; broad batch gate at approximately ten-feature cadence."
prerequisites: "L69 focused checkpoint preserved; L64 latest broad qualification."
next_action: "Preserve focused checkpoint and return to coordinator selection; broad batch qualification remains pending."
stop_after_completion: false
evidence: docs/conformance/session_logs/2026-09-14_class_array_packed_increment.md
