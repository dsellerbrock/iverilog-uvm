blocker: VVP-EXTENDED-ARGS-GLIBC-PERMUTE
status: REGRESSION_TESTED
base: da6dab52 (claude/iverilog-uvm-takeover-n1v3u6)
predecessors: CI-WIN-UVM-REGEX-NOOUTPUT in PR321 (Ubuntu/UCRT64 CI pass, other jobs pending); OT-SPI-INLINE-CALLER-OBJECT-METHOD local commit da6dab52, REGRESSION_TESTED, unpublished (docs/conformance/session_logs/2026-09-22_inline_caller_object_method.json).
objective: Restore documented vvp command-line semantics on glibc. Options precede the design file; every argument after it is an extended argument. Keep the fork's support for +plusargs placed before the file (dvsim ordering).
authorization: Continuous campaign coordinator selection; Verilog/Icarus compatibility preservation (AGENTS mission).
authority:
- Documentation/usage/vvp_flags.rst "Extended Arguments" (options before the input file; extended arguments after it) and vvp/vvp.man.in EXTENDED ARGUMENTS.
reproducer: ivtest JSON br_gh710a, br_gh710b, br_gh710c, dumpfile, sdf_header; `vvp x.vvp -vcd -dumpfile=foo` -> "vvp: invalid option -- 'c'".
root: vvp/main.cc getopt optstring lacks upstream's leading '+', so glibc permutes and parses post-file extended arguments as vvp options (commit b6cb9eea). macOS getopt does not permute.
allowed_files:
- vvp/main.cc option parsing and argv layout
- ivtest/test registration for a direct plusarg-before-file regression if not already covered
- owning docs/records
forbidden:
- Changing extended-argument semantics, plusarg visibility or option meanings
- Test gold edits
acceptance:
- Five JSON tests pass on Linux; plusargs before and after the file remain visible in order; options interleaved with leading plusargs still apply.
- Full JSON, legacy ivtest, VPI, UVM regressions pass.
risk: LOW-MEDIUM (runtime command-line only).
stop_after_completion: Freeze and return to coordinator selection.
