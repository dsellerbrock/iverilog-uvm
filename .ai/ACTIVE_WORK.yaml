id: L02
title: Restore automatic block storage on each entry
status: awaiting_validation
phase: integrated_validation
base_revision: e0dab7221
parent: U01
standards:
  - IEEE 1800-2017 and IEEE 1800-2023 6.21 and 6.8, automatic block lifetime and default initialization.
expected_behavior: Each entry to an automatic block has fresh local storage and default values; static variables retain their values and detached captures retain their activation.
observed_behavior: Class-task loop local bit done is 1 on its second entry, despite having no initializer. Both editions fail at time 1.
reproducer: ../evidence/campaign-20260908/u01-loop/reentry.sv
causal_mechanism: PBlock scope collapsing shares local storage with the enclosing task; explicit initializers run per entry but default initialization occurs only on task allocation.
allowed_scope:
  - elab_scope.cc automatic block frame selection
  - vvp/event.cc and event.h scalar event routing/history across nested automatic frames (automatic_events2 and event-history regressions)
  - ivtest permanent block reentry and lifetime controls
  - .ai/ACTIVE_WORK.yaml
  - .ai/CAMPAIGN.yaml
  - docs/conformance/BLOCKERS.md
  - docs/conformance/DISCOVERED_DEBT.md
  - docs/conformance/session_logs/2026-09-08_continuous_campaign.md
  - docs/conformance/matrices/ieee1800_2017_clause_matrix.md
non_goals:
  - No OpenTitan source edits or weakening of checks or traffic.
  - No unrelated scheduler or randomization redesign.
invariants:
  - Preserve static overrides, per-entry explicit initialization, reference identity, delayed captures, recursion and event context ownership.
  - Reuse existing allocation/free mechanism; no application special cases.
required_gates:
  - Paired edition failing reducer and permanent default-value/type/lifetime controls.
  - Focused lifetime/event/fork suites, integrated ivtest/VPI/negative/runtime, full JSON, real-DPI UVM, make check and independent review.
  - Fresh U01 replay after validated semantic checkpoint.
  - Remote CI before merge.
completion: Bounded automatic block reentry scope only; U01 requires checked normal application completion separately.
discovery_policy: Unrelated discoveries are record-only.
stop_after_completion: false
implementation_state: Frame selection and scalar ancestor-history repair pass focused tests; full integrated gates pending.
validation: Final coherent focus 93 legacy / 50 JSON and make check pass; independent review has no actionable finding.
