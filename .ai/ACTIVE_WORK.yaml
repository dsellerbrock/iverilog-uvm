id: Z01A
title: Exact bounded integral stages in the joint solver
status: completed_local
phase: checkpoint
base_revision: b9de0be7f
parent: Z01
standards:
  - IEEE 1800-2017 18.5.9 global constraints and 18.5.10 ordering (verified local LRM)
  - IEEE 1800-2023 18.5.8 global constraints and 18.5.9 ordering (verified local LRM)
expected_behavior: Legal integral solve-before directives retain the joint hard solution space and sample each stage uniformly over its distinct feasible projections; unordered/partially ordered values are deferred as late as possible.
observed_behavior: exact_joint currently rejects every nonempty order_pairs vector, even root-local scalar ordering in a coupled parent/child graph.
hypothesis: The joint component tuple sampler lacks rank-local projection sampling; reusing the non-joint Optimize diversity path would not prove ordered probabilities.
reproducer: evidence/campaign-20260908/z01a/reducer.sv (reproduced in both editions)
allowed_scope:
  - vvp/vvp_z3.cc (bounded joint ordering and tuple sampling)
  - ivtest (ordered joint regression and now-stale unsupported expectation only)
  - .ai/ACTIVE_WORK.yaml
  - .ai/CAMPAIGN.yaml
  - docs/conformance/BLOCKERS.md
  - docs/conformance/DISCOVERED_DEBT.md
  - docs/conformance/matrices/ieee1800_2017_clause_matrix.md
  - docs/conformance/session_logs/2026-09-08_continuous_campaign.md
non_goals:
  - Non-joint solver repair or architectural redesign.
  - Ordered joint dist/active randc, real variables, dynamic-size/element ordering, or larger-than-cap components.
  - Frontend expansion of new hierarchical ordering spellings.
invariants:
  - Keep complete component feasibility proofs before joint tuple-sampling RNG draws; no cap increase or prefix sampling.
  - Preserve hard/soft constraints, canonical identity, graph rollback, callbacks, rand_mode and deterministic joint RNG ownership/replay.
  - Rank partially ordered variables as late as allowed; keep broader Z01 open.
required_gates:
  - Both-edition reducer red then permanent small-domain probability, replay, alias/activation/callback and rollback tests.
  - Negative unsupported shapes and cycle checks; focused legacy and JSON tests.
  - Integrated ivtest/VPI/negative/runtime, full JSON, real-DPI UVM, make check, independent review.
  - Remote CI before merge.
dependencies: []
completion: All required local gates passed; bounded canonical integral stages resolved, parent Z01 remains partial, remote CI required before merge.
discovery_policy: Unrelated findings are record-only; proven prerequisites require a preserved-work coordinator boundary.
stop_after_completion: false
