blocker: DPI-EXPORT-DROPPED-EXIT-STATUS
status: REGRESSION_TESTED
base: b8f0e2a2 (claude/iverilog-uvm-takeover-n1v3u6; PR322 open)
predecessors: SOLVE-BEFORE-FIXED-ARRAY REGRESSION_TESTED (docs/conformance/session_logs/2026-09-22_solve_before_fixed_array.json); OT-SPI-INLINE-CALLER-OBJECT-METHOD and VVP-EXTENDED-ARGS-GLIBC-PERMUTE REGRESSION_TESTED; all in PR322 awaiting CI.
objective: An export "DPI-C" whose signature cannot be supported is dropped (C callers cannot link); that semantic loss must fail code generation instead of exiting 0.
authority: Manifesto/AGENTS no-silent-drop policy; tests/negative/m10_dpi_export_{class_handle,open_array}_argument.sv already require rejection.
root: tgt-vvp/vvp_scope.c emits the sorry from dpi_export_build_sig(..., quiet=0) but the caller only `continue`s without counting vvp_errors. The negative suite passed in CI only because non-root users cannot write /dev/null.dpiexport.c.
fix: Count the dropped export in vvp_errors (same as the adjacent C-name ABI conflict).
validation:
  focused: negative suite 148/148 as root (was 146/2); UVM dpi group 39/39 real DPI.
  integrated: ivtest gate exit 0 (legacy 6341 0 failed, VPI 126, negative 148/148 as root); full JSON 3273 0 failed.
  pending: PR322 CI.
stop_after_completion: Freeze and return to coordinator selection.
