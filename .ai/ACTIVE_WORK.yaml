id: V02
title: Honor default instance-averaged type coverage
status: in_progress
phase: root_cause_trace
base_revision: 6eee0480c
standards:
  - IEEE 1800-2017 and IEEE 1800-2023 19.11.3 and type-option table19-3.
expected_behavior: Default merge_instances=0 computes the weighted average of instance coverage; two half-covered equal-weight instances return50 percent even when their union is fully hit.
observed_behavior: Both editions return100 percent; merge_instances is accepted but current type_coverage always uses merged counters.
reproducer: ../evidence/campaign-20260908/v02/average.sv
causal_hypothesis: Type-option mode is not represented in runtime metadata, and the type query computes only the merged item-counter model.
allowed_scope:
  - Trace covergroup option lowering/metadata in elaborate.cc, netclass.h, t-dll-api.cc, ivl_target.h and tgt-vvp/draw_class.c only as actually needed; identify exact existing path before edits.
  - vvp class_type, vthread and vvp_cobject coverage query/options/instance lifecycle; parse/compile metadata only if required by traced path.
  - Paired ivtest type-coverage option regressions and corrections to historical test expectations demonstrated inconsistent with the LRM.
  - .ai/ACTIVE_WORK.yaml
  - .ai/CAMPAIGN.yaml
  - docs/conformance/BLOCKERS.md
  - docs/conformance/session_logs/2026-09-08_continuous_campaign.md
  - docs/conformance/matrices/ieee1800_2017_clause_matrix.md
non_goals:
  - No arbitrary coverage redesign, unrelated goal/strobe/cross options, or application changes.
  - No claim that V01 parent or complete functional coverage is qualified.
invariants:
  - Preserve explicit merged mode, option.weight semantics, zero-weight handling, unsampled and retired instance accounting.
  - Reuse existing instance coverage computation and registration where possible.
  - A discovered prerequisite requires preserved reducer/work and separate coordination before implementation.
required_gates:
  - Paired edition red/green default and explicit mode tests; unequal instance weights, empty/unsampled/lifetime and merged controls.
  - Applicable IEEE semantics and causal source trace; independent review.
  - Focused coverage, required integrated ivtest/VPI/negative/runtime, full JSON, real-DPI UVM and make check.
  - Remote CI before merge.
completion: Only exact supported default/explicit instance averaging scope with reviewed evidence; no closure while required gates remain pending.
discovery_policy: Unrelated discoveries are record-only.
stop_after_completion: false
