id: "U09"
title: "Legacy UVM cached-regex DPI entry points"
status: "in_progress"
phase: "awaiting_validation"
base_revision: "c9626a449"
last_validated_revision: "c9626a449"
selection_reason: "User requested legacy DPI; DD030/DD027 identify missing C exports in original1.2/1.1d. Pinned source already acquired; U08 fully validated before selection."
reproducer: "results-eb3ghca7 original1.2/1.1d runtime logs: missing uvm_dpi_regcomp, command-line regex errors and BUILDERR fatal. nm confirms regcomp/regexec/regfree exports absent."
expected_behavior: "Expose uvm_dpi_regcomp/regexec/regfree with the UVM1.2 cached POSIX ERE contract: compile REG_NOSUB|REG_EXTENDED without glob retry, reusable opaque handles, match/nonmatch polarity, null handling, release allocated regex, report invalid compilation and return null."
standards: ["Pinned Accellera UVM1.2 src/dpi/uvm_svcmd_dpi.c and .svh are legacy API authority.", "IEEE1800-2017/2023 clause35 DPI-C return/argument type mapping; chandle corresponds to an opaque C pointer. This is not full IEEE1800.2 qualification."]
root_cause: "Fork umbrella includes modern uvm_svcmd_dpi.c, whose regex adapters moved to SV; old libraries still import their C symbols. Existing uvm_re_match/glob exports do not satisfy the cached-regex ABI."
allowed_scope: ["Add the three missing C entry points to fork-owned uvm_dpi/uvm_dpi_iverilog.cc, reusing native POSIX regex and existing reporting helper.", "Permanent legacy cached-regex fixture, frontend registration, focused original-library ABI and smoke replay, directly affected documentation."]
non_goals: ["No changes to pinned library source, compiler semantics, modern regex/glob behavior or general visibility debt.", "No UVM1.2 application pass claim from smoke alone.", "UVM1.1d valid-pattern compatibility is tested; its historical print-only invalid-pattern diagnostics are not claimed identical to UVM1.2 error reporting."]
required_gates: ["Pre-fix missing-export reducer; both-edition standalone fixture positive/negative/handle-lifetime/error-count checks; independent review.", "Native build/export check; full integrated legacy/VPI/negative/runtime,JSON,real-DPIUVM,NFA,makecheck,full15release sweep; isolatedfrontend last and restored hashes."]
discovery_policy: "Unrelated discoveries record-only. Regression caused by this patch remains in scope."
next_action: "Write and run cached-regex red fixture before adding exports."
next_command: "nm -gU local-install/lib/ivl/uvm_dpi.vpi"
stop_after_completion: false

red_evidence: "evidence/u09/red.json: both2017/2023 compile0 then run1; missing uvm_dpi_regcomp and fatal legacy regex compilation. PrimaryDPI mapping is AnnexH scalar mapping (chandle void*) plus35.5.5/35.5.6 return/formal types, checked against both local editions."

reporting_refinement: "Original1.1d lacks m__uvm_report_dpi entirely. Both original1.2 ABI modes pass;1.1d invalid-pattern probe reaches missing-export internal error despite valid smoke passing. Extend existing legacy error helper to use the UVM callback when present and a visible native diagnostic when absent; no runtime architecture change. Preserve a permanent no-callback fixture and original1.1d error-path evidence."

focused_validation: "corrected-focused.json10/10: modern cached/uncached, no-callback, original1.1d/1.2 each2017/2023. Dotted callback lookup failed six cases and was corrected to VPI package syntax ::. Final original ABI checks include one native diagnostic for1.1d and real UVM error counts for1.2. Initial smoke replay both1.1d/1.2 passes; final full sweep required."
