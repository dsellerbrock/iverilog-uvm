id: L08
title: Reduce the UVM2020.1 resource-queue compile frontier
status: in_progress
phase: spec_grounded
base_revision: 53b58890c63a48aa1943c7ef065db59f19cebef3
standards:
  - IEEE1800-2017 and2023 6.20.3 requires a type actual and forbids hierarchical names;8.23 permits class scope type names.2023 explicitly permits type-parameter assignment with incomplete/type-parameter prefixes; preserve edition distinctions.
expected_behavior: types::rsrc_q_t is a public class typedef naming queue_type#(resource); passing it as checker_type T must preserve handle identity and stored value73 exactly as the explicit specialization control does.
observed_behavior: Scoped typedef reducer fails exactly one not-a-type diagnostic in both2017/2023 modes; replacing only the type actual by queue_type#(resource) compiles and runs PASSED in both. Fresh original2020.1.0 probe results-x3bxo9km repeats its first diagnostic.
reproducer: scripts/uvm_release_matrix.py probes and third_party/uvm-releases/results-jdbvlt0_/results.json; select only the first causal diagnostic.
selection_reason: User-requested major UVM release qualification remains blocked; a potentially legal type compatibility failure affects reusable verification libraries. Do not treat earlier legacy member-delay syntax as mandatory IEEE syntax without proof.
allowed_scope:
  - Read-only original UVM2020.1 sources and current compiler implementation.
  - Fresh evidence/campaign-20260908/l08 reducers, logs and primary-source notes.
  - .ai/ACTIVE_WORK.yaml and .ai/CAMPAIGN.yaml.
  - docs/conformance/BLOCKERS.md and session log.
non_goals:
  - No compiler source changes until failing reducer, expected semantics and causal scope are established in this contract.
  - No original library edits, broad release fixes, parent-feature closure, or unrelated discoveries beyond record-only.
invariants:
  - One active blocker; U01 preserved under u01-after-p04, B01 remote gate remains separate.
  - Native ARM64,300second per-process CPU guard, no RSS cap; preserve unrelated worktrees.
required_gates:
  - Confirm first failure on current validated compiler and original pinned source.
  - Paired edition reducer and applicable primary LRM semantics before implementation.
  - Any semantic patch requires permanent regressions and all required focused/integrated gates and review before closure.
completion: Close only the exact validated causal subcase; release-wide and application qualification remain separate.
last_validated_revision: 53b58890c63a48aa1943c7ef065db59f19cebef3
discovery_policy: Unrelated findings record-only.
stop_after_completion: false
next_action: "Finalize bounded type-only scoped lookup using existing class resolution; refresh allowed implementation scope before edits. Preserve parsed :: provenance, specialization identity and exact member shadowing; reject value/property/indexed/hierarchical actuals."
next_command: "sed -n '2948,3052p' elab_expr.cc"
root_cause: "Parser deliberately resolves ambiguous TYPE::member as PEIdent; NetScope::evaluate_type_parameter_ only resolves single unqualified PEIdent names. has_scoped_type_prefix distinguishes :: from dotted value paths. Existing scoped class resolvers preserve specialization/typedef provenance, but ordinary find_typedef may escape to enclosing scopes and needs an owner boundary for selected members."
review_note: Read-only design review recommends elaboration-time type-only resolution, no grammar precedence change and no value-type inference. Include explicit value/property/hierarchy negatives and specialization/shadowing controls.
evidence:
  - ../evidence/campaign-20260908/l08/scoped_type_argument.sv and scoped-type-2017.log/scoped-type-2023.log.
  - ../evidence/campaign-20260908/l08/explicit_type_control.sv and explicit-control-2017.log/explicit-control-2023.log.
  - third_party/uvm-releases/results-x3bxo9km/results.json original pinned release compile failure, complete and baseline_valid to verify on closure.
