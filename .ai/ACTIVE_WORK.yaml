id: PARALLEL-AFTER-L111
title: Finite first-match implementation and nested indexed constraint assessment
status: IMPLEMENTING
base_revision: 05b0c44e1
qualified_semantic_base: 366ef2bf1
authority: User authorizes two existing cheaper implementation agents, disjoint compiler mechanisms and focused validation, with seven broad gates near ten integrated fixes.
coordinator_owns: [Shared builds, Installation, Shared harnesses and outputs, Main manifests, Documentation, Integration and review]
lanes:
  - id: L106-CONSTANT-LOCAL-ARRAY-PATTERN
    agent: /root/constraint_abi
    status: FOCUSED_TESTED_BROAD_PENDING
    standards: IEEE1800-2017 and2023 10.9,13.4.3
    reproducer: evidence/batch-20260915-after-l95/next-const-string-array-initializer-assessment/assessment.md
    actual: Both editions crash during constant evaluation of mutable, const and static-function local fixed-string-array aggregate initializers; runtime and separate element assignments pass.
    expected: Evaluate each compatible local fixed-array assignment-pattern leaf into the correct local slot, preserving array shape, type conversions and frame lifetime.
    root_cause: NetAssign::eval_func_lval_ dereferences a missing word selector when a whole-array NetEArrayPattern assignment reaches the local array path.
    owns: [net_func_eval.cc]
    test_prefix: sv_const_local_array_pattern
    dependencies: Existing pattern flattening, LocalVar array storage and slot conversion; no assertion-lane dependency.
    scope: Whole local fixed-array assignment patterns through the existing evaluator; cover declaration initialization and equivalent whole-array assignment where supported. No dynamic/queue representation, parser or target edits; report broader requirements before editing.
    acceptance: Paired constant/runtime string and integral arrays, exact element order and nonzero/ascending/descending bounds, signed/width conversions, repeated automatic/recursive frames, const initializer legality and readonly mutation rejection, positional and already-supported keyed/default pattern boundaries, malformed shape/type negatives, unchanged selected-element stores and L99/L101 neighbors.
  - id: L107-FINITE-FIRST-MATCH-MULTICLOCK-PREFIX
    agent: /root/constraint_wide
    status: FOCUSED_TESTED_BROAD_PENDING
    standards: IEEE1800-2017 and2023 16.9.8,16.13.1,16.13.2
    reproducer: evidence/batch-20260915-after-l95/next-finite-sva-assessment/assessment.md
    actual: Both editions reject first_match(a ##[1:2] b) as a finite first-clock prefix; ordinary ranged-prefix support exists.
    expected: Retain earliest source endpoint; a child failure at35 cannot be rescued by a later source endpoint whose child would pass at55.
    root_cause: Parser fm metadata reaches bounded multiclock NFA admission, which explicitly rejects it; source attempt lacks first-accept cut handling.
    owns: [pform.cc, pform_sva_nfa.cc, pform_sva_nfa.h]
    test_prefix: sv_assert_multiclock_first_match_prefix
    dependencies: Existing finite source NFA, MATCH/CLOSE lifecycle and L103 parent aggregation. Independent of constant evaluation.
    scope: Direct Boolean first_match first-clock chains with one finite delay window, including exact-window boundary; reuse earliest-endpoint source closure without new scheduler/destination machinery. Do not accept broader nested/wrapper/match-item forms without proving preserved cut scope; diagnose unsupported forms explicitly.
    acceptance: Both editions earliest failure versus later would-pass endpoint, earliest success, no endpoint, exact/ranged windows, plain and implication use, ##0/##1 and reversed event order, overlapping attempts, cover once, Off/Kill/async disable/restart, destination backlog, construction/unsupported negatives and preserved ordinary L100/L102/L103 aggregation. Honor all earliest same-tick matches; do not claim Boolean-only merged paths are observably different without proof.
file_conflict_policy: Disjoint source and fixture ownership. No agent commits, shared build/install or shared harnesses. Report source freeze and do not edit again until coordinator releases it.
focused_features_completed_in_batch: 5
integrated_revisions: [415823320, 0a5293059, 3823bd356, 6d346269f, 9027c1938]
last_broad_qualification: docs/conformance/session_logs/2026-09-15_compiler_batch_l96_l105_qualification.json
prior_work: docs/conformance/session_logs/2026-09-15_l96_l105_active_work_archive.yaml
stop_after_completion: false

application_priority_assessment:
  authority: User requests Caliptra SV progress and one real OpenTitan UVM pass; preserve pending compiler work.
  installed_baseline: L108 semantic3823bd356; L106-L108 integrated and focused-tested, broad qualification pending.
  preservation: evidence/application-check-20260915/preserved-l106-l107/provenance.json
  shared_process: None; fresh L108 OpenTitan replay passed. See current application evidence below.
  lanes:
    - agent: /root/constraint_abi
      scope: Assess object-property versus parameterized-class name collision; private cheap reducer and IEEE lookup evidence only, no implementation edits yet.
      evidence: evidence/application-check-20260915/opentitan/result.json
    - agent: /root/constraint_wide
      scope: Assess authoritative Caliptra include and dependency configuration; private evidence only, no upstream edits or shared harness execution.
      evidence: evidence/application-check-20260915/caliptra/representative-include-closure.json
  next_selection: Coordinator reviews reducers and records explicit implementation ownership before source changes.

application_implementation:
  id: L108-IMPLICIT-OBJECT-METHOD-LOOKUP
  status: FOCUSED_TESTED_BROAD_PENDING
  agent: /root/constraint_abi
  standards: IEEE1800-2017 and2023 8.2,8.4,8.11,8.25.1
  reproducer: evidence/application-check-20260915/opentitan/lookup-reducer/ASSESSMENT.md
  actual: Two dotted calls on an implicit-this property are rejected as bare parameterized class scope calls in both editions; explicit-this runtime controls pass.
  expected: Resolve the object property according to lexical/member lookup before unrelated outer class names; preserve actual scope-resolution restrictions.
  owns: [elab_expr.cc, elaborate.cc, Statement.h, parse.y, pform.cc]
  fixture_prefix: sv_implicit_property_method_collision
  scope: Correct PECallFunction width/elaboration and PCallTask void-method receiver dispatch using existing receiver resolution; preserve indexes, arguments, line information and single evaluation. Only PCallTask scoped-prefix provenance in parser/carrier code; no grammar-language broadening, runtime/library/upstream edits.
  dependencies: Existing explicit-this receiver dispatch; no dependency on frozen L106/L107 patches.
  acceptance: Paired runtime void/value methods, null guard, inherited/protected and indexed properties, local/argument shadowing, unit/imported parameterized name collision, explicit-this equivalence; genuine bare generic scope, static context, missing member and access/specialization negatives; focused neighboring class tests and real unmodified OpenTitan smoke replay.
  preservation: L106/L107 remain unchanged; coordinator owns builds/install/shared harnesses/manifests/docs/review/integration.

L108_scope_refinement: Exact void-call diagnostic traced to PCallTask::elaborate_method_ in elaborate.cc14972; elab_expr-only candidates did not fix reducer. Coordinator authorizes paired void/value dispatch fix, preserving all negative scope checks.

L108_provenance_refinement: PCallTask drops dot-versus-scope syntax provenance. L107 is integrated, freeing pform.cc ownership. Authorize minimal bool carrier and propagation through parse construction/copies to preserve genuine scoped-call rejection; audit all constructors/conversions, retain Bison conflict signature.

caliptra_adapter_lane:
  id: APP-CALIPTRA-INCLUDE-OVERLAY
  status: CONFIGURATION_TESTED
  agent: /root/constraint_wide
  evidence: evidence/application-check-20260915/caliptra/provider-overlay-40.md
  root_cause: Generated file lists omit dependency include directories and seven source-demonstrated supplemental provider edges.
  owns: [scripts/caliptra_include_overlay.py, tests/test_caliptra_include_overlay.py]
  scope: Read-only include-directory adapter over pinned Caliptra compile.yml providers and seven documented supplemental edges. Emit ordered directories and provenance JSON for requested jobs; no source-file reorder, upstream edits, simulator invocation or guessed parameter overrides.
  dependencies: Existing native Python3.13 venv with PyYAML6.0.3; no new dependency installation. Independent of L108 compiler source.
  acceptance: Deterministic dependency-first paths, strict duplicate/missing-provider and cycle diagnostics, environment/COMPILE_ROOT expansion and existing directories, exactly40 known roots covered, normal reg headers exclude reg_ss. Coordinator replays both assertion modes and retains all later failures.
  counting: Application build configuration only; never counted as compiler semantic feature or runtime pass.

caliptra_runtime_assessment: Unmodified power2round_tb fails checked verdict with zero public-key writes versus256 after successful vector generation. Non-driving observer confirms upstream disconnected mem_rd_data_valid input (256 TB pulses, DUT remainsZ, zero writes). Retain failure; no compiler workaround or upstream modification.

next_parallel_runtime_lane:
  id: L109-JOINT-ORDERED-INDEPENDENT-RANDC
  status: FOCUSED_TESTED_BROAD_PENDING
  agent: /root/constraint_wide
  standards: IEEE1800-2017 18.4.2,18.5.10; IEEE1800-2023 18.4.2,18.5.9
  reproducer: evidence/batch-20260915-after-l95/next-z01-assessment/assessment.md
  actual: Both editions compile then reject a joint ordered-dist graph with an independent constrained randc leaf.
  expected: Solve independent enumerable randc components first and preserve cyclic progression, then existing ordered stages; randc is not itself an ordering operand.
  owns: [vvp/vvp_z3.cc]
  fixture_prefix: sv_joint_ordered_independent_randc
  dependencies: Existing graph-wide randc presampling and ordered scalar stages; independent of L108 parsing/lookup files.
  scope: Allow demonstrably independent enumerable randc components disjoint from order/dist references through existing randc-first sampling. Retain focused rejection of direct randc ordering and unsupported coupled components. No frontend changes, weakened constraints, false successes or altered rollback.
  acceptance: Paired deterministic dist and randc-cycle outcomes, independent scalar/element components where admitted, graph and object constraint preservation, unsatisfiable rollback and randc progression, disabled modes, direct illegal randc order and unsupported coupled negatives; existing Z01A/randc neighbors.
  coordinator: Owns shared builds/install/harnesses/manifests/docs/review/integration. No agent builds or commits.

current_application_evidence:
  opentitan: docs/conformance/session_logs/2026-09-15_implicit_property_method_lookup_validation.json
  caliptra: docs/conformance/session_logs/2026-09-15_caliptra_include_overlay_validation.json
parallel_selection_lane:
  agent: /root/constraint_abi
  status: ASSESSMENT_ONLY
  scope: Caliptra assessment found only a 2^32-element capacity abort, not a thin semantic fix. Assess existing constructor-dependent transition-bin reducer and independent frontend ownership next. No implementation until coordinator authorization.
  owns: [evidence/application-check-20260915/caliptra/next-caliptra-assessment, evidence/batch-20260915-after-l95/next-v01-assessment]
  build_policy: Coordinator owns all shared builds and tests; request a stable installed-tool window before reducer execution.

coverage_implementation_lane:
  id: L110-CONSTRUCTOR-TRANSITION-BINS
  status: FOCUSED_TESTED_BROAD_PENDING
  agent: /root/constraint_abi
  standards: IEEE1800-2017 and2023 19.5.2,19.11.3
  reproducer: evidence/application-check-20260915/l110-focused/nonzero-weight-union.sv
  assessment: evidence/batch-20260915-after-l95/next-v01-assessment/frontend-mechanism.md
  actual: Baseline drops constructor-dependent transition endpoints. Original zero-weight oracle was invalid; corrected default-weight two-instance oracle expects50percent and passes initial candidate. Remaining review and acceptance pending.
  expected: Resolve transition endpoint expressions at construction, retain the complete transition recognizer, and merge unsized bins by resolved transition identity.
  owns: [elaborate.cc, netclass.h, ivl_target.h, t-dll-api.cc, ivl.def, tgt-vvp/draw_class.c, vvp/parse.y, vvp/compile.h, vvp/class_type.h, vvp/class_type.cc, vvp/vthread.cc, vvp/vvp_cobject.h, vvp/lexor.lex]
  fixture_prefix: sv_cov_ctor_transition
  dependencies: Existing constructor range IR, dynamic coverage metadata and transition NFA; independent of L109 vvp_z3 runtime changes. L108 elaborate.cc changes integrated and frozen.
  scope: Extend existing metadata and evaluator end to end for constructor-dependent integral transition endpoints, including exact instance/type union identity and existing constant repetition modes. Preserve constant transition/value paths. No fabricated hits, flattened transitions, dropped terms or partial programs. Unsupported dynamic repetition and unrepresentable forms retain explicit diagnostics.
  acceptance: Both editions disjoint50percent and overlapping unions, named versus unsized bins, mixed constant/constructor ranges, signedness/defaults, consecutive/goto/nonconsecutive repetition, construction-time evaluation, instance/type queries, at_least/iff/reset/lifecycle, caps and atomic failure; invalid endpoints and unsupported forms; existing transition/value/cross neighbors and VVP metadata validation.
  coordinator: Shared builds/install/harnesses/main manifests/docs/review/integration only. Agent owns disjoint source and fixture files; no commits or new agents.

constraint_implementation_lane:
  id: L111-COUPLED-RANDC-ORDERED-DIST
  status: FOCUSED_TESTED_BROAD_PENDING
  agent: /root/constraint_wide
  standards: IEEE1800-2017 18.4.2,18.5.10; IEEE1800-2023 18.4.2,18.5.9
  reproducer: evidence/batch-20260915-after-l95/next-z01-coupled-assessment/coupled_randc_ordered_dist.sv
  assessment: evidence/batch-20260915-after-l95/next-z01-coupled-assessment/assessment.md
  actual: Legal coupled cyclic/ordinary ordered component is rejected by L109 admission.
  expected: Solve randc first, pin the selected cyclic value, then sample ordered ordinary variables in its feasible fiber; preserve cycles, constraints and transactional failure semantics.
  owns: [vvp/vvp_z3.cc]
  fixture_prefix: sv_joint_coupled_randc_ordered
  dependencies: L109 component classifier and existing cyclic sampling, joint enumeration and ordered stages; independent of L110 metadata/coverage files.
  scope: Complete enumerable scalar/fixed-element component support with one coupled cyclic leaf using existing stage/table mechanisms. Prove representation/support before draws without conditioning cyclic selection on later implementation failures. No dist fallback, weakened constraints or fake cycles. Larger/non-enumerable, multiple cyclic leaves and direct cyclic dist subjects remain explicit unsupported boundaries unless separately proved.
  acceptance: Paired multiple-cycle runtime oracle, scalar/fixed element paths claimed, multiple ordinary completions per cyclic value, UNSAT rollback/history/RNG, disabled modes, zero-weight/zero-total/cap behavior, direct illegal order negatives, L109 independence and Z01 neighbors. Distinguish standards-permitted randomization failure from implementation limits.
  coordinator: Shared builds/install/harnesses/main manifests/docs/review/integration. No agent builds or commits.

application_seed_lane:
  id: APP-ROOT-SEED-CONTROL
  status: FOCUSED_TESTED_BROAD_PENDING
  agent: /root/constraint_abi
  assessment: evidence/application-check-20260915/root-seed-assessment/l110-followup/ASSESSMENT.md
  reproducer: evidence/application-check-20260915/root-seed-assessment/root_seed.sv
  actual: +ntb_random_seed=1 and2 are ignored and produce identical root RNG streams.
  expected: Numeric uint32 seed controls design-root initialization reproducibly before root thread creation; omission preserves the current default stream and hierarchical process/object stability.
  owns: [vvp/compile.cc, vvp/vthread.cc, vvp/vthread.h]
  fixture_prefix: sv_runtime_root_seed
  scope: Once-only exact numeric seed parsing before first compile_thread creates a vthread; seed remains in VPI argv. Reject empty/signed/whitespace/junk/overflow/duplicate values before design thread creation. Reuse root generator, no UVM/corpus edits or special-case workloads.
  dependencies: L110 integrated and source released. Independent of L111 vvp_z3.cc.
  acceptance: Omitted default exact stream, repeated/equal/different seeds, uint32 zero/max, invalid and duplicate options, argv order, root/fork/object hierarchy stability and explicit srandom overrides, real unmodified OpenTitan smoke under explicit seeds when focus passes.
  standards: Application DV control; preserve IEEE1800-2017/2023 18.13 stability. The option spelling itself is not an IEEE conformance requirement.
  counting: Track application runtime capability separately from IEEE semantic increments.
  coordinator: Shared builds/install/harnesses/main manifests/docs/review/integration. No agent builds or commits.

L111_failure_refinement: Touching cyclic/distribution representation is preflighted with typed results. Other existing late unsupported rejections may remain because all exact-joint streams draw only from snapshotted graph owners; failed calls restore root/child RNG, values and cyclic histories. Deterministic failed-call-versus-control regression proves no draw is committed or skipped. Diagnostic timing alone is not a correctness requirement; semantic EMPTY is distinct from unsupported representation.

current_checkpoint:
  semantic_integrations: [L106, L107, L108, L109, L110, L111, L112, L113, L114]
  application_capability_revision: a8b831496
  installed_revision: L112 executable source plus uncommitted L113; fingerprints in evidence/application-check-20260915/l113-focused/initial-direct.json
  processes: None live at coordinator audit.
  l111_evidence: docs/conformance/session_logs/2026-09-15_coupled_randc_ordered_solving_validation.json
  seed_evidence: docs/conformance/session_logs/2026-09-15_runtime_root_seed_validation.json
  previous_goal_turn: Progress; L113 and L114 integrated with focused runtime/neighbor evidence; L115 selected from confirmed truncation.

sva_implementation_lane:
  id: L112-TWO-WINDOW-FIRST-MATCH
  status: FOCUSED_TESTED_BROAD_PENDING
  agent: /root/constraint_wide
  standards: IEEE1800-2017 and2023 16.9.8,16.13
  reproducer: evidence/batch-20260915-after-l95/next-first-match-two-window-assessment/two_window_earliest_failure.sv
  actual: Both editions reject legal finite two-window first_match source prefix at explicit windows <= 1 admission.
  expected: Earliest source endpoint at50 launches failing child at55; later endpoint must not rescue failure. Preserve all tied earliest matches.
  owns: [pform.cc, pform_sva_nfa.cc, pform_sva_nfa.h]
  fixture_prefix: sv_assert_multiclock_first_match_two_window
  dependencies: Integrated L107 finite source NFA and earliest-close lifecycle. Independent frontend assessment may not edit these files.
  scope: Admit direct finite Boolean chains with multiple literal windows through existing NFA depth proof and earliest endpoint closure. No runtime/scheduler changes or weakened shape diagnostics; request revised scope if existing NFA cannot preserve required semantics.
  acceptance: Paired earliest failure/success, tied paths, exact/ranged windows, cross-clock delay0/1 and coincident reverse order, implication/plain/cover, overlapping attempts, cancellation/restart/backlog, depth64/65 and unsupported unbounded controls, L107/L86 neighbors.
  coordinator: Owns all builds/installations/shared harnesses/main manifests/docs/review/integration. Agent prepares source and private fixtures only; no commits or agents.

frontend_assessment_lane:
  agent: /root/constraint_abi
  status: ASSESSMENT_ONLY
  scope: Trace nested indexed class constraint leaf.cyc[0] silently dropped in original L111 fixture. Establish standards, exact root cause, cheap reproducer and independent file ownership before implementation authorization. If ownership overlaps SVA files, report and select another independent mechanism with coordinator.
  evidence: evidence/application-check-20260915/l111-focused/boundary-direct.json
  owns: [evidence/application-check-20260915/nested-indexed-constraint-assessment]
  execution: Coordinator owns shared build and harness execution; agent may prepare source/commands and request coordinator repro runs.

L112_oracle_review: Original assessed c2 generator produces posedges25/65, not55/75, and b stimulus races source edge30. Preserve original evidence; require corrected off-edge stimulus and explicitly computed clock schedule before accepting earliest-failure oracle. Admission rejection remains reproduced, runtime timing claim needs correction.

nested_constraint_implementation_lane:
  id: L113-NESTED-FIXED-ELEMENT-CONSTRAINT
  status: FOCUSED_TESTED_BROAD_PENDING
  agent: /root/constraint_abi
  standards: IEEE1800-2017 and2023 18.3,18.4,18.5,18.8
  reproducer: evidence/application-check-20260915/nested-indexed-constraint-assessment/nested_indexed_leaf.sv
  baseline: evidence/application-check-20260915/nested-indexed-constraint-assessment/baseline.json
  actual: Both editions warn constraint ignored and runtime fails selected-element equality; nested scalar and direct element controls pass.
  expected: Nested fixed integral element references participate as canonical graph leaves with correct activity, value assignment and cyclic history.
  root_cause: Mixed object prefix and indexed terminal has no frontend IR identity; existing scalar state path cannot preserve element solve/writeback semantics.
  owns: [elaborate.cc, vvp/vvp_z3.cc]
  fixture_prefix: sv_constraint_nested_fixed_element
  dependencies: Existing direct ELEM identity, live nested object graph and L111 transaction/cyclic machinery. Disjoint from L112 pform files.
  scope: Resolve fixed integral constant-index terminal below class object prefix, preserve declared bounds/type/access and canonical graph identity end to end. No pinned-state substitution, dropped constraints or cap increases. Unsupported shapes must retain focused failure; report architecture expansion before edits outside ownership.
  acceptance: Paired rand/randc scalar and enum elements, ascending/descending/nonzero/negative bounds, repeated/aliased references, activity, equality/inside/dist/order where legal, null handles and failed-call rollback/history, out-of-range/nonintegral/unsupported index controls, direct-element and nested scalar/state/L111 neighbors. No ignored-item warnings in successful cases.
  coordinator: Owns all shared builds/installations/harnesses/main manifests/docs/review/integration. Agent source/private fixtures only; no commits/new agents.

L112_integration:
  revision: 8be562e9e
  evidence: docs/conformance/session_logs/2026-09-15_multi_window_first_match_validation.json
  status: FOCUSED_TESTED_BROAD_PENDING
next_sva_assessment_lane:
  agent: /root/constraint_wide
  status: ASSESSMENT_ONLY
  owns: [evidence/application-check-20260915/next-sva-after-l112]
  scope: Assess next independent standards-grounded SVA blocker with cheap failing reproducer and hand-computed nonvacuous clock oracle. No implementation before coordinator selection; no L113 file edits or shared builds/tests.
L113_progress:
  source: Frozen elaborate.cc c4bc74ce and vvp_z3.cc7c1af5a1 installed by coordinator98514.
  evidence: evidence/application-check-20260915/l113-focused/expanded-direct.json
  acceptance: Original silent-drop reproducer and expanded positive/negative direct cases match expected outcomes. Final harness packaging, focused nonintegral oracle and neighbors pending. No integration or broad qualification yet.
  scope_boundary: Existing element readback/writeback is limited to64bits; L113 wider nested elements reject explicitly. Direct wider-element path requires separately recorded debt, no claim of complete integral support.

first_match_suffix_implementation_lane:
  id: L114-FIRST-MATCH-SOURCE-SUFFIX
  status: FOCUSED_TESTED_BROAD_PENDING
  agent: /root/constraint_wide
  standards: IEEE1800-2017 and2023 16.9.8,16.13
  reproducer: evidence/application-check-20260915/next-sva-after-l112/first-match-with-source-suffix.sv
  baseline: evidence/application-check-20260915/next-sva-after-l112/baseline.json
  actual: Both editions reject legal first_match subsequence followed by a fixed source-clock suffix before multiclock handoff.
  expected: Commit earliest wrapped endpoint, retain all tied exits, continue retained paths through remaining source suffix, and discard later wrapper endpoints even if they could rescue the full sequence.
  owns: [pform.cc, pform_sva_nfa.cc, pform_sva_nfa.h]
  fixture_prefix: sv_assert_multiclock_first_match_source_suffix
  dependencies: Existing finite NFA and L112 tied-exit transport; independent of L113 elaborate.cc/vvp_z3.cc.
  scope: Direct finite Boolean first_match prefix followed by fixed finite same-clock Boolean suffix, then existing multiclock handoff. Retain wrapper boundary provenance in NFA, cut inside-wrapper alternatives at earliest exit while continuing tied exits. No scheduler/runtime edits or false prefix closure. Report scope expansion before broader changes.
  acceptance: Paired nonvacuous earliest failure versus later potential success, source-suffix failure after wrapper success, tied exits, overlap/cancel/restart, handoff0/1 coincident orders, depth boundaries, explicit unsupported nested/nonprefix/repetition/unbounded shapes; migrate now-legal existing negative to runtime positive and run L107/L112 neighbors.
  oracle_review: Initial baseline x goes low before later suffix tick70, so it establishes legality but not later-rescue discrimination. Permanent fixture must keep a later alternative successful while committed earliest path fails.
  coordinator: Shared build/install/harnesses/main manifests/docs/review/integration. Agent owns source/private fixtures only; no commits/new agents/worktrees.

L113_lookup_review:
  reproducer: evidence/application-check-20260915/l113-focused/missing-terminal.sv
  actual: Both editions silently compile leaf.missing[0] by falling through when leaf lacks missing and outer class declares missing array.
  requirement: Once a class-object prefix resolves, terminal lookup must diagnose missing property and never rebind to an outer property. Same selected nested identity scope; fix before integration.

L114_zero_delay_review:
  evidence: evidence/application-check-20260915/next-sva-after-l112/zero-delay-candidate.json
  actual: Initial candidate falsely accepts later wrapper endpoint when earliest endpoint has false same-clock zero-delay suffix; both modes report passes1 instead of required0.
  root_cause: NFA fusion adds suffix guard to the edge used to detect earliest wrapper exit.
  required_refinement: Preserve wrapper-boundary guard conjunction separately through fusion/folding so earliest exit commits regardless of suffix truth. Retain complete transition guards for suffix evaluation; no later rescue.

L113_integration:
  revision: 355243ab4
  evidence: docs/conformance/session_logs/2026-09-15_nested_fixed_element_constraints_validation.json
  status: FOCUSED_TESTED_BROAD_PENDING
wide_element_assessment_lane:
  agent: /root/constraint_abi
  status: ASSESSMENT_ONLY
  owns: [evidence/application-check-20260915/next-wide-element-assessment]
  scope: Trace confirmed direct65-bit element truncation through all readback/model-extraction/writeback callers. Assess reuse of existing arbitrary-width scalar transport and complete element activity/transaction/cyclic semantics; prepare concrete independent implementation scope before authorization.
  baseline: evidence/application-check-20260915/nested-indexed-constraint-assessment/direct-wide-baseline.json
  execution: No agent shared builds/tests/install/commits/new agents or worktrees.

wide_element_implementation_lane:
  id: L115-WIDE-FIXED-RAND-ELEMENT-TRANSPORT
  status: AUTHORIZED
  agent: /root/constraint_abi
  standards: IEEE1800-2017 and2023 18.3,18.4,18.5,18.8,18.13
  reproducer: evidence/application-check-20260915/nested-indexed-constraint-assessment/direct_wide_element_debt.sv
  baseline: evidence/application-check-20260915/nested-indexed-constraint-assessment/direct-wide-baseline.json
  actual: Both editions report successful randomization but lose required bit64 of a65-bit fixed-array rand element.
  expected: Preserve every declared bit through state reads, constraints, ordinary solving and writeback with full-vector transactional rollback.
  owns: [vvp/vvp_z3.cc, elaborate.cc]
  fixture_prefix: sv_constraint_wide_fixed_element
  dependencies: Existing scalar arbitrary-width vector/Z3 conversions and L113 graph identities; independent of L114 pform/NFA.
  scope: Complete arbitrary-width ordinary rand fixed-element value transport across state pins, accept-current, diversity, guards, model extraction and writeback. Reuse vvp_vector4_t and existing binary Z3 conversions. Lift L113 nested element width admission only when shared transport correct. Keep <=64 behavior and RNG consumption unchanged. Audit every shared-helper caller; no new representation/cap or silently truncated path.
  boundaries: Wide randc history and exact enumerated ordered/dist domains remain separately unimplemented where uint64 representation is required; reject atomically before writes/draws rather than truncate. Do not claim these implemented or waive eventual support. Preserve already-supported <=64 cases; no arbitrary restriction of unrelated ordinary wide fields without demonstrated mechanism limit.
  acceptance: Paired65/96/128-bit direct+nested, high/low-bit constraints and arithmetic, signed/unsigned and 2/4-state values, multiple words/nonzero bounds, state pinning and X/Z diagnostic, high-bit randomness with deterministic replay, alias/activity, full-vector value and RNG rollback, explicit unconverted boundaries; existing element/scalar/member/container/randc/order/transaction neighbors unchanged.
  coordinator: Shared builds/install/harnesses/main manifests/docs/review/integration. Agent source/private fixtures only; no commits/new agents/worktrees.

L114_integration:
  revision: d25b1f94d
  evidence: docs/conformance/session_logs/2026-09-15_first_match_source_suffix_validation.json
  status: FOCUSED_TESTED_BROAD_PENDING
ranged_suffix_assessment_lane:
  agent: /root/constraint_wide
  status: ASSESSMENT_ONLY
  owns: [evidence/application-check-20260915/next-sva-after-l114]
  scope: Assess finite ranged same-clock suffix after first_match prefix. Earliest wrapper exit commits while suffix retains all endpoints; distinguish plain existential and implication aggregation. No implementation before selection.
  execution: No shared builds/tests/install/source changes/commits/new agents/worktrees.
L115_migration_authorization: Replace obsolete L113 wide_fail paired compile-negative with checked65-bit nested runtime positive under name sv_constraint_nested_fixed_element_wide. Preserve old artifacts in git/evidence. Agent owns replacement fixture/config/gold/private L113 list; coordinator main manifest mapping and removal at integration. Unsupported wide cyclic/ordered/dist controls remain explicit.
