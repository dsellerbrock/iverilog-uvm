id: U03
title: Select an acquired pinned UVM release in the iverilog driver
status: in_progress
phase: regression_testing
user_authorization: Explicit request for a UVM version picker similar to VCS; use pinned submodules or fallback downloader.
prerequisite: U02 pinned acquisition tooling completed140c58b14; L06 local semantic gates passed at4965219df. No dependence on B01 Windows export mechanism.
suspended_work: B01 awaiting external Windows CI at PR273/c189df5b8; contract saved in evidence/u03/suspended-b01.yaml. No gate waiver or closure.
contract:
  - --uvm=VERSION selects an acquired release and implies UVM; --uvm-list lists available releases.
  - Preserve -uvm bundled default, --uvm-home path selection, and --uvm-version reporting.
  - Reject malformed/missing release IDs and conflicting explicit home/version selectors; never silently fall back.
  - Release directories default to lib/ivl/uvm/releases; IVERILOG_UVM_RELEASES overrides the catalog directory.
  - Acquisition script optionally registers verified releases as source-directory links; preserve conflicting existing paths.
  - No compiler-time download, source rewriting, runtime redesign, or claims that a selected release is qualified.
required_gates:
  - Baseline missing-selector reducer; permanent driver resolution, listing, invalid/missing/conflicting option and spaced-path checks.
  - Acquisition registration preservation check; existing UVM frontend controls; real-DPI selected known-release smoke.
  - Independent review and required integrated validation; preserve frozen installed tools until ready to validate candidate driver.
next_action: Await integrated90425, then run full JSON serially; checkpoint U03 after required gates.
stop_after_completion: false

local_evidence: Baseline rejects --uvm-list; candidate picker controls pass; seven acquisition integrity tests pass; all15 registered; selected2020.3.1 real-DPI smoke passes with zero warning/error/fatal.
validation_running: Integrated90425; frontend92166 passed all scenarios, final independent review clear.
next_command: tail -20 ../evidence/campaign-20260908/u03/integrated.log
