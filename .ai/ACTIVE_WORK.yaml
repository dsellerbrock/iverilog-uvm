id: U05
title: Dispatch standalone UVM DPI reports back to SystemVerilog
status: in_progress
phase: spec_grounded
base_revision: 47e6c87b3f420776e915cc969fe29ac03d8ac827
last_validated_revision: 47e6c87b3f420776e915cc969fe29ac03d8ac827
suspended_parent: U04 preserved in evidence/campaign-20260908/u04/suspended-u04.yaml; no parent implementation started.
selection_reason: Legacy regex error paths require real UVM reporting; standalone umbrella currently replaces the exported callback with a no-op, proved independently.
reproducer: ../evidence/campaign-20260908/u04/report_bridge.sv with report-baseline-compile.log and report-baseline-runtime.log; reports remains0 instead of1.
expected_behavior: A context DPI call to m_uvm_report_dpi invokes m__uvm_report_dpi in selected uvm_pkg exactly once with all six arguments intact and restores caller scope.
root_cause: UVM_DPI_STANDALONE defines m__uvm_report_dpi as no-op under an obsolete dead-code assumption; generated per-design stubs already show the existing runtime dispatcher ABI.
standards:
  - IEEE1800-2023 H.7.3 Table H.1 maps int and string to C int and const char pointer; H.9.2/H.9.3 require context exported calls to execute with the selected scope. Verify matching2017 text before closure.
allowed_scope:
  - uvm_dpi/uvm_dpi_iverilog.cc standalone callback adapter using existing runtime export dispatcher.
  - uvm_dpi/build_uvm_dpi.sh comments describing the adapter.
  - .github/uvm_frontend_test.sh and permanent tests/uvm_releases reporting fixtures.
  - Active/campaign/blocker/session evidence and documentation.
non_goals:
  - No legacy regex adapter or original vendor-source edits during U05.
  - No new runtime dispatcher, public ABI redesign, report suppression or diagnostic-only stand-in for the actual callback.
invariants:
  - One active blocker; preserve U04 reducer and original sources, then resume U04 after U05 validation.
  - Preserve merged regression builds that link real per-design dispatchers; adapter only under standalone macro.
  - Native ARM64 serial build,300second CPU guard, no RSS cap; no installs or frontend relocation test while gates run.
required_gates:
  - Paired2017/2023 failing baseline and passing standalone callback with all arguments checked.
  - Installed real-UVM report-server severity count test and frontend relocation/selection suite.
  - makecheck, required integrated legacy/VPI/negative/runtime, JSON, real-DPI UVM, NFA and independent review before closure.
completion: Close standalone report dispatch only after all gates; then resume U04 legacy ABI scope.
stop_after_completion: false
next_action: Implement standalone adapter matching generated export C stub, add permanent paired reducer and real-UVM count check, then build and validate.
next_command: sed -n '192,222p' uvm_dpi/uvm_dpi_iverilog.cc
