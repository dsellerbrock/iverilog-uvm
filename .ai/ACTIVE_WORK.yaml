id: TYPED-ARRAY-RETURN-ELEMENTS
title: Preserve real and string array return storage through element reads and writes
status: DONE
base_revision: 83d5471e3
standards:
  ieee1800_2017: "13.4.1; 7.4 unpacked arrays; 11.4 assignment operators"
  ieee1800_2023: "13.4.1; 7.4 unpacked arrays; 11.4 assignment operators"
reproducer: evidence/dd031-current-assessment/review/
current_behavior: "Both editions abort compiling real array return compound updates and string array return element writes."
expected_behavior: "The function-name variable has its declared array return type; element reads/writes and real compound updates use array storage, including automatic calls and independent copied results."
root_cause: "Scalar return special cases intercept array signals in typed expression and assignment emission despite emitted return-array storage."
allowed_scope:
  - Restrict scalar return opcodes to scalar signals in real/string emitters
  - Reuse existing fixed-array read/write emission for return arrays
  - Cover real compound read/write siblings, index bounds, repeated automatic calls and copy independence
  - Paired focused regressions and direct neighboring scalar-return checks
forbidden_scope:
  - Unrelated scalar string character-write or compound-string fallback redesign
  - Array ABI redesign or broad solver work
validation_batch: 20260914_after_l64
focused_features_completed_in_batch: 2
pending_gates: "Reproduce, patch, root build, paired focused tests and permanent neighbors; broad suite at approximately ten features."
prerequisites: "PR281 header fix published and locally validated at4956b2c1c; six CI jobs running in34919528675. Patch preserved separately; CI observation continues. L65 integrated as83d5471e3."
next_action: "Implementation worker patches typed scalar-return guards; independent worker extends focused boundary evidence."
stop_after_completion: false

validation: "Root28/28 paired; permanent8/8 both harnesses; neighbors4/4; L65 integration8/8 both. Independent review passed. Full suite deferred to batch checkpoint."
