id: "U11"
title: "Original UVM1.0p1/1.1a command-line and regex DPI ABI"
status: "in_progress"
phase: "reproducing"
base_revision: "d39880176"
last_validated_revision: "795d9f360"
reproducer: "Original release results-6_pvcxya: missing dpi_get_next_arg_c/dpi_regcomp;standalone old-ABI fixture to pin iteration and regex semantics."
expected_behavior: "Old zero-argument iterator returns actual VPI argv in order,ends with null and restarts next call;tool metadata is actual VPI data. Old cached-regex API uses strict POSIX ERE with real handles,match/nonmatch and cleanup."
standards: ["IEEE1800-2017/2023 35.5.5/35.5.6,AnnexH DPI C-compatible string/chandle ABI;original pinned Accellera1.0p1/1.1a src/dpi/uvm_svcmd_dpi.c/.svh define release-specific API"]
root_cause: "Installed backend exports modern initialized iterator and prefixed cached-regex API only. Old no-init iterator is not ABI-compatible with a plain alias. Icarus provides argc-bounded flat VPI argv;old vendor pointer-stack convention cannot be copied."
allowed_scope: ["Fork-owned DPI umbrella old entrypoints;reuse existing regex/tool metadata helpers;standalone paired-edition regression and frontend registration;original source release replay and directly affected documentation."]
non_goals: ["No original UVM edits,DPI disabling,empty argv/match stubs,full native release backend qualification;no unrelated1.0p1 unresolved-functor/cast or1.1b/c recording/parser fixes."]
required_gates: ["Pre-fix missing-entrypoint proof;both editions actual argv/restart/metadata/regex/lifetime controls;independent review;native build/makecheck/integrated/JSON/UVM/NFA/releases;frontend last alone/restore/fivehashes."]
next_action: "Establish old API failing standalone test,then add minimal adapters."
next_command: "cat ../evidence/campaign-20260908/l24/older-dpi-source-notes.md"
stop_after_completion: false
