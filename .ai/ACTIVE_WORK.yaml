blocker: OT-SPI-INLINE-CALLER-QUEUE-FOREACH
status: REGRESSION_TESTED
base: b9befcb735cbbbb56feacfbb9fbdbd120b622f55
parent_pr: https://github.com/dsellerbrock/iverilog-uvm/pull/319
objective: Honor caller-scope lookup and actual caller-state queue iteration in inline randomize constraints used by pristine SPI TPM sequences.
authorization: Continuous compiler campaign; coordinator freezes published and locally validated SVA patch, selects reproduced SPI compiler blocker.
standards:
- IEEE 1800-2017 18.7 inline scope resolution, 18.5.8.1 iterative constraints
- IEEE 1800-2023 18.7 inline scope resolution, 18.5.7.1 iterative constraints
reproducer: evidence/review-20260920/spi-caller-foreach-next/caller_queue.sv
red: Both editions reject foreach(data_q[i]) while data_q is a caller-owned queue absent from the randomized item.
expected: Resolve target first, then caller; snapshot state collection and constrain target elements using actual runtime indices and values. Preserve empty iteration, resizing across calls, failed-solve rollback and four-state semantics.
root: Simple foreach lowering returns unsupported after target property lookup fails; caller-state collection path currently handles hierarchical object/struct-element arrays only.
allowed_files:
- elaborate.cc constraint foreach lookup and existing caller-state capture/template emission
- vvp/vvp_z3.cc existing state-foreach expansion if required by the proven representation
- necessary declarations only after coordinator records their direct dependency
- directly related private and permanent regressions and owning documents
forbidden:
- OpenTitan or Caliptra source edits
- Relaxing array element equivalence to bypass valid queue copy-out diagnostics
- Ignored constraints, changed RNG ownership or failed-solve rollback, arbitrary bounds, invented array elements
- Unrelated constraint-function or nested-struct implementation
lanes:
  implementation:
    owner: selected_vif_relay_lifetime Terra/medium after current assessment handoff
    files: elaborate.cc and vvp/vvp_z3.cc; private caller-foreach evidence
  coordinator:
    owner: root
    files: shared build/install/test outputs, standards interpretation, integration, review, documentation
acceptance:
- Both editions pass exact caller-state queue copy constraints, empty and resized queues.
- Target-first name collision and explicit caller qualification remain correct.
- Contradictory constraints fail with target values preserved; invalid paths never silently disappear.
- Existing caller-state, foreach, scope-resolution and randomization regression groups pass.
- Required integrated gates per regression policy; risk HIGH for shared constraint IR behavior.
prerequisite: Trace existing qforeach/state snapshot and operand lowering before editing; record any material scope expansion first.
stop_after_completion: Freeze validated patch and return to coordinator selection; full mission remains active.

parallel_test_lane:
  owner: eval_evidence_audit Luna/low
  files: evidence/review-20260920/spi-caller-foreach-next/independent-boundaries only
  task: Independent empty/resizing, name-collision and failed-solve rollback regression sources; no compiler or shared builds.

latest_validation:
  evidence: docs/conformance/session_logs/2026-09-22_caller_queue_foreach_focus.json
  source_freeze: Build3; compiler and permanent test sources frozen.
  completed: Focused paired cases, permanent legacy/JSON focus, mapped constraint UVM, smoke, integrated, rootseed, makecheck, NFA.
  pending: Publication and CI; all required local gates passed.
