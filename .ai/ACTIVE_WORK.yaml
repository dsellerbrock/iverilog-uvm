id: SV-UVM-NEXT-BATCH-20260921
status: PARALLEL_IMPLEMENTATION
objective: Full IEEE1800-2017/2023 compiler semantics and independent UVM compatibility; unmodified stable OpenTitan and Caliptra applications.
authority: User-authorized continuous campaign; coordinator selects implementation only after reducer/spec/root cause.
branch: agent/scope-sva-next-20260921
head_checkpoint: dba0473b6
history: evidence/review-20260920/next-pwrmgr-hdl-path-assessment/before-ACTIVE_WORK.yaml
completed_focused_subsets:
  - SCOPE-RANDOMIZE-UNKNOWN (25653fdc0)
  - OT-PWRMGR-LONG-FIXED-ANTECEDENT (acb786965)
  - CONSTRAINT-OBJECT-METHOD-RECEIVER (346b48f68)
  - ENUM-NAME-BARE-CONCAT-TYPE (c8e6cb97c)
  - SELF-PACKAGE-TYPE-CAST (e42f114cd)
  - SIGNED-COVER-BINS-CROSS-ZERO (e42f114cd)
  - VPI-PACKED-ELEMENT-ACCESS (1a340208c)
  - CONSTRAINT-DIVMOD-ZERO (86a2e967d)
  - JOINT-ORDERED-ACTIVE-RANDC (dba0473b6)
validation: All seven required local gates passed for nine integrated fixes through c1635efe9; see2026-09-21_nine_fix_batch_qualification.json. Private candidates await separate integration/validation. No completed-clause/full-application claim.
coordination:
  owner: Coordinator owns selection, source integration, shared build/install, tests and final review.
  workers: At most2implementation agents with disjoint file ownership; private patches only.
  preservation: External AGENTS.md and unique evidence remain untouched; no new worktrees.
  forbidden: Dropped semantics, fabricated outputs, application changes to conceal compiler defects, weakened tests.
assessments:
  packed_vpi:
    owner: constraint_guard_semantics, Sol medium
    state: REPRODUCED_ROOT_TRACE
    evidence: evidence/review-20260920/next-pwrmgr-hdl-path-assessment
    observation: Packed multidimensional element paths do not resolve; by_index returns one bit instead of whole element. Actual pwrmgr cnt_q root44bits exists, element paths absent.
    scope: Trace declaration metadata, VPI lookup/index/select handles and get/put/force/release semantics; proposal before implementation.
    standards: IEEE1800-2017/2023 VPI packed array model and38.21 handle_by_name; verify corresponding index clauses.
  vif_event:
    owner: package_enum_context, Terra medium
    state: APPLICATION_REDUCTION
    evidence: evidence/review-20260920/next-pwrmgr-event-assessment
    observation: pwrmgr scoreboard compound virtual-interface edge expressions are skipped with compile-progress warnings.
    scope: Reproduce exact edge semantics and trace existing event lowering; proposal before implementation.
    acceptance_basis: Observe expression transitions, not every operand change; no skipped waits.
next_action: Integrate reviewed VPI corrections and structural event implementation, run focused semantic tests, then select the next reproduced application/debt/IEEE blocker.

selected_implementation:
  id: COMPOUND-VIF-EDGE-EVENTS
  status: PRIVATE_IMPLEMENTATION_UNDER_REVIEW
  owner: vif_proxy_implementation, Sol medium
  standard: IEEE1800-2017/2023 9.4.2 event expression and edge transitions
  reproducer: evidence/review-20260920/next-pwrmgr-event-assessment/compound_edges.sv
  red: Both editions compile0 but fail at time1 because reduction/equality/selected-bit waits were skipped.
  expected: Wait until the complete expression has the requested edge; operand changes alone do not imply an edge.
  root: PEventStatement::elaborate_st compound class-to-VIF expressions fall through static synthesis and are skipped.
  scope: Structural implementation scope and coordinator glue are defined in event_structural_expansion below; procedural resampling proposal rejected.
  acceptance: Paired runtime edge counts, no early execution, predicate unchanged on operand mutation, rearming, packed field/select,4state transitions, rebind semantics grounded in LRM, real-edge rejection.
  forbidden: Remove warnings without semantics, broaden sensitivity to all operands, skip waits, modify application sources.
  dependencies: Disjoint from VPI declaration/runtime proposal; coordinator shared build/install/tests.

selected_vpi_implementation:
  id: VPI-PACKED-ELEMENT-ACCESS
  status: FOCUSED_TESTED_BATCH_GATES_PENDING
  owner: constraint_guard_semantics, Sol medium
  standards: IEEE1800-2017/2023 38.19 and38.21; verify chapter37 object properties against selected integral type.
  evidence: evidence/review-20260920/next-vpi-packed-element-assessment/RESULTS.md
  reproducer: evidence/review-20260920/next-pwrmgr-hdl-path-assessment/probe.sv plus probe.c; paired runtime failures retained.
  expected: Legal indexed packed elements expose the declared remaining width and real read/write/force/release semantics; invalid selects return null.
  root: tgt-vvp discards packed dimensions; VVP signal indexing returns flattened bits and name lookup has no indexed descent.
  scope: Private patch in tgt-vvp/vvp_scope.c, vvp/lexor.lex, vvp/parse.y, vvp/compile.h, vvp/words.cc, vvp/vpi_priv.h, vvp/vpi_signal.cc, vvp/vpi_priv.cc, vvp/array_common.h, vvp/array.cc, vvp/vpi_callback.cc. Propose additional files first.
  architecture: Preserve ordered declaration metadata without changing flattened value storage; reuse existing selected-value machinery, stable handle ownership, and callbacks. Verify observable VPI types and properties from normative object model.
  acceptance: Paired2017/2023 nets/variables, ascending/descending/nonzero/negative ranges, nested selectors, unpacked words, escaped identifiers, invalid indices, XZ, get/deposit/force/release and callbacks; unchanged pwrmgr22-bit element paths.
  forbidden: Path-check exceptions, dummy handles, forged values, application edits, metadata-only success claims.
  dependencies: Disjoint from structural event worker files; coordinator owns shared glue, integration, builds and regression directories.
  boundary: Integral packed selection via by-name and by-index; no full VPI object-model qualification or multi-index API completion claim.

  scope_refinement: IEEE chapter37 requires packed prefix handles retain net/variable type, not part-select type. vpi_callback.cc is authorized solely to route these real selected handles through existing filtered signal/array callback machinery.

event_scope_review:
  evidence: docs/conformance/session_logs/2026-09-21_vif_event_pulse_boundary.json
  rejected: Procedural post-wake filter loses same-timeslot pulses; private exploratory patch remains unapplied.
  next: Read-only trace of synchronous dynamic expression capture and exact runtime scope proposal before further edits.

event_structural_expansion:
  status: COORDINATOR_AUTHORIZED_IMPLEMENTATION
  reason: Procedural resampling loses intermediate edges; use synchronous structural proxy and existing expression/edge networks.
  design: evidence/review-20260920/next-compound-vif-edge-fix/RUNTIME_SCOPE_PROPOSAL.md
  worker: vif_proxy_implementation, Sol medium
  worker_files: elaborate.cc, expr_synth.cc, netlist.h, netlist.cc, emit.cc, target.h, target.cc, t-dll.h, t-dll.cc, t-dll-api.cc, ivl_target.h, vvp/vvp_vinterface.h, vvp/vvp_vinterface.cc, vvp/event.h, vvp/event.cc
  coordinator_glue: tgt-vvp/vvp_scope.c, tgt-vvp/draw_net_input.c (new LPM output nexus linkage), and VVP directive parser/compiler registration; worker must supply exact descriptor/API requirements, not edit Sol-owned files.
  ownership: Sol remains sole private editor of shared packed-VPI file set; coordinator integrates common glue after reviewing both patches.
  acceptance: Existing compound/table/pulse fixtures; simultaneous automatic activations; changed and unchanged handle rebind; alias mutation propagation; old-source unlink; scope teardown; no VM reentry; no side-effect or function semantics lost.
  boundary: No general runtime interpreter or AST clone framework; preserve unsupported forms with focused diagnostics. Propose further files before editing.

event_worker_escalation: Terra completed repeated planning-only turns with no structural source changes; replaced with Sol medium for the authorized cross-stage structural implementation. Existing findings and fixtures retained.

vpi_force_overlap_expansion:
  status: COORDINATOR_AUTHORIZED_IMPLEMENTATION
  owner: constraint_guard_semantics, Sol medium
  invariant: Packed selections share root storage but force/release callbacks must identify the affected selection; sibling callbacks cannot fire spuriously.
  root: Existing signal force callback registry discards affected slice; local selected-handle routing alone cannot preserve overlap semantics.
  additional_files: vvp/vvp_vpi_callback.h, vvp/vvp_net_sig.cc, vvp/vthread.cc
  scope: Extend existing callback dispatch with exact force mask and release range; preserve whole-object and real callbacks. Private patches only.
  acceptance: Paired sibling/prefix/leaf exact callback counts and values; sparse masks; registration/removal during dispatch; legacy force neighbors.
  dependencies: Disjoint from structural event worker; coordinator owns shared integration/builds.

selection_priority:
  authority: User reiterated debt, unmodified stable OpenTitan/Caliptra, and missing IEEE1800 semantics as next work.
  current: Finish two selected application-driven compiler fixes; do not count review-only or compile-only results as integrated fixes.
  ready: VARIABLE-OUTPUT-PORT-DIRECTION has paired failing runtime and explicit-wire control; select when elaborate.cc ownership becomes free.
  assessment: caliptra_next_blocker, Luna low, read-only current stable-release failure selection; no third implementation lane.

event_validity_expansion:
  status: COORDINATOR_AUTHORIZED_IMPLEMENTATION
  reason: IEEE2017/2023 25.9 null dereference fatal must occur only when expression is evaluated; 11.3.5 preserves short-circuit behavior.
  worker: vif_proxy_implementation, Sol medium, existing authorized files only.
  design: Structural data plus validity, expression-aware short-circuit propagation, arm-aware final waitable event validity input; no procedural post-wake resampling.
  coordinator_glue: Target emission and .eventvalid parser/compiler registration; worker supplies exact APIs and descriptors.
  acceptance: Dormant null permitted; armed null fatal; guarded null branches valid; four-state conditional evaluation; validity/data propagation must not create transient false fatal or lose same-slot pulses; automatic lifecycle tests.
  constraint: Reuse existing synthesis helpers; no general expression interpreter or speculative framework; no silent unsupported expression validity fallback.

ready_continuous_string:
  id: CONTINUOUS-STRING-ASSIGN
  status: REPRODUCED_SPEC_GROUNDED_ROOT_CAUSED_AWAITING_FILE_OWNERSHIP
  standards: IEEE1800-2017/2023 6.5,6.16,10.3.2
  evidence: evidence/review-20260920/next-caliptra-debt-assessment/CONTINUOUS_STRING_ASSESSMENT.md
  root: String variable coerced to unresolved net and lowered through fixed-width vector synthesis.
  proposed: Preserve complete string variable storage and reuse behavioral continuous-variable lowering with time-zero evaluation and RHS sensitivity.
  dependency: elaborate.cc remains owned by structural event lane; no concurrent edits authorized.
  acceptance: Paired constant,dynamic,empty,resized strings; existing multi-driver rejection; unmodified stable Caliptra rej_bounded_tb replay.

selected_constraint_divzero:
  id: CONSTRAINT-DIVMOD-ZERO
  status: FOCUSED_TESTED_BATCH_GATES_PENDING
  owner: constraint_guard_semantics, Sol medium
  standards: IEEE1800-2017/2023 11.3.4,11.3.5,18.3
  reproducer: evidence/review-20260920/next-constraint-array-assessment/state_selected_divzero.sv
  red: Both editions incorrectly randomize successfully with active10/0==-1.
  expected: Active X-producing division/remainder fails with focused constraint evaluation error; inactive short-circuit paths remain sifted; legal nonzero solutions remain available.
  root: Common Z3 div/mod builder uses totalized bit-vector operations without existing guard-aware state checks.
  scope: Private vvp/vvp_z3.cc patch only; permanent focused regression additions; coordinator shared builds/integration.
  design: Reuse existing StateCheck and typed error placeholder machinery; accepted models must exclude active zero divisor, never accept placeholder values.
  acceptance: Signed/unsigned divide/remainder, inactive implication/logic/conditional, random legal divisor, forced-zero focused diagnostics,wide values,transactional value/RNG/randc behavior; paired editions.
  dependencies: Disjoint from all structural event files; continuous-string and output-port fixes remain awaiting elaborate.cc ownership.

event_synchronous_cone_expansion:
  status: COORDINATOR_AUTHORIZED_IMPLEMENTATION
  owner: vif_proxy_implementation, Sol medium
  additional_worker_files: vvp/logic.h, vvp/logic.cc
  coordinator_files: tgt-vvp/vvp_scope.c, tgt-vvp/draw_mux.c
  root: Deferred boolean/mux gates lose event-expression pulses and leave guarded validity stale at wait entry.
  design: Mark only gates newly synthesized for event data/validity cones; carry event_synchronous property to target; existing functor symbols with /event suffix select synchronous runtime propagation.
  forbidden: Global change to ordinary combinational scheduling; delayed arm validation that rescues null dereference after later same-slot assignment.
  acceptance: Static/dynamic AND pulses,guarded null and same-root rebind,null-at-arm rescue negative,XZ conditional,automatic context reset/teardown; full prior edge fixtures.

event_part_select_expansion:
  status: COORDINATOR_AUTHORIZED_IMPLEMENTATION
  owner: vif_proxy_implementation, Sol medium
  additional_worker_files: vvp/part.h, vvp/part.cc
  coordinator_files: tgt-vvp/vvp_scope.c, vvp/lexor.lex, vvp/parse.y, vvp/compile.h
  root: Constant part-select SA functors defer propagation independently of AND/event and lose the reproduced same-slot pulse.
  design: Event-only part-select marker and synchronous propagation; preserve ordinary part-select scheduling. Worker supplies factory signature before coordinator glue.
  acceptance: Static and dynamic selected-bit Boolean pulses in both editions; atomic vector01-to10-to01 must not fabricate an AND edge; existing select bounds, XZ, automatic context lifetime and event validity tests.

selected_ordered_randc:
  id: JOINT-ORDERED-ACTIVE-RANDC
  debt: DD-001/Z01
  status: FOCUSED_TESTED_BATCH_GATES_PENDING
  owner: constraint_guard_semantics, Sol medium
  standards: IEEE1800-2017/2023 18.4.2; ordering18.5.10 in2017 and18.5.9 in2023.
  reproducer: evidence/review-20260920/next-constraint-ordered-randc-assessment/joint_ordered_active_randc.sv
  red: Both editions blanket-reject legal ordering of ordinary rand variables whenever a separate active randc exists; ingredient controls pass.
  root: Joint exact sampler rejects active randc instead of respecting implicit randc-first priority before explicit ordinary stages.
  scope: Private vvp/vvp_z3.cc patch and paired permanent regressions only; coordinator owns integration/build/install.
  design: Reuse complete component tables and transactional randc machinery; establish implicit cyclic stage before ordered ordinary stages. Retain preflight caps and RNG ownership.
  semantic_boundary: 18.4.2 permits failures caused by cyclic ordering; do not infer universal success or prematurely reset histories merely to obtain a satisfiable ordinary-stage model.
  acceptance: Full/restricted cycles,coupled and independent cyclic subjects,disabled randc,illegal explicit randc ordering,mathematically justified ordinary conditional distributions,seed replay,cap/UNKNOWN/UNSAT value/history/RNG/callback rollback.
  forbidden: Remove rejection without correct priority; cap increases; arbitrary model selection or biased successful subsets; event/elaboration edits.
  dependencies: Disjoint from event worker; divmod integrated86a2e967d.

event_atomic_fanout_expansion:
  status: COORDINATOR_AUTHORIZED_PRIVATE_IMPLEMENTATION
  owner: vif_proxy_implementation, Sol medium
  additional_worker_files: vvp/vvp_net_sig.cc
  root: Synchronous event-cone gates fabricate edges from partial fanout of one atomic vector update; static baseline does not.
  design: Signal-source nesting boundary with event-cone recompute queue and final validity-before-edge publication; no queue survives source dispatch. Existing pure_comb evaluation transaction is too broad and must remain separate.
  constraints: Defer only marked event-cone consumers; ordinary events, signal writes and VPI callbacks keep their behavior. Hook declarations must avoid event.h/net.h include cycles. Context teardown removes pending references.
  acceptance: Both atomic and pulse controls; static/dynamic/automatic/XZ/rebind/null tests; independent same-slot NBA updates; nested callback-origin source writes must remain distinct from derived fanout.
  review: Source-only nesting cannot silently coalesce reentrant procedural/VPI writes; trace and test this boundary before integration. Default-root seeding must not leak one automatic activation into another.

event_callback_boundary_review:
  root: filter_mask_ invokes callbacks inside downstream signal dispatch; object callback ordering alone does not prove reentrant write separation.
  candidate: Worker private callback suspend/resume refinement in vvp_net_sig.cc; no shared application yet.
  acceptance: boolean_callback_atomic fixture captures distinct callback writes; callback suspension must not publish a partially propagated original vector as a false edge. Test fanout order permutations and original-source plus callback-source transitions.
  status: Architecture risk remains open until boundary evidence is reviewed.

batch_qualification:
  state: REQUIRED_LOCAL_GATES_PASS_PUBLICATION_PENDING
  revision: dba0473b6
  integrated_fixes: 9
  reason: Approximately-ten cadence and full-UVM policy threshold; event patch stays private until acceptance passes.
  coordinator: Owns serial broad suites and PR preparation; no source integration/build/install until gates terminate.

selected_runtime_root_seed:
  id: DV-RUNTIME-ROOT-SEED
  status: PRIVATE_PATCH_PREPARED_AWAITING_COORDINATOR_VALIDATION
  owner: caliptra_next_blocker, retained Luna low for mechanical historical-patch rebase; coordinator owns semantic review and runtime validation.
  objective: Restore application-selectable runtime root seeding without changing default streams or hierarchical random stability.
  standard_boundary: IEEE1800-2017/2023 random stability and explicit seeding are invariants; +ntb_random_seed syntax is a vendor-compatible DV extension, not a normative IEEE option.
  red: evidence/review-20260920/rootseed-baseline-audit/current-runtime.json shows seed1 and seed2 produce identical four random words in both editions; option parser absent.
  root: Historical a8b831496 feature was not integrated in current main lineage; do not blindly cherry-pick unrelated files or cite old qualification.
  scope: Private mechanical rebase of vvp/compile.cc, vvp/vthread.cc, vvp/vthread.h and root-seed regressions from a8b831496; no shared source/build/install changes during qualification.
  acceptance: Compare actual random payloads, not echoed seed labels; same-seed replay, different-seed variation, default compatibility, hierarchy/sibling stability, explicit overrides, numeric boundaries and focused invalid-option errors before simulation; both editions.
  dependencies: Disjoint from event worker files; integration after frozen batch qualification. No new worktree.

selected_ordered_randc_multidist:
  id: JOINT-ORDERED-RANDC-MULTIDIST
  status: PRIVATE_FOCUSED_TESTED_ADDITIONAL_FAILURE_BOUNDARY_AUDIT
  owner: constraint_guard_semantics, retained Sol medium
  standards: Both editions18.4.2; 2017 18.5.4/18.5.10 and2023 18.5.3/18.5.9.
  reproducer: evidence/review-20260920/next-constraint-ordered-randc-multidist-assessment/joint_ordered_randc_multiple_dist.sv
  root: Multi-distribution preflight omits implicit cyclic and intermediate weighted prefixes; current implementation rejects this valid combination before drawing.
  scope: Private vvp/vvp_z3.cc patch and focused regressions only. No shared edits/build/install during frozen batch qualification.
  design: Reuse exact distribution resolver, expose nonmutating positive support and prove existing-cap complete component prefix tree before any draw; then reuse transactional cyclic and ordinary staged sampling.
  policy: Conflicting statistical weights may follow existing documented priority under the full IEEE exceptions; all hard domains and zero-weight exclusions remain mandatory. No mandated-product or cross-vendor-identical probability claim.
  acceptance: Paired weighted/cyclic/order interactions, zero/excluded fibers, caps, replay and complete value/RNG/history/callback rollback; preserve edition-specific range rules and single-dist marginal tests.
  forbidden: Drop hard constraints, silently fallback, increase caps, choose only successful unsupported prefixes, alter shared frozen source.
  dependencies: Rootseed mechanical rebase is complete and waiting; only event and this solver lane actively implement.

event_callback_replay_scope:
  state: PRIVATE_REVIEW_PENDING
  files: vvp/vvp_net.h and vvp/vvp_net_sig.cc in addition to existing event/logic/part ownership.
  purpose: Source transaction boundaries and ordered callback-origin marked-cone replay preserve parent atomicity and separate nested source pulses while ordinary storage/callback execution stays synchronous.
  acceptance: Parent-fanout low/high/reordered callback taps, automatic context liveness/recycling and active-null negatives; no shared integration before full focused matrix and coordinator review.

packed_metadata_synthesis_regression:
  id: VPI-PACKED-ELEMENT-ACCESS-SYNTH-REGRESSION
  status: FOCUSED_TESTED_AWAITING_BROAD_RERUN
  owner: Coordinator integrated reviewed Sol patch
  trigger: Broad JSON synth_interface_member_transfer-S fails runtime with four packed dimension metadata target is not integral storage diagnostics.
  evidence: evidence/review-20260920/batch-nine-20260921/json-first-failure
  scope: Private trace and minimal repair of integrated packed-metadata emission/resolution; tgt-vvp/vvp_scope.c, vvp/words.cc, tests. Propose other files before editing.
  acceptance: Original synthesis runtime reducer, matching nonsynthesis control, paired 2017/2023 packed VPI controls, exact metadata widths and alias handling; no metadata omission to conceal unsupported semantics.
  coordination: Multidist patch frozen pending integration; this is its replacement lane. Shared broad process60589 terminated on JSON failure; coordinator owns build/install and validation.

selected_strict_uvm_regex:
  id: UVM-STRICT-REGEX-GLOB-FALLBACK
  status: PRIVATE_FOCUSED_TESTED_AWAITING_NEXT_BATCH_INTEGRATION
  owner: caliptra_next_blocker, retained Luna low for bounded removal after coordinator root-cause proof
  objective: Honor explicit strict/deglob mode in the real UVM DPI API without application-specific fallback.
  scope: Private copy of uvm_dpi/uvm_dpi_iverilog.cc and focused tests only. No shared source/build/install modifications during qualification.
  standard_boundary: Pinned UVM API behavior; IEEE1800.2 clause mapping unassessed, no IEEE1800 language qualification claim.
  evidence: evidence/review-20260920/next-uvm-regex-assessment/current-results.json
  root: regcomp wrapper retries invalid strict expressions as globs despite deglob=0.
  acceptance: Invalid strict pattern returns null; same pattern explicitly deglobbed succeeds; valid regex matching/nonmatching, slash-bracket behavior, bounds, legacy strict behavior remain unchanged; paired real DPI.
  dependencies: Event, multidist and rootseed patches frozen; this is the only active implementation lane. Coordinator alone builds private DPI candidate after review.

rootseed_threadless_validation_boundary:
  status: PRIVATE_FOCUSED_TESTED_ENTRY_HOOK_REPAIR
  owner: caliptra_next_blocker, retained Luna low; coordinator validated original private runtime
  evidence: evidence/review-20260920/rootseed-rebase/coordinator-build/threadless-boundary.json
  root: Option validation runs only from compile_thread, so designs with no threads silently accept invalid root-seed options.
  scope: Replace compile.cc hook with one vvp/lib_main.cc::vvp_run hook after lifecycle check and before compile_design. Keep vthread.cc/h parser and default RNG unchanged. Private files only.
  acceptance: Existing paired seed driver plus threadless invalid, duplicate, valid-boundary, and no-option controls; no shared build/install.
  dependency: Regex patch frozen; this is its replacement implementation lane, alongside solver boundary tests.

selected_continuous_string:
  id: CONTINUOUS-STRING-ASSIGN
  status: PRIVATE_FOCUSED_TESTED_COORDINATOR_REVIEW
  owner: vif_proxy_implementation, retained Sol medium with elaboration context
  standards: IEEE1800-2017/2023 6.5,6.16,10.3.2
  evidence: evidence/review-20260920/next-caliptra-debt-assessment/CONTINUOUS_STRING_ASSESSMENT.md
  reproducer: continuous_string.sv and continuous_string_dynamic.sv in assessment directory; both editions abort instead of preserving string values.
  root: Continuous-assignment elaboration promotes string REG to vector-oriented UWIRE and lowers via scalar net/BUFZ; runtime lacks valid string storage.
  scope: Private elaborate.cc patch and focused regressions only. Reuse existing continuous-variable initialization/sensitivity lowering before lnet promotion. Reserve driver identity and preserve all assignment restrictions.
  acceptance: Paired constant,dynamic,empty,resized,concat,function-dependency,time-zero cases; duplicate continuous/procedural-overlap rejection; delayed/selected/member forms must have correct behavior or focused existing unsupported diagnostic, never crash. Retained stable Caliptra rej_bounded_tb is integration acceptance.
  forbidden: App changes, string truncation, fabricated paths, weakened driver checks, global scheduler changes, shared build/install/source changes.
  event_dependency: Event candidate392b50c66ed9e918c3e8fbaed13df195f2b74ee57e061c6511cf4484bb0e8eac safely suspended immutable pending integration. No event worker edits remain; elaborate.cc ownership transferred for this separate private candidate. Coordinator resolves final patch order after broad gate/publication.
  lanes: This lane and solver failure-boundary fixtures; rootseed/regex/event immutable awaiting integration.

continuous_string_review_checkpoint:
  candidate: evidence/review-20260920/next-caliptra-continuous-string-fix/CODE.patch
  sha256: 9e8dc260ac98df3c7f429c2934275477e01cf854dbd17ebf4155bc05463e662f
  evidence: 45 expected outcomes pass privately across2017/2023/2012; integrated application acceptance pending.
  lane: Retained Sol medium audits unused _ivl_continuous_variable attribute and synthesis boundary; only private correction allowed.
  preparation: Retained Luna low prepares unchanged stable Caliptra replay command read-only; coordinator executes.

selected_variable_output_direction:
  id: VARIABLE-OUTPUT-PORT-DIRECTION
  status: COORDINATOR_AUTHORIZED_PRIVATE_IMPLEMENTATION
  owner: vif_proxy_implementation, retained Sol medium
  standards: IEEE1800-2017/2023 23.2.2.3 and23.3.3.2; verify applicable wording before editing.
  evidence: evidence/review-20260920/next-pwrmgr-driver-assessment/port-kind-results.json
  reproducer: port_logic_vector.sv, port_procedural_struct.sv, interface_resolved_ports.sv; explicit-wire control passes.
  expected: Variable output values remain internal driver values when external resolved wire has conflicting driver; external wire resolves normally. Explicit wire ports retain collapse semantics.
  root: REG-to-UWIRE conversion allows output-variable connection collapse and external driver feedback; trace precise connection path before editing.
  scope: Private elaborate.cc patch and focused regressions; reuse existing directional output buffers and original variable identity. If declaration metadata is lost, report precise prerequisite before expanding files.
  acceptance: Paired2017/2023 scalar/vector/packed-struct and nested outputs, procedural and continuous drivers, external conflict/release, explicit-wire collapse control, variable multiple-driver and input/inout neighbors.
  forbidden: App edits, warning-only masking, changing explicit net behavior, shared compiler/build/install edits.
  dependencies: Event and corrected string patches frozen immutable; elaborate.cc private ownership transfers here. Coordinator handles ordering and shared integration afterPR308. Caliptra trace lane is read-only and disjoint.

string_permanent_packaging:
  owner: caliptra_next_blocker, retained Luna low
  status: AUTHORIZED_PRIVATE_TEST_PACKAGING
  scope: Private permanent simulation and negative/synthesis regression bundle for frozen corrected CONTINUOUS-STRING-ASSIGN; no compiler/shared manifests edits.
  acceptance: Preserve original fixtures and expected semantic/diagnostic checks; provide installation map consistent with existing legacy/JSON patterns. Coordinator reviews and registers.
  lanes: Second implementation/test lane alongside private output-port fix; Caliptra observer is read-only diagnostic preparation.

multidist_permanent_packaging:
  owner: constraint_guard_semantics, retained Sol medium with solver fixture context
  status: AUTHORIZED_PRIVATE_TEST_PACKAGING
  scope: Frozen joint ordered randc multiple-dist fixtures and transactional range-cap/UNKNOWN checks; no source/shared manifests/build changes.
  acceptance: Real isolated legacy and JSON execution against private solver runtime, exact diagnostics and positive stdout; UNKNOWN injection remains platform-scoped with explicit macOS provenance.
  lanes: Replaces completed string packaging lane; output-port implementation independent.

port_permanent_packaging:
  owner: vif_proxy_implementation, retained Sol medium with port fixture context
  status: AUTHORIZED_PRIVATE_TEST_PACKAGING
  scope: Frozen VARIABLE-OUTPUT-PORT-DIRECTION tests, private permanent bundle only; no shared files/builds.
  acceptance: Actual isolated legacy and JSON runners, both editions, exact self-check/diagnostic oracles including selected/unpacked neighbors and synthesized scalar semantics.
regex_permanent_packaging:
  owner: coordinator
  scope: Existing strict_regex_exec fixture copied to future tests/uvm_regex_strict_exec_test.sv and compiled with bundled UVM package in isolated directory; existing real private DPI candidate.
  acceptance: Both editions semantic assertions, one expected legacy invalid-regex diagnostic; existing UVM auto-discovery means no new pipeline mechanism.

event_permanent_packaging:
  owner: constraint_guard_semantics, retained Sol medium
  status: AUTHORIZED_PRIVATE_TEST_PACKAGING
  scope: Frozen COMPOUND-VIF-EDGE-EVENTS fixture bundle, existing legacy/JSON and VPI runner conventions; no compiler edits or new shared builds.
  acceptance: Paired atomic/pulse/reconvergence, validity/null, automatic/rebind tests with exact oracles; VPI nested callback publications preserve assertions and run through appropriate harness. Actual isolated execution required.
  dependency: Worker vif_proxy_implementation provides retained private compiler/runtime locations and fixture mapping. Source remains frozen; coordinator integrates four event patch layers.
  lanes: Second test lane alongside port packaging; no new agents or worktrees.

shared_six_validation:
  status: FOCUSED_TESTED_BROAD_GATES_PENDING
  evidence: docs/conformance/session_logs/2026-09-21_shared_six_focus.json
  owner: Coordinator; shared source frozen during qualification.
  release_replay_preparation:
    owner: caliptra_next_blocker, retained Luna low
    scope: Only evidence/review-20260920/shared-six-release-replay-current/runner.py and preparation notes; no compiler, application, shared build or test execution.
    acceptance: Exact prior command verification, fresh compile, pinned clean corpora, tool hashes, serial timed runs, separate semantic failure classification.

next_pwrmgr_assessment:
  owner: vif_proxy_implementation, retained Sol medium
  status: READ_ONLY_ASSESSMENT
  scope: Only evidence/review-20260920/next-pwrmgr-after-six-assessment notes and reducers; no compiler or corpus edits, shared test/build execution, or application sweeps.
  objective: Identify next standards/API-grounded pwrmgr startup path blocker with a cheap reproducer after six-fix integration. Fresh application replay remains coordinator-owned.

shared_six_regression_repair:
  status: COORDINATOR_AUTHORIZED_PARALLEL_ROOT_CAUSE
  evidence: evidence/review-20260920/shared-six-qualification/integrated.log
  reason: Broad gate found 15 legacy regressions and one VPI failure after six-fix integration. No failure waiver.
  event_lane:
    owner: constraint_guard_semantics, retained Sol medium
    scope: Read-only diagnosis and isolated reproducers under evidence/review-20260920/shared-six-event-repair; event runtime history, activation, NBA and interface validity regressions. No shared edits/builds.
  port_lane:
    owner: vif_proxy_implementation, retained Sol medium
    scope: Read-only diagnosis and isolated reproducers under evidence/review-20260920/shared-six-port-repair; shortreal/output-port, partselsynth, packed struct reset and VHDL arithmetic regression paths. No shared edits/builds.
  acceptance: Standards-grounded root cause and minimal proposed patch with per-file ownership before implementation; preserve current failures and all new passing semantics.

shared_six_event_part_repair:
  status: COORDINATOR_AUTHORIZED_PRIVATE_IMPLEMENTATION
  owner: constraint_guard_semantics, retained Sol medium
  root: Automatic part/event indexes ancestor-owned state with incoming nested activation context; concurrent activations miss selected-local edges.
  files: vvp/part.cc; vvp/part.h only if necessary.
  scope: Private source/object overlay and patch under shared-six-event-repair, no shared source/build/install changes.
  acceptance: Concurrent activations, ancestor/fork, recursion, teardown, default activation, and prior new compound VIF pulse/callback semantics; assess remaining NBA/interface failures separately.
  dependency: elaborate.cc belongs to port lane; mixed class/VIF dropped dependency proposal goes to that owner and coordinator.

strict_regex_wrapper_regression_alignment:
  owner: Coordinator
  status: COORDINATOR_AUTHORIZED_TEST_REPAIR
  reproducer: Existing uvm_regex_legacy_glob_test fails current strict implementation with REGEX error; before.log preserved under strict-regex-wrapper-repair.
  authority: Pinned UVM1.2 src/dpi/uvm_regex.cc uses strict POSIX regcomp; bundled uvm_regex.svh has explicit deglob or glob conversion API.
  root: Historical test requires removed implicit fallback, contrary to selected strict-regex semantics.
  scope: tests/uvm_regex_legacy_glob_test.sv only; use explicit uvm_glob_to_re, retain positive/negative glob and normal regex checks. New strict rejection test remains unchanged. No application/compiler/library edits.
  acceptance: Actual real-DPI before failure and both wrapper/strict tests pass afterward, explicit conversion checked at runtime.

shared_six_port_repair:
  status: COORDINATOR_AUTHORIZED_PRIVATE_IMPLEMENTATION
  owner: vif_proxy_implementation, retained Sol medium
  root: Behavioral output carrier adds a scheduling boundary and loses initialization/same-process propagation for real/integer outputs.
  scope: Private elaborate.cc candidate and focused regressions only; no shared source/build changes. Compare removal of unnecessary carrier with existing coercion plus directional formal isolation before adopting type-specific exclusions.
  acceptance: All26 paired output cases, real/integer driver-conflict boundaries, shortreal runtime, VHDL immediate outputs and exact br_gh169b VPI. Preserve negative procedural/initializer conflicts and explicit-wire behavior.
  synthesis_boundary: Blanket disabling synchronous event marking under -S is not authorized; retained behavioral processes still require correct event semantics. Event worker owns runtime diagnosis.
publication_override:
  user_instruction: Open PR for substantial six-fix batch immediately after required local checks pass; do not wait for10 fixes.
  prerequisite: Repair current broad regressions, perform focused repair checks, then complete required local qualification on integrated source.

event_repair_source_liveness_expansion:
  owner: constraint_guard_semantics, retained Sol medium
  status: COORDINATOR_AUTHORIZED_PRIVATE_IMPLEMENTATION
  additional_file: vvp/event.cc
  evidence: Part owner-context repair fixes automatic_events2 but ancestor/history still fail; automatic edge/anyedge callback-cone guard rejects ancestor source before descendant fanout.
  scope: Validate incoming context against actual automatic_event_source_scope_, using existing source-scope/lifetime machinery; preserve stale/sibling rejection and downstream scope fanout.
  acceptance: Ancestor-to-descendant, concurrent activations, recursive generations, teardown and all event repair focus; no liveness bypass.

compound_mixed_event_guard_repair:
  owner: vif_proxy_implementation, retained Sol medium; event worker supplied root cause
  status: COORDINATOR_AUTHORIZED_PRIVATE_IMPLEMENTATION
  file: elaborate.cc
  root: ANYEDGE collector restricted to direct NetEProperty bypasses compound mixed VIF/object expressions and silently drops VIF dependencies.
  fix: Remove only direct-property restriction, allowing existing dependency collector to issue its focused unsupported mixed-dependency diagnostic.
  acceptance: Existing exact negative gold, supported mixed explicit-event-list positive, and pureVIF compound event regressions. No unsupported-to-implemented claim.

port_carrier_removal_review:
  decision: Full behavioral carrier deletion approved; retain existing lnet coercion and directional formal BUFZ isolation.
  evidence: All26 semantic port cases and real/integer immediate propagation, VHDL arithmetic and VPI enumeration pass. Restoring REG without carrier fails10cases; type-exclusion workaround is unnecessary.
  diagnostic_change: Existing lnet conflict error identifies offending procedural/initializer assignment and continuous driver. Intentional exactgold wording/location updates authorized after preserving old/new evidence; rejection semantics unchanged.
  owner: vif_proxy_implementation private elaborate.cc and associated exactgold only.

event_marking_boundary_repair:
  status: COORDINATOR_AUTHORIZED_PRIVATE_IMPLEMENTATION
  owner: vif_proxy_implementation private elaborate.cc; event worker supplied IR evidence
  root: RAII synchronous marking affected every event expression, changing ordinary NBA Boolean coalescing and leaving marked sensitivity cones in synthesis output.
  fix: Enable marked expression synthesis only when collect_vif_member_paths_ finds actual VIF reads; preserve complete mixed VIF expression marking. No scheduler changes or blanket synthesis disable.
  acceptance: OrdinaryNBA paired case, both failing -S cases, actualVIF40casefocus and runtimecontext additions.
  mixed_guard_boundary: Restore focused rejection of unsupported object-property plusVIF expression without rejecting newly supported ordinary-signal plusVIF expressions.

event_marking_boundary_review_failure:
  status: CANDIDATE_REJECTED
  evidence: VIF-only marking private eventfocus38of40; paired static interface Boolean pulse loses same-slot blocking transition.
  decision: Keep original pulse oracle and do not integrate VIF-only marking. No global synthesis exemption.
  next_assessment: Runtime owner evaluates region-aware marked-combinational publication using existing scheduler region and Active queue; source changes beyond approved part/event require concrete proposal. Static synthesis orphan issue remains separate.
  port_independence: Port carrier deletion and supported/unsupported mixed-dependency guard proceed separately; failed VIF-only experiment remains historical evidence.

event_nba_publication_repair:
  status: COORDINATOR_AUTHORIZED_PRIVATE_IMPLEMENTATION
  owner: constraint_guard_semantics, retained Sol medium
  files: vvp/event.cc and readonly NBA/ReNBA accessor in vvp/schedule.h, vvp/schedule.cc
  root: Marked combinational event cones recompute after each NBA source store, exposing intermediates instead of iterative Active evaluation. Blocking pulses require synchronous sourcepublication and cannot be unmarked by expression shape.
  scope: Reuse existing Active/active_sync mechanism to defer only marked combinational work from NBA/ReNBA, preserving normal ingress, validity, direct edges, callback publication and queue ordering. No scheduler ordering rewrite.
  acceptance: NBAcoalescing, directsignal NBA edgehistory, blocking pulses, NBA-from-NBA stages, NBA-to-Active interactions, nested VPIwrites during NBA, ReNBA scheduling, and pending automatic-context lifetime. Static synthesis defect remains independently outstanding.

event_deferred_context_generation:
  status: COORDINATOR_AUTHORIZED_PRIVATE_IMPLEMENTATION
  owner: constraint_guard_semantics, retained Sol medium
  additional_files: vvp/vthread.h and vvp/vthread.cc
  root: Deferred NBAcomb work may outlive an automatic activation; recycled pointer plus same scope passes prior liveness check. Retaining refcount incorrectly keeps logically dead activation live.
  fix: Assign unique nonzero generation on allocation/reuse; readonly getter returns0 for non-live context. Capture/check centrally when vvp_event_enqueue_comb queues work. No per-functor duplication or allocator/event coupling.
  acceptance: Cancellation plus same-scope storage reuse red/green oracle; pending work never affects replacement activation; all automatic event and callback regressions remain required.

event_literal_initialization_repair:
  status: COORDINATOR_AUTHORIZED_PRIVATE_IMPLEMENTATION
  owner: constraint_guard_semantics, retained Sol medium
  file: vvp/logic.cc
  root: Event-marked Boolean functor literal padding uses uppercase C4 constants scheduled in Active, after pushed waiters; initialized nonnull formal therefore exposes false cached validity before literal operands arrive.
  evidence: shared-six-port-repair/followup/lldb-validity-backtraces.txt; compile.cc input_connect uppercase versus lowercase constant initialization.
  fix: Reuse existing lowercase constant pre-simulation initialization for literal operands of event-synchronous functors only, after confirming exact literal source. Do not change null validation timing or global constant scheduling.
  acceptance: Interface cast startup, initially-null rejection, guarded inactive/active-null, conditional expression, all event focus; ordinary functor initialization unchanged.

synthesis_event_boundary_repair:
  status: COORDINATOR_AUTHORIZED_PRIVATE_IMPLEMENTATION
  owner: vif_proxy_implementation, retained Sol medium
  files: Private vvp-s.conf experiment corresponding to tgt-vvp/vvp-s.conf.in; synth2.cc read-only hardware-clock assessment pending concrete scope.
  root: Legacy synth pass lowers residual VVP behavioral body expressions to stale structural temporaries; synth2 separately reuses synchronous event-marked selectors as hardware clock inputs.
  first_fix: Test omitting legacy residual-expression lowering in VVP configuration while retaining synth2 hardware conversion and all behavioral event semantics.
  second_assessment: Give synthesized hardware clock ordinary structural propagation without changing surviving behavioral sensitivity cones; no global marker removal.
  acceptance: Original partselsynth and packed-struct asynchronous-reset regressions; paired same-slot static interface pulses under synthesis; shared surviving event and synthesized clock boundary.

publication_review_checkpoint:
  pr: https://github.com/dsellerbrock/iverilog-uvm/pull/309
  published_head: b03bccdf94e539eb5bbee34fd79092dfdc311af4
  state: DRAFT_LOCAL_REGRESSIONS_DISCLOSED
  clarification: Earlier coordinator publication prerequisite applied to ready-for-merge qualification; user-requested review draft is published with failures and pending checks explicit. Required repair, validation and merge gates remain unchanged.

event_constant_cone_initialization:
  owner: Coordinator
  status: IMPLEMENTATION_AUTHORIZED
  root: Literal initialization only at marked gates misses upstream constant BUFTs generated for event expressions; those constants also arrive after pushed waiters.
  files: expr_synth.cc, t-dll.cc, t-dll.h, t-dll-api.cc, ivl_target.h, ivl.def, tgt-vvp/draw_net_input.c; runtime literal initialization supplied by event lane.
  fix: Mark constants created inside event synthesis with existing compiler attributes; carry one target metadata bit and use existing lowercase pre-simulation constant emission. Ordinary constant timing stays unchanged.
  acceptance: Paired initial valid and genuinely-null events, guarded true/false/XZ validity, interface cast sensitivity, complete event focused suite.

synthesized_clock_expression_repair:
  status: COORDINATOR_AUTHORIZED_PRIVATE_IMPLEMENTATION
  owner: vif_proxy_implementation, retained Sol medium
  files: netlist.h, net_event.cc, elaborate.cc, synth2.cc; netlist.cc only if constructor implementation requires it; tgt-vvp/vvp-s.conf.in for residual-expression fix.
  root: Synthesized hardware clock reused event-synchronous selected expression and sampled before ordinary structural data cone settled.
  fix: Preserve an owned duplicate of elaborated event expression on its probe; resynthesize selected hardware clock with ordinary propagation while leaving surviving behavioral event cone unchanged. Reuse expression synthesis and marker restoration.
  acceptance: Packed-struct clock/reset runtime, original partselsynth, retained same-slot pulses, shared hardware clock plus behavioral watcher, paired editions.

windows_event_api_exports:
  owner: Coordinator
  status: COORDINATOR_AUTHORIZED_IMPLEMENTATION
  root: PR309 CLANG64 and UCRT64 fail linking vvp.tgt because nine new event/VIF target API functions are absent from ivl.def.
  scope: ivl.def only; add exact declared and defined exports. No semantic changes or toolchain workaround.
  acceptance: Compare every newly added target API declaration against definitions and unique export entries; exact-head Windows CI remains required platform evidence.
  evidence: evidence/review-20260920/pr309-initial-ci/assessment.json

next_opentitan_regex_assessment:
  status: READ_ONLY_ASSESSMENT_NOT_IMPLEMENTATION
  owner: Retained caliptra_next_blocker, Luna low
  objective: Verify concrete caller/conversion chain behind pinned GPIO/pwrmgr strict regex failures before classifying them as upstream-invalid source.
  scope: Read pinned OpenTitan/UVM and local wrappers; write bounded evidence under next-opentitan-regex-assessment only. No source edits or application modifications.

next_opentitan_regex_private_trace:
  owner: Retained Luna low
  status: COORDINATOR_AUTHORIZED_ASSESSMENT
  scope: Private umbrella copy and generated VVP copies under next-opentitan-regex-assessment/trace only; reuse existing regex trace helper in legacy entry point. No shared source, installed binary, application source, or test-output changes.
  reason: Several located source calls run after build, so they do not prove attribution of time-zero REGEX_INV errors. Capture exact runtime patterns before classifying the current blocker.

next_vpi_force_callback_assessment:
  status: COORDINATOR_AUTHORIZED_READ_ONLY_ASSESSMENT
  owner: Retained caliptra_next_blocker, Luna low
  scope: Canonical graph and current source navigation, local IEEE38.36.1 references, private reducers under evidence/review-20260920/next-vpi-force-callback-assessment only. No shared source, tests, builds, install or corpus edits.
  objective: Revalidate recorded compiled force/release callback statement-object fidelity and variable-bit registration debt on installed09316; provide smallest failing positive, rejection and boundary observations before next selection.

pr309_required_local_qualification_complete:
  head: 09316da3d48f2d0e24e710a47bc63e19f94a586a
  evidence: docs/conformance/session_logs/2026-09-21_pr309_local_qualification.json
  status: ALL_SEVEN_LOCAL_GATES_PASS_CI_PENDING
  session: 12449 completed exit0; do not poll or restart.
  next: Publish evidence-only commit, update PR, inspect exact-head CI; no merge without required local evidence and user-authorized green-CI boundary.
