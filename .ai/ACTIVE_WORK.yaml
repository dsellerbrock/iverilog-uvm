id: "L25"
title: "Integral function return variable used as a ref actual"
status: "in_progress"
phase: "reproduced"
base_revision: "e6ea5810f"
last_validated_revision: "e18128369"
reproducer: "evidence/campaign-20260908/l25/red.sv/red.json: result=7;bump(result) by ref must return12;both2017/2023 compile0 then fail with unresolvedfunctor placeholder and wrongreturn. Original1.0p1 build_coverage three sites match."
expected_behavior: "The function-name variable declared by13.4.1 is real variable storage;an equivalent-type ref aliases it under13.5.2,including observation during a call. Preserve return-expression override,recursive/automatic context ownership and existing ordinary-return semantics."
standards: ["IEEE1800-2017/2023 13.4.1 implicit function-name variable and return override;13.5.2 true reference,equivalenttypes;6.21 lifetime where applicable."]
root_cause: "tgt-vvp/vvp_scope.c omits integral return signal in favor of stackret;draw_ufunc.c ref_actual_is_nameable_ and task $ivl_ref_bind emission reference absent vSIGNAL_0. Runtime final-resolution placeholder is downstream symptom. Smallest correct addressable-storage design not yet chosen."
allowed_scope: ["Integral function-name ref actual storage/binding using existing return/context architecture;shared function/task call paths and regression tests. Broader changes require causal justification at deliberate boundary."]
non_goals: ["No placeholder suppression,fabricated return,copy-in/out pretending to be ref,unrelated1.0p1 callback cast/typeidentity fixes,string/real/nonintegral return qualification,fullsubroutine/UVM closure."]
required_gates: ["Both-edition failing reducer;truealias/read-during-call/recursive return/lifetime and return-override controls;independent design/code review;nativebuild/makecheck/focusedneighbors/integrated/JSON/UVM/NFA/releases;frontend last alone/restore/hashes. No source changes yet."]
next_action: "Trace return-slot and context lowering;choose smallest true-alias storage mechanism before source edits. Preserve callback cast issues separately."
next_command: "sed -n '338,355p' tgt-vvp/vvp_scope.c; sed -n '69,132p' tgt-vvp/draw_ufunc.c"
stop_after_completion: false
