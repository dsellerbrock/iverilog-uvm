id: ARRAY-PACKED-SELECT-INCREMENT
title: Preserve fixed-array word identity through packed-select expression updates
status: FOCUSED_TESTED
base_revision: d5c44575e
standards:
  ieee1800_2017: "7.4.6, 11.4.2, 11.5.1"
  ieee1800_2023: "7.4.5, 11.4.2, 11.5.1"
reproducer: evidence/array-packed-incdec-l68/review
current_behavior: "Legal packed bit/part ++/-- inside fixed-array words reach target generation and are rejected."
expected_behavior: "Capture the array word and packed index once; preserve word and bit neighbors, selected width and result context, typed invalid reads and suppressed invalid writes."
root_cause: "The SELECT target rejects array signal carriers despite retained word metadata; folded selected lvalues also need their array word preserved."
allowed_scope:
  - Extend selected integral expression load/store to fixed unpacked-array words
  - Preserve independent word/base registers and combine validity for existing partial array stores
  - Retain selected word identity during constant-fold reconstruction
  - Paired focused positive, boundary, negative and permanent regressions
forbidden_scope:
  - Class-property, dynamic-array or queue storage redesign
  - Drop writes or ignore invalid-index rules
validation_batch: 20260914_after_l64
focused_features_completed_in_batch: 4
pending_gates: "Broad batch gate at requested cadence; focused implementation and permanent checks passed."
prerequisites: "L67 focused revision57ccfa80d and main repair merged ind5c44575e. PR281 regressions remain live after successful platform builds."
next_action: "Preserve focused checkpoint and return to coordinator selection; continue observing published CI."
stop_after_completion: false

evidence: docs/conformance/session_logs/2026-09-14_array_packed_increment.md
