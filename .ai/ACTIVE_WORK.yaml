id: U01
title: Reproduce the current unmodified Darjeeling debug-xbar DV frontier
status: in_progress
phase: post_p04_unmodified_replay
base_revision: 53b58890c63a48aa1943c7ef065db59f19cebef3
standards:
  - Application DV and UVM execution; precise IEEE mechanism to be established from first divergence.
expected_behavior: The original xbar smoke test completes with checked matched request/response traffic and zero outstanding transactions.
observed_behavior: Fresh L07 smoke passes with115requests/230checked items and zero UVM warnings/errors/fatals. Preserved old executable repeats PASS/PASS/FAIL with4sequence-parent warnings; new executable repeats three passes. Repeatable teardown qualification remains open.
reproducer: lowrisc:dv:top_darjeeling_xbar_dbg_sim:0.1, xbar_smoke, pinned OpenTitan 7a3ad34b6d483f4d1d69ac670ddb1c45f1172e19.
allowed_scope:
  - Read-only application corpus and current harness/source inspection.
  - Fresh evidence/campaign-20260908/u01-after-p04 build/log/reducer artifacts; preserve earlier evidence.
  - .ai/ACTIVE_WORK.yaml
  - .ai/CAMPAIGN.yaml
  - docs/conformance/BLOCKERS.md
  - docs/conformance/session_logs/2026-09-08_continuous_campaign.md
non_goals:
  - No application source edits, disabled checks, weakened smoke traffic, or broad census.
  - No compiler implementation until a causal reducer and exact scope are established.
invariants:
  - Use campaign native tools, pinned Python 3.13/FuseSoC, fresh output root, original providers and source list.
  - Preserve 300-second per-process CPU guard and bounded wall time.
  - Compilation, reaching a later failure, or a pass banner without meaningful checking is not application qualification.
required_gates:
  - Corpus/toolchain fingerprints and exact command/source/provider provenance.
  - First divergence from current-source replay, with traffic and scoreboard evidence.
  - Any subsequent semantic patch must establish edition-specific semantics and full required regression gates before closure.
completion: U01 remains open until normal smoke completion and checked traffic are proven; a new earlier blocker requires a deliberate prerequisite boundary.
discovery_policy: Unrelated findings are record-only.
stop_after_completion: false
last_validated_revision: 53b58890c63a48aa1943c7ef065db59f19cebef3
selection_reason: Revisit meaningful unmodified DV after the validated coverage milestone; distinguish remaining simulator defects from pinned application or UVM-version teardown behavior without suppressing warnings.
next_action: Rebuild and run the original pinned xbar smoke in a fresh u01-after-p04 output root with validated P04 runtime. Compare source fingerprints, checked traffic and teardown; no warning suppression.
evidence:
  - u01-after-l07/result.json and source-comparison.txt;360exported files unchanged.
  - u01-after-l07/repeat-results.json; actual warning lines checked against severity summary, corrected4not5.
  - u01-after-l07/phase-guard-results.json and return-guard-results.json;5raw/5clean each,4warnings/0warnings.
  - u01-after-l07/phase-trace.log; unmodified source and executable with phase trace, clean checked completion.
compatibility_evidence: Pinned OpenTitan VCS/Questa configs select UVM1.2; campaign uses2020.3.1. Original tl_device_seq runs forever and xbar smoke does not explicitly stop it.
guard_mechanism: start guards its caller; phase cleanup kills phase processes; parent guard warns on terminal caller. Correct reduced warning behavior alone does not qualify application teardown.
diagnostic_limits: Added observer changes finish timing and is not qualification. Initial function-valued wait attempt woke too early and is invalid. Bounded delayed observer checks after the known completion time; warning-free repeats stay warning-free after an additional1ns, so deferred report timing is not established as the explanation.
next_command: "cat ../evidence/campaign-20260908/u01-after-p04/result.json"
