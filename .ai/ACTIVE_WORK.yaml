id: L50
title: Preserve empty implicit-sensitivity waits during synthesis
status: FOCUSED_VALIDATED
base_revision: 8e5a86047
origin: DD-024
standards:
  - IEEE 1800-2017 9.4.2.2 and 9.2.2.2
  - IEEE 1800-2023 9.4.2.2 and 9.2.2.2
reproducer:
  - evidence/dynamic-mixed-driver-assessment/synthesis-empty-sensitivity.sv
  - evidence/dynamic-mixed-driver-assessment/synthesis-empty-always-comb.sv
current_behavior: Synthesis classifies the permanent null-body wait used for an empty always @* by dereferencing its absent statement and crashes.
expected_behavior: Empty always @* remains an unsynthesized process that never executes, while constant-only always_comb executes at time zero and synthesizes constant logic.
allowed_scope:
  - NetEvWait asynchronous classification null-body guard
  - Legacy synthesis tokenizer null-body guard for the retained permanent wait
  - Focused synthesis/runtime tests, registrations, gold, evidence and ACTIVE_WORK
forbidden_scope:
  - Changing implicit sensitivity construction or runtime scheduling
  - Changing always_comb time-zero semantics
  - Broad suites before the campaign batch boundary
validation_batch: 20260914
focused_features_before_this: 7
last_validated_revision: Uncommitted L50 tree. Incremental build/install passed; focused legacy 2/2 and JSON 4/4 passed; empty always @* and always_comb null-target synthesis passed in both editions.
pending_gates: Broad suites remain deferred until the campaign batch boundary.
next_action: Root review and atomic integration, then L51 package-qualified task diagnostics.
discovery_policy: Other synthesis crashes remain separate debt.
stop_after_completion: false
