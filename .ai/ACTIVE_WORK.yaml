id: "L14"
title: "Hierarchical references to static locals in automatic scopes"
status: "in_progress"
phase: "awaiting_validation"
base_revision: "a85a256b1"
last_validated_revision: "a85a256b1"
selection_reason: "Original UVM1.2 now fails only four compiled_regex references in visit plus existing ignored-constraint warning; user prioritizes1.2 for OpenTitan."
expected_behavior: "Both editions6.21 explicitly permit hierarchical access to static variables inside automatic tasks/functions, except unnamed blocks. Actual automatic variables remain inaccessible through hierarchical names."
standards: "IEEE1800-2017 and2023 6.21,8.6; specific6.21 static-variable allowance governs despite general23.6 automatic-object wording. Primary local texts verified."
allowed_scope: ["Resolve named static local variable references according to actual declaration lifetime, including read/write and class method contexts.", "Preserve automatic-variable rejection, named/unnamed scope boundaries and existing lookup precedence.", "Permanent paired-edition regressions; required integrated gates and original UVM replay."]
non_goals: ["No DPI-regex symbols or ignored constraint fixes.", "No UVM source edits, application pass claim, or unrelated storage redesign."]
required_gates: ["Both-edition red reducer and positive/negative lifetime controls, focusedlegacy/JSON,independent review.", "Integratedlegacy/VPI/negative/runtime,fullJSON,real-DPIUVM,NFA,makecheck,release sweep,isolatedfrontend and restored tool fingerprints."]
next_action: "Await JSON26820,UVM66826,releases84865; run frontend last alone, verify restored tools and hashes before L14 closure."
next_command: "tail -n15 ../evidence/campaign-20260908/l14/json.log ../evidence/campaign-20260908/l14/releases.log"
stop_after_completion: false

root_cause: "symbol_search rejected every item in an automatic prefix scope before checking its declaration. Lifetime overrides on NetNet/PWire/NetEvent now govern variable access; defer through intermediate named scope and check terminal item. Explicit automatic declarations in static scopes remain rejected."
focused_evidence: "Both-edition baseline fails4errors; candidate reducer runsPASSED. FocusedJSON12/0,legacy12/0,neighbors18/0. Initial wrapper include and JSON diagnostic-path configuration errors corrected without compiler changes; final focused3.log retained."

candidate_revision: "69ff60cc6"
live_gates: {json: 26820, uvm: 66826, releases: 84865}
validation_checkpoint: "makecheckexit0; final scoped review clear. JSON after integrated terminal, frontend last alone. Verify root restoration and evidence/l14/sha256.txt before closure."

l14_integrated_terminal: "21445 exit0;legacy4905total4900pass0fail2NI3EF,VPI105/0,negative149/0,runtime15/15,copyout6/6,exports66. JSON26820 started after terminal. UVM66826 and releases84865 live; frontend unstarted."
