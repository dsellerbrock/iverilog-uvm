id: "L17"
title: "Integral compound updates of dynamic-array class properties"
status: "in_progress"
phase: "spec_grounded"
base_revision: "8b7c35ba5"
last_validated_revision: "5ba60567f"
reproducer: "DD035 five original1.2 register-library compound integral array-element updates emit object-RHS null fallbacks; original1.2 OpenTitan overallDEBT despite runtime checking."
expected_behavior: "Update the selected integral dynamic-array property element using the compound operator, with correct width/signedness, one index evaluation, preserved receiver identity and invalid-index no-write semantics. Never replace the array property with null."
standards: ["IEEE1800-2017/2023 11.4.1 compound assignment equals corresponding blocking update with LHS index evaluated once; verify both editions11.8 sizing and7.4.6 invalid-index semantics."]
root_cause: "show_stmt_assign_sig_prop_darray_index rejects every nonzero compound opcode by returning-1, which caller interprets as whole-array assignment and sends integral RHS to object evaluator. Existing typed container load/store and vector operator helpers can implement the selected element update."
allowed_scope: ["tgt-vvp/stmt_assign.c integral selected dynamic-array property compound path; permanent paired-edition tests and required validation; directly affected docs."]
non_goals: ["No original UVM/OpenTitan source changes, new runtime architecture, general real/string compound feature, or unrelated queue/assoc rewrite."]
required_gates: ["Red reducer and exact no-fallback runtime result; both-edition permanent operator/index/width/receiver/bounds cases; focused neighbors and independent review.", "Native build,integrated/JSON/UVM/NFA/makecheck/15release sweep;isolatedfrontend last/restoration/hashes;original1.2 OpenTitan replay after validation."]
next_action: "Establish reducer, verify existing typed container opcodes and index error flag semantics, implement smallest correct integral path."
next_command: "sed -n '3855,3949p' tgt-vvp/stmt_assign.c"
stop_after_completion: false
