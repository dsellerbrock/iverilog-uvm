id: "L36-L40"
title: "Phase-2 completion: fixed-array locators, bit-stream-cast crash, inside{} queue gap, chained container/string select, and a class-handle-degrade exemption gap -- all found by re-auditing every DD046 'loud diagnostic' disposition against a stricter bar (does it fire ONLY on illegal SystemVerilog?)"
status: "implemented, six-gate validation in progress"
phase: "phase 2 (compile fallbacks) close-out, per user-directed sequencing: smokes -> compile fallbacks -> implementations"
base_revision: "9ca238c53 (L35 closed, PR #276 merged)"
last_validated_revision: "pending -- six-gate sweep running"
parent: "User challenge: 'are the fallbacks all complete? Not just papered over by a loud compile diagnostic?' Then: 'fix array locator, fix the other 8, ensure FULLY done with fallback stubs, then report test suite/OpenTitan/Caliptra status before implementing.' Then, after L40's first fix attempt was reverted for a caught regression: 'Fix L40 Completely then fix any other failings.'"
blockers:
  - id: "L36"
    title: "Fixed-array locator methods rejected legal non-integral/non-zero-base arrays"
    reproducer: "evidence/campaign-20260908/l36/"
  - id: "L37"
    title: "Bit-stream cast to an unpacked struct crashed the compiler"
    reproducer: "evidence/campaign-20260908/l37/"
  - id: "L38"
    title: "inside{} silently forced membership to 0 for a queue-valued (non-property, non-plain-signal) expression"
    reproducer: "evidence/campaign-20260908/l38/"
  - id: "L39"
    title: "Chained container-index then string byte-select on a class property rejected as unsupported"
    reproducer: "evidence/campaign-20260908/l39/"
  - id: "L40"
    title: "A class-handle value read through a specialized parameterized class's own type parameter silently degraded to an empty string with zero diagnostic"
    reproducer: "evidence/campaign-20260908/l40/"
    note: "First fix attempt (wholesale in_class_scope -> in_unspecialized_param_class) was REVERTED when the UVM gate caught uvm_vreg::allocate() regressing. Root-caused via IVL_L40_DEBUG instrumentation: a circular forward-declaration (uvm_vreg.svh <-> uvm_mem_mam.svh) collapses that assignment's target to cast_type==IVL_VT_BOOL, the same mechanism the guard's comment already documents for LOGIC. Re-fixed with a narrow additional carve-out (in_real_specialized_param_class) that only hard-errors a REAL specialization, leaving in_class_scope's broad permissiveness (BOOL collapse included) untouched. Verified clean on both L40 reducers and uvm_vreg.svh."
allowed_scope: ["elab_expr.cc: L36 (array-locator dispatch), L39 (apply_trailing_container_indices_ netstring_t branch), L37 (PECastType::elaborate_expr non-packed fallback).", "tgt-vvp/eval_vec4.c: L38 (missing IVL_VT_QUEUE check).", "netmisc.cc: L40 (class_rval_degrade_ok gains a narrow in_real_specialized_param_class carve-out; is_class_new/class_new_hard_error/in_class_scope/in_unspecialized_param_class all untouched).", "ivtest/regress-sv.list + ivtest/regress-vvp.list + ivtest/vvp_tests/*.json + 9 new ivtest/ivltests/*.v + gold files (both the Perl-harness combined-log convention and the JSON-harness split stdout/stderr convention, which differ in whether the source path carries a './' prefix): permanent regressions for all five, plus fixing two PRE-EXISTING stale JSON-manifest entries (sv_array_find_last_fixed_base_fail, sv_array_find_last_fixed_shape_fail) that L36's earlier work updated in regress-sv.list but not regress-vvp.list."]
non_goals: ["L37 explicitly does NOT implement general bit-stream casting (packed<->unpacked/struct/class per 6.24.3) -- scoped out per advisor as feature-sized; only converts an unbounded crash into a bounded sorry.", "Did not fix the newly-found, unrelated, pre-existing vvp/class_type.cc:536 class_property_t::get_string gap (integral class property read as string) -- recorded in DISCOVERED_DEBT.md, feature-sized, not attempted.", "Did not chase the newly-found $low()/$high() 'object code 615' runtime fallback surfaced while auditing Iteration -- recorded in DISCOVERED_DEBT.md as an unexplored candidate.", "Did not re-attempt eval_string.c:249 or the qualification fallback (.github/uvm_test.sh:139) -- both re-confirmed at their prior disposition, not re-investigated.", "Could not construct a minimal, standalone (non-UVM) reproducer for L40's BOOL forward-ref-collapse case despite two attempts -- the real UVM register-model source (uvm_vreg.svh/uvm_mem_mam.svh's circular typedef) is the only confirmed trigger, so that case's permanent regression guard is the pre-existing sv_package_lazy_subroutine_scope.v -uvm test rather than a new dedicated reducer."]
other8_audit: "See evidence/campaign-20260908/other8-audited/README.md for the full disposition of every 'other 8' candidate: 4 became real fixes (L36-L39, plus L40 found via a 5th path and fixed correctly on the second attempt); increment/decrement, events/waits, vvp_process.c:808, and the qualification fallback were time-boxed with reducers and confirmed either already-loud-and-correct or genuinely unreached; Iteration surfaced the new $low/$high candidate instead of resolving its original target line."
validation_contract: "Six-gate sweep (integrated ivtest, JSON/VVP, UVM, NFA dual-run, UVM release matrix, frontend alone/last) running over the combined L36-L40 changeset plus 9 new permanent ivtest cases. First ivtest run caught two gold-file path-prefix mistakes (my manual gold generation omitted the harness's actual invocation path form) and the L40 regression described above; both fixed. First JSON run caught two PRE-EXISTING stale regress-vvp.list/vvp_tests/*.json entries left over from L36's earlier (incomplete) update, now fixed -- rerunning JSON confirmed 1873/0 clean. Fill in final exact counts for all six gates here before closing this ticket."
discovery_policy: "This entire batch exists because the user directly challenged the prior session's 'loud diagnostic = resolved' assumption for DD046's Array locators/Casts/Membership rows -- each was re-checked against the precise bar 'does this diagnostic fire ONLY on illegal input', not just 'is there a cerr'. L40's own history inside this same batch is a second-order demonstration of the identical discipline: a fix that looked complete against hand-built reducers was caught wrong by the UVM gate, root-caused with instrumentation rather than reverted-and-abandoned (per explicit user direction to fix it completely), and landed correctly once the actual third scenario was understood. Direct confirmation of why this campaign requires all six gates, including UVM, before any fix is trusted, not just a hand-built reducer."
evidence: "../evidence/campaign-20260908/l36/, l37/, l38/, l39/, l40/, other8-audited/"
next_action: "Once the six-gate sweep completes clean: (1) fill in exact gate counts + frozen-hash diff in this file and BLOCKERS.md's L36-L40 combined-validation note; (2) commit with git commit -F <file> and push; (3) give the user the requested test-suite/OpenTitan/Caliptra status report; (4) only then take the user's confirmation to begin phase-3 implementation work."
next_command: "Run the remaining gates (UVM, NFA dual-run, UVM release matrix) sequentially, then frontend alone/last, after nothing else is installing local-install/lib/ivl/ivl concurrently."
stop_after_completion: false
