id: V01
title: Exact merged constructor-dependent value-bin universe
status: awaiting_validation
phase: integrated_validation
base_revision: 9218751e2
standards:
  - IEEE 1800-2017 and IEEE 1800-2023 19.11.3, with value-bin identity in 19.5.1.
expected_behavior: With merge_instances=1, bins b[] over [0:1] and [1:2] form three type bins; sampling 0 and 1 covers two, yielding 66.666667 percent.
observed_behavior: Both editions return100 percent on the LRM overlapping-range example.
reproducer: ../evidence/campaign-20260908/v01/union.sv
causal_hypothesis: type_coverage uses maximum registered family size raised to hit count rather than the complete union of registered bin identities.
allowed_scope:
  - vvp/class_type.cc and class_type.h dynamic value-bin universe registration and merged denominator
  - vvp/vthread.cc covgrp_dyn_states_ registration call (sole caller currently passes only count)
  - ivtest paired merged-bin regression coverage and lists
  - .ai/ACTIVE_WORK.yaml
  - .ai/CAMPAIGN.yaml
  - docs/conformance/BLOCKERS.md
  - docs/conformance/session_logs/2026-09-08_continuous_campaign.md
  - docs/conformance/matrices/ieee1800_2017_clause_matrix.md
non_goals:
  - No general coverage architecture redesign, unrelated options, transition/cross extension, or application edits.
  - Default merge_instances weighted averaging is a separate observed obligation; do not claim it implemented by correcting a union denominator.
invariants:
  - Preserve bin-name identity, ignored/illegal exclusions, unsampled instance contributions, cumulative instance lifetime and hit thresholds.
  - Never grow coverage universe only from observed hits or enumerate unbounded integral domains.
required_gates:
  - Paired red/green reducers for overlapping and disjoint partial domains, existing controls, and lifetime/threshold boundaries.
  - Applicable LRM semantics, causal trace and independent review.
  - Focused coverage, required integrated ivtest/VPI/negative/runtime, full JSON, real-DPI UVM and make check.
  - Remote CI before merge.
completion: Bounded explicit merged value-bin universe only; broader type-option model remains unqualified.
discovery_policy: Unrelated discoveries are record-only.
stop_after_completion: false
validation: Paired permanent test green and previous-runtime red; focus53 legacy/43 JSON, make check and independent review pass. Integrated and UVM pending, full JSON follows integrated.
