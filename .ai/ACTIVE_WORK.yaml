id: S07
title: Expand nested parameterized sequence aliases
status: in_progress
phase: reproduce
base_revision: 6f49fe1eb
standards:
  - IEEE1800-2017 and IEEE1800-2023 sequence declaration/argument rules; exact clause to verify before implementation.
expected_behavior: A named sequence alias containing a parameterized sequence invocation has the same temporal meaning as the directly expanded sequence, with each invocation binding its own actuals.
observed_behavior: Retained S04 reducer Left/Right aliases containing Pair(a,b)/Pair(c,d) report No function named Pair; direct invocations work. Reverify on current validated source.
reproducer: ../evidence/campaign-20260908/s04/window-nested-alias.sv
selection_reason: Reusable assertion sequence composition is a standards obligation and common DV pattern; existing direct-versus-nested reducer isolates an implementation gap without redesigning multiclock architecture.
allowed_scope:
  - Shared sequence declaration expansion, actual binding and directly affected normalizers in pform.cc and necessary declarations.
  - Permanent paired regressions, exact conformance records and required focused/integrated validation.
non_goals:
  - No unrelated SVA operators, multiclock S02, general formal lookup redesign or application source changes.
invariants:
  - Preserve lexical binding, independent actuals, recursion safeguards, expression ownership and temporal semantics.
  - Preserve existing sampling, cancellation and one parent verdict; no dropped calls or fallback default operands.
required_gates:
  - Current paired failing reducer and direct expansion controls; applicable edition semantics verified.
  - Causal trace, minimal fix and permanent paired binding/timing/negative controls.
  - Focused legacy/JSON both engines, affected neighbors,NFA,integrated,VPI,negative,runtime,fullJSON,real-DPIUVM,makecheck and independent review.
  - Remote CI required before merge.
completion: Close only the reproduced nested sequence expansion scope; broader SVA and formal/application qualification remain separate.
discovery_policy: Unrelated findings record-only; prerequisite tickets require deliberate boundary and preserved parent work.
stop_after_completion: false
last_validated_revision: 6f49fe1eb
next_action: Reproduce retained alias failure in both editions and compare direct expansion; query shared graph then trace sequence expansion.
