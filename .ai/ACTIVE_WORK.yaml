id: "L13"
title: "Preserve signed byte semantics of string character reads"
status: "in_progress"
phase: "awaiting_validation"
base_revision: "a83731194"
last_validated_revision: "9baa5a9c3"
suspended_parent: "L12; partial source patch, tests and contract preserved in evidence/campaign-20260908/l12. Restore only after validated prerequisite."
reproducer: "String containing octal377: assignment s[0] to int yields255, while s.getc(0) and byte cast yield-1. Both editions required."
expected_behavior: "Character selection yields signed byte, including widened assignments, arithmetic and typed arguments, consistent with getc. Ordinary packed vector bit/part selects retain unsigned semantics."
standards: "Both IEEE1800 editions6.16 table and6.16.3 require byte/getc equivalence;6.11.3 byte defaults signed. Verify all expression sizing paths against11.8."
root_cause_hypothesis: "resolve_type_ assigns atom2u8 to string indexing; ordinary NetESelect string path is untyped and unsigned. Audit relevant test_width/elaboration paths before changing shared typing."
allowed_scope: ["Shared string character result type, sizing and elaboration paths and permanent regressions.", "Paired-edition baseline/candidate and all integrated validation; documentation."]
non_goals: ["No L12 struct-member support until prerequisite validated.", "No UVM source edits, unrelated traversal/constraint changes, string part-select extension."]
required_gates: ["Paired-edition signed highbit ASCII/zero/bounds controls, widened assignments/arithmetic/formals and packed-vector unsigned controls.", "Independent review,focusedlegacy/JSON,integratedlegacy/VPI/negative/runtime,JSON,UVM,NFA,makecheck,isolatedfrontend,release smoke replay."]
stop_after_completion: false
next_action: "Await real-DPI UVM12635; then run isolated frontend and verify root restoration and width-sha256.txt before L13 closure. Resume L12 from evidence/l12/resume2 afterward."
next_command: "tail -n 15 ../evidence/campaign-20260908/l13/width-uvm.log"

root_cause: "resolve_type_ assigns unsigned atom2u8 after consuming a string index; shared untyped NetESelect constructor initializes unsigned. Both PEIdent width inference and lowered select must carry signed byte. Change only string character (width8) result, leaving vector select construction unsigned. Restored compiler fingerprints match U07 before candidate."

l13_live_gates: {}
l13_candidate: "f5c9a0305; JSON4/0,legacy4/0,stringneighbors40/0,independent review clear. NFA58/58 and makecheck passed. No installs or source changes during gates."

l13_candidate_revision_gap: "Parameter byte probe octal200,A,377 yields-1,-1,-1, revealing preexisting one-bit parameter selector hidden by sign extension. Within active result-width/type scope. Integrated77859/UVM67427/release76597 deliberately terminated143 before modifying compiler; no pass claim. NFA/focused results remain scoped to f5c9a0305. Route typed string parameter to string character NetESelect and fold constant strings as bytes, not packed bits; add permanent parameter coverage before rerunning gates."

l13_expanded_candidate: "Final focusedJSON6/0,legacy6/0,stringneighbors42/0; real-index rejection retained; independent review clear. Typed string parameter had been routed to packed one-bit selection; now NetECString plus signed byte select and constant byte folding preserve width/order/bounds. All required gates must be rerun for final source; interrupted initial runs are not passes."

l13_final_candidate: "9baa5a9c3; finalfocused6/0,neighbors42/0,reviewclear,makecheckpass. Fullintegrated4286,UVM41207,NFA21037 running; fullJSON/frontendunstarted."

l13_final_nfa_terminal: "21037 exit0;58passed0failed. Final makecheck also exit0; other required gates pending."

l13_integrated_terminal: "4286 exit0; legacy4887total4882pass0fail2NI3EF,VPI105/0,negative149/0,runtime15/15,copyout6/6. FullJSON99705 started after legacy/VPI terminal."

l13_release_terminal: "1174 exit1 due known release gaps; results-6pvdp6vh complete/baseline_valid.8SMOKE_PASS6COMPILE_FAIL1RUNTIME_TIMEOUT; every row status and source tree fingerprint matches validated U07. Source9baa5a9c3; full qualification still pending other gates."

l13_json_terminal: "99705 exit0;1779tests0failures. UVM41207 still live; frontend unstarted."

l13_uvm_terminal: "41207 exit0;355passed0failed0skipped,realDPI. Frozen fingerprints unchanged before isolatedfrontend78098 start."

closure: "9baa5a9c3;legacy4887total4882pass0fail2NI3EF,VPI105/0,negative149/0,runtime15/15,copyout6/6,JSON1779/0,real-DPIUVM355/0/0,NFA58/58,makecheck,focusedJSON6/0,legacy6/0,stringneighbors42/0,independent review,frontendS1-S10; installed root restored and frozen hashes unchanged"

reopened: "Narrow bit[1:0] index2 becomes signed -2 because cast_to_int2 returns BOOL expressions unchanged. Confirmed parameter result0 versus getc99. L12 resume2 patch/tests/contract preserved outside repository; dependent work suspended."

width_correction: "Explicit cast_to_width preserves source signedness, followed by separate signed32 NetECast for two-state conversion. Prior direct cast_signed mutated extension node and failed; corrected candidate focusedJSON6/0,legacy6/0,neighborsJSON42/0,independent review clear. Baseline9b fails both editions narrow unsigned test. All integrated gates pending."

width_gate_checkpoint: "Candidate849a779ea; integrated97512,UVM12635,release59096 live. NFA8558 exit0 and makecheckexit0. FullJSON after integrated terminal; frontend last alone; verify width-sha256.txt and root restoration before closure. L12 suspended in evidence/l12/resume2."

width_integrated_terminal: "97512 exit0; legacy4887total0unexpected,VPI105,negative149,runtime15/15,copyout6/6. JSON16047 running; UVM12635/releases59096 pending; frontend last alone. Next command: tail -n15 ../evidence/campaign-20260908/l13/width-json.log ../evidence/campaign-20260908/l13/width-uvm.log"

l13_width_json_release_terminal: "JSON16047 exit0,1779/0. Release59096 exit1 for known gaps: results-m_3ysaa5 complete/baseline_valid,8SMOKE_PASS6COMPILE_FAIL1RUNTIME_TIMEOUT. All15 status/source fingerprints/normalized compiler logs match previous9b sweep (width-release-comparison.json). Frozen installed hashes unchanged. UVM12635 verified live, frontend unstarted."
