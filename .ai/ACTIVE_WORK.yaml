id: "L21"
title: "Preserve rebound class handles during captured-object mutation notification"
status: "in_progress"
phase: "root_caused"
base_revision: "e7d5b40fb"
last_validated_revision: "fe12166bd"
reproducer: "evidence/campaign-20260908/l21/red.sv: scalar h.value+=rebind() updates captured original correctly but notification restores old h, undoing RHS reassignment."
expected_behavior: "Mutate the captured class object and notify its actual aliases; do not assign its old handle back into a variable rebound during RHS evaluation. Preserve existing property-change filtering and context/alias delivery."
standards: ["IEEE1800-2017/2023 8.4 object handles;11.4.1 compound update;9.4.2 event sensitivity where applicable;verify exact relevant semantics in both editions."]
root_cause: "notify_mutated_object_root_ unconditionally sends captured root_obj to root_net, even when RHS reassigned that source variable to another class object."
allowed_scope: ["vvp/vthread.cc captured class-root notification identity check using existing signal/context helpers; permanent paired rebind/alias/event controls and required gates;direct docs."]
non_goals: ["No L17 target restore yet; no general value-aggregate copyback, static overlay or scheduler redesign; no unrelated stale-root paths without proof or application-source changes."]
required_gates: ["Both-edition red/focus,receiver reassignment/null/alias/event and ordinary mutation controls;independent review;native build,makecheck,integrated/JSON/UVM/NFA/15release sweep;frontend last/restore/hashes."]
suspended_parent: "L17 originalpartial.patch plus resumed partial-after-l20.patch, contract and receiver-debug evidence preserved in evidence/campaign-20260908/l17; resumed patch reversed from worktree."
next_action: "Verify live class-root identity/context and alias notification semantics; implement smallest guard preserving rebound variable, then validate."
next_command: "sed -n '19414,19482p' vvp/vthread.cc"
stop_after_completion: false
