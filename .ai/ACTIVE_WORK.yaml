id: "L35"
title: "Assigning a string value to a real target silently substituted 0.0"
status: "closed"
phase: "done"
base_revision: "d0b6015bf"
last_validated_revision: "HEAD (L35 closed, all six gates passed)"
parent: "DD046 -- found via re-verifying the 'Lost expression typing' row's empirical-sweep disposition"
reproducer: "evidence/campaign-20260908/dd046/c22_real_string.sv (the defect); c22b_atoreal_ok.sv/c22c_int_to_real_ok.sv/c22d_real_to_real_ok.sv (positive paths unaffected); ivtest/ivltests/sv_real_string_assign_fail.v (new permanent CE regression)."
expected_behavior: "real r; string s; r = s; has no defined implicit conversion (IEEE 1800-2017/2023 6.16 defines only string<->integral and string<->string); this must be a compile error, not a silent 0.0."
root_cause: "netmisc.cc's elab_and_eval (both overloads), cast_type==IVL_VT_REAL branch, unconditionally stubs any type-mismatched tmp to NetECReal(0.0) with no exclusion for a well-typed STRING source -- unlike the adjacent BOOL/LOGIC/vectorable branch, which already excludes STRING per R30's identical fix for a vector target. Confirmed the naive alternative (falling through to cast_to_real()'s existing codegen) is also wrong: tgt-vvp/eval_real.c's draw_unary_real() would bit-reinterpret the string's packed bytes as an integer and convert that to real -- not a defined semantic either."
allowed_scope: ["netmisc.cc: added a tmp->expr_type()==IVL_VT_STRING hard-error check inside both elab_and_eval overloads' cast_type==IVL_VT_REAL branches, before the existing stub.", "ivtest/regress-sv.list + ivtest/ivltests/sv_real_string_assign_fail.v: new permanent CE regression."]
non_goals: ["Did not touch the STRING-target or CLASS-target stub branches in the same functions (probed, never fired, left as-is).", "Did not fix the second overload's BOOL/LOGIC/vectorable branch missing the same STRING exclusion its sibling has (netmisc.cc:2352-2358) -- noted in BLOCKERS.md as a possible third instance of the same defect class, unreproduced this pass.", "Did not touch the genuinely-unresolved/generic-type stub path the branch was originally written for -- a non-STRING tmp type still gets the compile-progress 0.0 placeholder."]
validation_contract: "All six required gates: integrated ivtest 4982/4977/0F/2NI/3EF (prior baseline 4981/4976 +1 new CE test), JSON 1873/0 (unaffected), UVM 357/0/0, NFA 58/58, releases 15/15 SMOKE_PASS, frontend all S1-S12. Frozen hashes show only local-install/lib/ivl/ivl (the frontend binary) changed vs the L34 baseline -- exactly as expected for a pure elaboration-time fix."
discovery_policy: "Methodology note recorded in DISCOVERED_DEBT.md: an empirical probe sweep finding zero hits is only trustworthy if the probe was fire-tested on a hand-built positive case first -- a probe that never fires and one that's never reached look identical. This reversed the DD046 re-triage's initial 'Lost expression typing: empirically unreached' disposition within the hour."
evidence: "../evidence/campaign-20260908/l35/ and ../evidence/campaign-20260908/dd046/c22*.sv"
next_action: "DD046's curated 12-row table (now effectively 13, with L35 split out) is fully exhausted for quickly-bounded candidates. Remaining options: (a) the second overload's un-excluded vectorable branch (netmisc.cc:2352-2358), sized small -- likely a 15-minute fire-test-and-fix if it reproduces; (b) mine the raw 12849-line fallback-tracked-candidates.tsv for genuinely fresh candidates (mechanical filter: stub/degradation-tagged lines in bug-dense files NOT near an existing cerr); (c) the real-typed (IVL_VT_REAL) nested class-property VPI-lvalue gap (L33/L34's named follow-on, sized as needing a new runtime class + grammar token); (d) shift to the user's third priority phase (broader implementation work) -- per advisor's most recent recommendation, this is likely the highest-value next step since DD046's own completion rule is now satisfied for all curated candidates and phases 1-2 of the user's stated order (smokes, then compile fallbacks) are both done."
next_command: "Either: (a) write a real=string-style reducer for netmisc.cc:2352-2358 (an assignment routed through the ivl_type_t overload with a BOOL/LOGIC/vectorable lv_net_type and a string source) and fire-test it before trusting either outcome; or (b) ask the user to confirm the phase-3 shift to broader implementation work, since the explicit instruction ('smokes -> compile fallbacks -> implementations') has now had both of its first two phases satisfied with evidence."
stop_after_completion: false
