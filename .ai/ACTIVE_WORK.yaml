status: in_progress
branch: agent/caliptra-param-consequent-repeat-20260923
baseline: PR329 head 098080821454e9d2119141290cd2408fc76f3a6d; stacked while PR328 and PR329 await CI
blocker: CALIPTRA-PARAM-CONSEQUENT-REPEAT
goal: Execute parameter-valued consecutive repetition inside an implication consequent with the bound resolved per instance, without dropping the property or changing its match and failure timing.
standards: IEEE 1800-2017 and IEEE 1800-2023 sequence repetition and implication semantics, including 16.9.2 and 16.12.7; confirm edition text before implementation.
failing_reproducer: evidence/caliptra-param-repeat-20260923/consequent_repeat.sv has start |=> !ready[*W] ##1 ready with W=2 and W=3; both editions exit 1 and drop the property.
positive_control: evidence/caliptra-param-repeat-20260923/literal_control.sv compiles with [*3] in both editions.
application_evidence: Pinned clean Caliptra v2.1.2 file src/ecc/formal/properties/fv_ecc_hmac_drbg_interface_constraints.sv has two independent dropped properties at lines 53 and 98 in both editions; standalone bind-target error is separate.
expected: Each true antecedent starts a distinct next-cycle consequent attempt; each of W repeat cycles must match !ready and the following cycle must match ready. Preserve false-antecedent vacuity, failures, overlapping attempts, four-state sampling, disable iff cancellation, per-instance overrides, and valid zero-bound behavior.
root_cause: pform.cc retains symbolic repetition as rep_kind 4, but sva_parameter_repeat_try_assertion_ accepts it only in an antecedent with a one-step consequent; the NFA refuses rep_kind 4 and the fallback drops this consequent sequence.
allowed_scope:
  - pform.cc focused instance-elaborated consequent lowering, reusing existing bound preflight and verdict/disable machinery.
  - Dedicated paired-edition positive, negative, and boundary SVA fixtures and gold files.
forbidden:
  - Caliptra or OpenTitan source changes; parse-time substitution of a parameter default; assertion drops or approximate capped semantics.
  - Claude-owned elaborate.cc, vvp/vthread.cc, vvp/vvp_z3.cc, and vvp/vvp_vinterface.cc without a documented scope expansion and ownership handoff.
  - Shared builds, installs, and regression output directories by an implementation agent; coordinator owns them.
acceptance: Executable W=2 and W=3 instance overrides with exact pass/fail timing; early and terminal failure; overlapping attempts and vacuity; NBA and off-clock disable; W=0 and invalid-bound behavior; both -g2017 and -g2023; pinned Caliptra file reaches no parameter-repeat drop. Focused neighbors and required broad gates before qualification.
dependency: PR328 merged at 4f66ed666; PR329 remains pending current-head CI. This branch is stacked on PR329 head but pform.cc work is source-disjoint. Publish a separate PR after local gates, then reconcile the stack after PR329 merges.
evidence: docs/conformance/session_logs/2026-09-23_caliptra_param_consequent_repeat_baseline.json
parallel_lane: PR329 queue-pop source and focused evidence are preserved at pushed head 098080821454e9d2119141290cd2408fc76f3a6d while current-head CI runs.
next: Review the locally qualified SVA patch, publish its separate PR, and monitor current-head CI. Do not claim Caliptra DV or full SVA qualification.
