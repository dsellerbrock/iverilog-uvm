status: in_progress
branch: agent/opentitan-queue-pop-20260923
baseline: PR328 head 902323fb10ce8d5d8f0d3d5e36a0f33ec71deb7f; stacked until PR328 merges
blocker: OT-SPI-QUEUE-POP-DROPPED
goal: Evaluate a parenthesis-free zero-argument queue pop_front on a class property exactly once, return the typed element, and remove that element.
standards: IEEE 1800-2017/2023 5.13 and 7.10.2.4; a direct built-in method call may omit empty parentheses, and pop_front removes and returns the first element.
failing_reproducer: evidence/opentitan-queue-pop-20260923/repro.sv compiles with a dropped-expression warning then fails its value/side-effect check in both editions.
positive_control: The same reducer with -DPARENS_CONTROL passes in both editions.
expected: The parenthesis-free and parenthesized calls have the same result and queue mutation; empty queue returns the specified nonexistent-element value without a mutation.
root_cause_hypothesis: PEIdent::elaborate_expr_class_member_ in elab_expr.cc drops a parenthesis-free class-property queue method; PECallFunction already emits the typed $ivl_queue_method$pop_front expression for the parenthesized path.
allowed_scope:
  - elab_expr.cc class-property queue method expression path and necessary width typing.
  - Dedicated positive, negative, and boundary regression fixtures and gold files.
forbidden:
  - OpenTitan or Caliptra source changes.
  - VVP runtime and Claude-owned constraint/VIF event-relay files without a demonstrated necessity and coordinator scope update.
  - Shared manifests, builds, installs, and output directories by an implementation agent; coordinator owns them.
acceptance: Focused -g2017/-g2023 result and side-effect checks; parenthesized control; empty-queue boundary; invalid-arity negative; neighboring queue regressions; no compile-progress drop.
dependency: PR328 remains open; this branch is stacked on its tested source/test head and must be rebased or retargeted after PR328 merges.
previous_candidate: docs/conformance/session_logs/2026-09-23_opentitan_nba_codegen_focus.json
stop_after_completion: Return patch and focused evidence to coordinator; select another ticket only after integration.
