id: "L41"
title: "Class-body import statement misdiagnosed on error recovery"
status: "closed"
phase: "phase 3 (implementation campaign), first increment after the OpenTitan/Caliptra rebaseline"
base_revision: "631bba6e8 (PR #277 merged, L36-L40 closed)"
last_validated_revision: "HEAD (L41 closed, all six gates passed)"
parent: "OpenTitan rebaseline after PR #277 -- re-verifying the goal file's 'parameterized-class inheritance blocking ac_range_check_env_cov' priority item against fresh census hard_errors data"
reproducer: "evidence/campaign-20260908/l41/l41_class_import.sv (the fix); l41_fn_import_no_class.sv/l41_fn_import_second.sv (the DD-018 tangent, confirmed pre-existing and unrelated); ivtest/ivltests/sv_class_body_import_fail.v (new permanent CE regression); real OpenTitan verification against ac_range_check_env_cov.sv and pwrmgr_base_vseq.sv."
expected_behavior: "import pkg::*; directly inside a class body is illegal (IEEE 1800-2017/2023 A.1.8/A.2.1.3 footnote); Icarus already rejected it, but must do so with ONE focused diagnostic naming the actual illegal construct, not a syntax-error cascade that blames later, unrelated lines."
root_cause: "parse.y's class_item production had no package_import_declaration alternative, so a bare K_import in a class body fell through to generic error-recovery rules (error ';' / IDENTIFIER error ';') instead of a dedicated check."
allowed_scope: ["parse.y: added package_import_declaration as a class_item alternative.", "pform_package.cc: pform_package_import() gained a dynamic_cast<const PClass*> check on the immediate enclosing scope, rejecting the import with a citation-bearing diagnostic when it fires (method-body imports are a different, non-PClass LexicalScope and are unaffected by this check either way).", "ivtest/regress-sv.list + ivtest/ivltests/sv_class_body_import_fail.v + gold: new permanent CE regression.", "docs/conformance/DISCOVERED_DEBT.md: DD-018 recorded (function/task-body import failing outright, unrelated pre-existing gap found while verifying this fix's scope, NOT fixed)."]
non_goals: ["Did NOT investigate or fix DD-018 (import anywhere inside an ordinary function/task body fails with a plain syntax error, independent of classes) -- genuinely separate, deserves its own dedicated session.", "Did NOT re-diagnose the line-19 cascade in ac_range_check_env_cov.sv (ac_range_check_env_pkg::access_type_e resolution) -- confirmed pre-existing in the ORIGINAL unfixed trace, unrelated to this fix, not investigated further.", "Did NOT touch the unrelated duplicate-module-declaration issue in pwrmgr_csr_assert_fpv.sv."]
validation_contract: "All six required gates: integrated ivtest 4990/4985/0F/2NI/3EF (prior baseline 4989/4984 +1 new CE test), JSON 1873/0 (unaffected), UVM 357/0/0, NFA 58/58, releases 15/15 SMOKE_PASS, frontend all S1-S12. Bison conflict counts identical before/after (563 shift/reduce, 1122 reduce/reduce) -- zero new grammar ambiguity introduced."
discovery_policy: "Caught and corrected an unverified claim in-flight: the first draft of the code comments asserted method-body imports were 'unaffected, still legal' without testing it. A quick probe found this false (they already fail independent of this fix or of classes) -- corrected the comments before closing and recorded the real finding as DD-018 rather than let an unverified assertion ship in the source tree."
evidence: "../evidence/campaign-20260908/l41/"
next_action: "Select the next Phase-B implementation-backlog item from tasks/todo.md (candidates: DD-018 function/task-body import; vvp/class_type.cc:536 integral-class-property-to-string; the $low()/$high() 'object code 615' fallback; eval_string.c:249). Commit and push L41, open a PR, then continue the implementation campaign per the user's standing directive."
next_command: "git add -A && git commit -F <msg-file> (L41), git push, gh pr create; then pick the next tasks/todo.md item."
stop_after_completion: false
