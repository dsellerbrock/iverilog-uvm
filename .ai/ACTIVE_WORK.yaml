id: CI-EXPR-SYNTH-STRCMP-HEADER
title: Declare strcmp directly in synthesis translation unit
status: LOCALLY_VALIDATED
base_revision: 5c0f5588e
reproducer: "PR280 Ubuntu22.04 and24.04 build jobs fail expr_synth.cc:1630: strcmp was not declared; compiler identifies missing cstring."
expected_behavior: "The synthesis translation unit builds with supported GCC/Clang toolchains without incidental transitive string declarations."
allowed_scope:
  - Add direct standard declaration header for existing strcmp use in expr_synth.cc
  - Rebuild affected object and run focused synthesis neighbors
  - Publish minimal repair for merged PR280 and verify cross-platform CI
forbidden_scope:
  - Change expression semantics or redesign synthesis
  - Fold separately committed L65 implementation into the CI repair
standards: "C++ standard-library declaration dependency; no IEEE semantic change"
last_validated_revision: "L64 local7gate qualification; PR280 merged with Ubuntu build failures. L65 preserved on agent/constraint-multidim-reductions-20260914 at2bb01d593."
pending_gates: "Affected build, focused synthesis, CI review on repair PR"
next_action: "Reuse implementation worker for direct include, root builds/publishes; independent worker reviews affected uses."
stop_after_completion: false

validation: "Serial native build/install passed; existing synthesis fixtures passed 2/2 in each edition using the legacy harness. Independent review passed. Cross-platform CI pending repair PR."
