blocker: OT-SPI-SELECTED-VIF-EDGE
status: REGRESSION_TESTED
base: 50cda4fdf; preceding event-list fix frozen with focused and mapped UVM gates passing.
objective: Implement correct selected packed VIF edge event semantics and remove the SPI compiler crash.
reproducer: evidence/review-20260920/spi-event-list-next/crash-assessment/reducer_vif_select_direct.sv
red: Compiler exits139 in 2012/2017/2023; full SPI LLDB stack NetESelect synthesize to NetPartSelect to NetPins pin.
clauses:
- IEEE 1800-2017 9.4.2 edge-sensitive events and 25 virtual interfaces
- IEEE 1800-2023 9.4.2 edge-sensitive events and 25 virtual interfaces
expected: A posedge of the selected packed interface bit resumes the waiter exactly once; nonselected changes do not; preserve four-state edge rules and live expression dependencies.
root_hypothesis: Structural event synthesis receives a dynamic class-property selector without a signal net; route through existing VIF/class event expression mechanisms instead of synthesizing the class index.
allowed_files:
- elaborate.cc
- expr_synth.cc only if a shared null-result propagation defect is proven; no suppressing valid event semantics
- private evidence/review-20260920/spi-selected-vif-edge/
- owning regressions and documentation at integration
forbidden:
- No application edits, queue type relaxation, assertion suppression, broad edge sensitivity or compile-only completion.
- Runtime scope revised below after confirmed missing selected-edge metadata; unrelated fixes remain forbidden.
lanes:
  implementation:
    owner: canonical_graph_refresh Terra/medium
    files: elaborate.cc and optional justified expr_synth.cc; private implementation notes
    task: Trace root cause, reuse existing mechanisms, smallest complete fix; report dependencies before scope expansion.
  tests:
    owner: vif_proxy_implementation Sol/medium
    files: private spi-selected-vif-edge/tests only
    task: Paired semantic positive/negative/boundary tests; no shared builds or harnesses.
coordinator: Source review, shared build/install/harnesses, standards decisions, permanent registration and integration.
acceptance:
- Both editions reproduce red then compile and execute meaningful selected posedge/negedge checks.
- Nonselected bit suppression, dynamic index changes, nested receiver/VIF rebinding and four-state transitions verified as applicable.
- Automatic context, cancellation, ordinary selected event control and invalid selections/type diagnostics covered.
- Fresh pristine SPI compile verifies crash removed without hiding remaining diagnostics.
- Focused adjacent and mapped UVM gates pass; broad suites at batch checkpoint unless risk escalates.

root_cause_confirmed:
  issue: VIF member synthesizes through proxy, ordinary class-property index does not; unchecked null offset enters NetPartSelect.
  guard_limit: Null guard alone skips event downstream and is not a semantic fix.
expanded_runtime_scope:
  owner: canonical_graph_refresh Terra/medium
  files: netlist.h, net_event.cc, t-dll.cc, t-dll.h, t-dll-api.cc, ivl_target.h, ivl.def, tgt-vvp/vvp_process.c, vvp/compile.cc, vvp/compile.h, vvp/vthread.cc, vvp/event.cc, vvp/event.h and directly necessary opcode declarations.
  design: Reuse synchronous event-expression evaluator and VIF signal registration/lifetime mechanisms; extend metadata/runtime only as required for full selected edge semantics.
  constraint: Unknown/out-of-range packed selectors yield X, which participates in the standard edge table; do not treat these as inactive.
  risk: HIGH runtime/event registration change; full applicable gates required before promotion.
  authorization: Explicit user crash-fix request plus coordinator root-cause scoped expansion; proceed without another approval.

resumed_after_qwen_evaluation:
  coordinator: Reviews existing source patch, owns build and all shared validation.
  regression_lane: eval_evidence_audit Luna/low owns private same-signal waiter regression only.
  integration_corrections: Export observer edge API on Windows and sort opcode table before first build.
  next: Serial build/install then paired selected-VIF semantic matrix.

post_build2_parallel_repair:
  runtime_lane: selected_vif_relay_lifetime Terra/medium owns vvp/vthread.cc relay allocation and teardown only.
  coordinator_lane: elaborate.cc null-VIF selected event routing and shared build/tests.
  regression_lane: Luna private same-signal regressions complete.
  evidence: tests/candidate-build2 has allocator assertion on VIF updates; null-at-arm unexpectedly exits zero.
  invariant: Bounded registration lifetime, safe fanout callbacks, no semantic suppression or per-occurrence leaks.

null_vif_root_cause:
  evidence: Generated recipe replaces null VIF property read with X, bypassing runtime error and blocking forever.
  coordinator_scope: tgt-vvp/eval_vec4.c plus recipe-active query in vvp_process.c/vvp_priv.h; no elaborator change needed.
  fix: Preserve raw property read within observer recipe so existing runtime null diagnostic terminates simulation.

called_function_null_boundary:
  red: null-function-review/results.json; null VIF inside called selector silently returns default in both editions.
  root: Function code is generated outside recipe emission, so compile-time recipe flag cannot guard its property reads.
  correction: Runtime property-null guard opcode distinguishes actual dereference from lawful null comparison and reuses observer diagnostic.
  scope: eval_vec4.c, vvp/codes.h, vvp/compile.cc, vvp/vthread.cc; paired negative and guarded-positive regressions.
  qualification: Initial broad run interrupted deliberately; no completed broad result claimed.

qualification_readonly_lane:
  owner: selected_vif_relay_lifetime Terra/medium
  purpose: Assess remaining pristine SPI queue-output type errors for next coordinator selection.
  allowed: Canonical graph/source reads and private spi-queue-type-next reproducers using installed tools.
  forbidden: Compiler edits, shared builds/harnesses, application changes; current implementation remains frozen.

seed3_user_requested_revalidation:
  authorization: User explicitly requests seed3 fix and investigation of seed validity and VCS quirks; necessary downstream checker correction authorized.
  owner: selected_vif_relay_lifetime Terra/medium, private assessment and focused tests only while selected-VIF compiler patch qualifies.
  scope: Audit existing clock-activity checker patch and replay isolated overlay; preserve pristine release and assertion strength.
  coordinator: Public vendor evidence, shared tools and final integration.
  selected_vif_checkpoint: Build6 private38/38, legacy171/171, JSON171/171; broad gates pending.

until_continuation_assessment:
  owner: selected_vif_relay_lifetime Terra/medium
  purpose: Trace reproduced late-drop continuous until failure in official OpenTitan checker overlay.
  scope: Read compiler sources and local normative clauses; isolated reducers only in evidence/review-20260920/sva-until-continuation-next.
  forbidden: No compiler edits or shared builds while selected-VIF build6 qualifies.
  next: Coordinator selects implementation only after current validated checkpoint or explicit safe suspension.

qualification_build6_final:
  state: REGRESSION_TESTED, documented; publication pending.
  evidence: docs/conformance/session_logs/2026-09-21_selected_vif_edge_focus.json
  all_required_gates: Passed; session29234 terminal0.
