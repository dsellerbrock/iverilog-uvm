id: PARALLEL-BATCH-L86-L87
title: Selected-element ordering and bounded multiclock antecedent semantics
status: IMPLEMENTING
base_revision: fd500e2b5
authority: "User authorized two existing cheaper implementation lanes, disjoint ownership, focused validation per fix and broad qualification near ten integrated fixes."
coordinator_owns: [All shared builds and installation, Legacy and JSON harnesses, Main manifests and documentation, Integration and review]
lanes:
  - id: L87-JOINT-SELECTED-ELEMENT-ORDERING
    parent: Z01
    agent: /root/constraint_abi
    status: REPRODUCED
    standards: "IEEE 1800-2017 18.5.9/18.5.10; IEEE 1800-2023 18.5.8/18.5.9; distribution interaction uses L85 shared subset."
    reproducer: evidence/batch-20260914-after-l84/next-selection/joint_element_order.sv
    actual: "Both editions compile, then reject legal selected integral array-element ordering because the joint stage map accepts only scalar property references."
    expected: "Distinct selected integral elements have distinct canonical ordering identities; sample complete legal stage projections and conditional fibers."
    owns: [vvp/vvp_z3.cc, elaborate.cc]
    test_prefix: sv_randomize_joint_element_order
    allowed_scope: "Bounded joint solve-before over statically selected fixed-array integral elements, including scalar/element mixtures. Reuse OrderRef identities and existing element ASTs, exact stage projection, L85 distribution preflight and transaction rollback. Also diagnose constant out-of-range direct fixed-array operands in the PEConstraintOrder vars_to_ir lowering in elaborate.cc; invalid ordering must not reach the ignored-constraint warning fallback."
    forbidden_scope: "Dynamic selection/size ordering, real ordering, randc ordering, new distribution families, parser/target redesign, unrelated constraint diagnostics, SVA files and weakened diagnostics."
    dependencies: "L85 integrated at9f416e09a with focused validation; independent of L86. Root owns old global_ordered_fail fixture/golds."
    acceptance: "Both editions: hand-derived marginal/fiber probabilities, distinct elements of same array, scalar/element mixtures, aliases, nonzero declared bounds, transitive/partial ordering, frozen rand_mode, L85 supported dist interaction, seeded replay, unsat/cap/RNG rollback, cyclic and out-of-range/unsupported references; focused solver neighbors."
  - id: L86-MULTICLOCK-BOUNDED-ANTECEDENT
    parent: S01
    agent: /root/constraint_wide
    status: IMPLEMENTING
    standards: "IEEE 1800-2017/2023 16.9.2, 16.12, 16.13.1, 16.13.3"
    reproducer: evidence/next-batch-after-l84/multiclock_range_antecedent.sv
    actual: "Both editions reject the bounded variable-length antecedent because the multiclock lowerer accepts only fixed-length boolean chains."
    expected: "Every matching antecedent endpoint creates its standards-correct consequent obligation; the two-endpoint witness yields exactly one failure."
    owns: [pform.cc, pform_sva_nfa.cc, pform_sva_nfa.h]
    test_prefix: sv_assert_multiclock_bounded_antecedent
    allowed_scope: "Finite constant ##[m:n] delay windows in otherwise fixed single-clock antecedents of multiclock implications, preserving every match, clock handoff, attempt identity and action semantics. Reuse existing multiclock lowering/NFA infrastructure. Coordinator authorizes the necessary parent-tagged dynamic handoff and per-parent pending/failure aggregation within the owned lowering files; reuse existing queue lowering where possible. No fixed backlog cap or truncated attempt identity."
    forbidden_scope: "Unbounded sequences, general repetition/operator expansion, solver sources, parser grammar changes, dropping alternative matches or weakening assertion semantics."
    dependencies: "Existing fixed-chain multiclock lowering and clock-flow semantics; independent of L85."
    acceptance: "Both editions: all matching endpoints, early/late consequent pass/fail, overlapping antecedent attempts, zero/equal/finite range boundaries, |-> and |=> handoff, coincident edges in both source orders, preponed sampling, disable iff, cover/assert action counts and focused invalid/unsupported bounds; existing multiclock/NFA neighbors. If public action/attempt identity requires more architecture, report exact invariant and scope expansion before proceeding; do not replace the property by unrelated public assertions."
file_conflict_policy: "No edits outside lane ownership; root owns shared builds, harnesses, manifests and docs. Preserve published PR283/284 branches."
validation_batch: 20260914_after_l84
focused_features_completed_in_batch: 1
last_broad_qualified_semantic_revision: 240dd5f6f
last_broad_qualification: docs/conformance/session_logs/2026-09-14_compiler_batch_l75_l84_qualification.json
stop_after_completion: false

coordinator_scope_refinement:
  l85: "A retained complete-range guard evaluated after a prefix draw can cause seed-dependent unsupported rejection. Prove every reachable prefix before sampling or implement exact conditional range behavior; no success-conditioned sampling."
  l86: "IEEE 16.12.7 and 16.14.1 require a single property-attempt verdict across sibling antecedent endpoints. Scalar request counts lose parent identity. A parent with two matches and one failed child must dispatch pass=0/fail=1. Dynamic parent-tagged transport and retirement are necessary because a paused destination clock leaves an unbounded backlog even with a finite antecedent. Architecture expansion is coordinator-authorized; fixed-path cloning is incorrect."

integrated_focused_fixes:
  - id: L85
    revision: 9f416e09a
    evidence: docs/conformance/session_logs/2026-09-14_joint_ordered_distribution_validation.json
    broad_qualification: pending near ten integrated fixes
