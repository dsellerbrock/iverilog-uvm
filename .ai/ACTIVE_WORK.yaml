id: "L24"
title: "Void function calls in dedicated void-cast statement form"
status: "in_progress"
phase: "focused_tested"
base_revision: "0a691c8e2"
last_validated_revision: "58edf8034"
reproducer: "evidence/campaign-20260908/l24/red.sv;original1.0p1/1.1a find_all/get_args calls each yield three void-cast-of-void errors."
expected_behavior: "The dedicated void'(function_subroutine_call); statement executes the resolved void function exactly once with ordinary receiver, argument, output/ref and dispatch semantics. It does not turn void calls into expressions or permit task casts."
standards: ["IEEE1800-2017/2023 13.5 Syntax13-3;AnnexA.6.9/A.8.2;13.4.1 contrasts expression use;6.24.1 ordinary expression casts exclude void"]
semantic_interpretation: "Both editions grammar admits function_subroutine_call without a nonvoid restriction;prose explains nonvoid discards but contains no prohibition for the dedicated statement. Permitted reading is an explicit grammar/prose inference independently reviewed,not a quoted affirmative sentence or vendor bypass."
root_cause: "PCallTask::elaborate_build_call_ resolves a genuine void function and retains normal call lowering,then unconditionally adds an error solely because void_cast_ is set. Remove only that overrestriction."
allowed_scope: ["Resolved user void-function statement legality in PCallTask shared call builder;paired effects/dispatch/output/ref and rejection controls,required gates."]
non_goals: ["No arbitrary void expressions/task casts,deferred assertion action relaxation,new runtime ops,library-specific behavior or UVM source edits;no full call/UVM qualification."]
required_gates: ["Both-edition reducer/call effects/empty/virtual/output/ref and direct-call parity;task-cast/void-expression/deferred-action negatives;independent review;native build/makecheck/integrated/JSON/UVM/NFA/releases;frontend last alone/restore/hashes."]
next_action: "Run required full gates on frozen candidate;JSON after integrated terminal;frontend last alone/restore/hashes."
next_command: "tail -12 ../evidence/campaign-20260908/l24/integrated.log"
stop_after_completion: false
